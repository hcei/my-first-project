# 使用说明

书画机械臂调试助手 —— 控制台程序 `Robot.exe` 与图形界面 `RobotGUI.exe` 的构建、运行与上机注意事项。

---

## 1. 这是什么

基于 Modbus RTU 的三自由度 Delta 机械臂，用刚性 3D 打印笔架上的动物毛毛笔书写汉字，并可自动生成配套山水线稿作画。

仓库提供两个程序，它们**共用同一套服务层、配置文件和审计链路**：

| 程序 | 用途 | 适用场景 |
|------|------|----------|
| `Robot.exe` | 控制台菜单程序 | 上机分步调试、单点/方向/Z 轴标定、批量协议验证 |
| `RobotGUI.exe` | Win32 图形界面（主页 / 设备连接 / 书写任务 / 书写平面） | 日常操作、任务预检与执行、结果查看 |

服务层（`gui_service.cpp`）统一负责状态快照、配置读写、串口连接、任务线程、结构化审计，所以两个程序看到的是同一份状态、写的是同一份日志。

> **书写前必须先备好汉字字库**（HanziWriter 数据），否则一输入汉字就会报“找不到 X.json”。字库不随 exe 打包，见下面第 4 节。

---

## 2. 构建

**依赖**

- Windows 10/11
- MSYS2 MinGW-w64 g++（默认路径 `D:\MSYS2\mingw64\bin\g++.exe`，如不同请改 `build.bat` 顶部的 `CXX`）
- 无需其他第三方库；`nlohmann/json.hpp` 已随仓库提供，构建为静态链接，产物不依赖额外 DLL

**一键构建**

```bat
cd /d <项目根目录>
build.bat
```

产物：

- `Robot.exe` —— 控制台程序
- `RobotGUI.exe` —— 图形界面

清理中间文件：

```bat
build.bat clean
```

> 仓库保留了 `Robot.vcxproj`（MSVC 工程），但当前只在 MSYS2 MinGW 下构建验证过，MSVC 路径未验证。

---

## 3. 快速开始（不接硬件先跑通）

两个程序都支持 `--dryrun`：**不打开串口，所有运动只生成帧并记录，不真正下发**。审计日志里记为 `mode=DRYRUN`。

```bat
RobotGUI.exe --dryrun
```

```bat
Robot.exe --dryrun
```

建议第一次先用 DRYRUN 走一遍：GUI 里输入文字 → 预检 → 开始书写 → 停止任务，确认排版与阶段推进正常，再考虑上真机。

> DRYRUN 仍会**读取字库**生成笔画，所以跑之前请先按第 4 节放好字库。

---

## 4. 字库（HanziWriter 数据，书写必需）

程序按**规范笔顺**书写，需要每个汉字的笔画中线数据（HanziWriter 格式，每字一个 `<汉字>.json`）。这份字库**不编译进 exe、也不放进 git 仓库**，需要单独下载到本机固定目录。

### 4.1 存放位置（关键）

代码里字库根目录写死为常量 `HANZI_BASE_DIR`（`robot_common.cpp`），默认值：

```
D:/objects/hanzi-writer-data
```

程序实际读取的完整路径是：

```
D:/objects/hanzi-writer-data/data/<汉字>.json
```

也就是说，字库的 JSON 文件必须放在 **`D:\objects\hanzi-writer-data\data\`** 这个目录下（注意中间还有一层 `data`）。例如写“书”字，程序会去找 `D:\objects\hanzi-writer-data\data\书.json`。

### 4.2 从 Release 安装字库

1. 到本仓库 GitHub **Releases** 页面，下载对应版本附带的字库压缩包 `hanzi-writer-data.zip`（约 9574 个常用汉字 JSON，解压后约 49 MB）。
2. 解压后得到 `data` 文件夹，把它放到 `D:\objects\hanzi-writer-data\` 下，最终形成：

   ```
   D:\objects\hanzi-writer-data\
   └── data\
       ├── 一.json
       ├── 二.json
       ├── 书.json
       └── ……（每个汉字一个 json）
   ```

3. 每个 JSON 需含 `medians`（笔画中线）字段，HanziWriter 官方数据默认满足。

> 若你的机器上没有 D 盘、或希望放到别处，见 4.3 改根目录。

### 4.3 更换字库根目录

字库根目录是代码常量 `HANZI_BASE_DIR`（当前 `D:/objects/hanzi-writer-data`），不是配置文件里的项。要换位置有两种做法：

- **改目录**：把解压出的 `data` 文件夹整体移动到 `D:\objects\hanzi-writer-data\data\`（最省事，无需重编）。
- **改代码重编**：编辑 `robot_common.cpp` 里 `std::string HANZI_BASE_DIR = "...";` 为你的根目录（其下同样要有 `data\` 子目录），再执行 `build.bat` 重新生成两个 exe。

### 4.4 缺字库的表现

- 输入某字后预检/书写报“**找不到 X.json**”或界面提示“找不到字形数据，跳过：X”，即该字 JSON 缺失或字库根目录不对。
- 处理：确认 `D:\objects\hanzi-writer-data\data\X.json` 存在；比赛现场文字为常用汉字，随包字库已覆盖，若遇生僻字需自行补齐对应 JSON。

---

## 5. 图形界面（RobotGUI.exe）

左侧固定导航四页（主页 / 设备连接 / 书写任务 / 书写平面），顶部紫色标题栏，底部状态栏。

### 5.1 主页

- **设备信息**：设备状态、连接类型、串口、通信参数、当前坐标、坐标依据、笔状态、当前模式、急停状态、当前任务、最近响应、最近 TX、连续失败、实际位置、资源占用。
  - 注意：`当前坐标` 是**最后有效 ACK 后的软件位姿**，不是控制器上报的真实位置；`实际位置`、`资源占用` 在协议未确认前恒为“未接入 / 协议待确认”，不要当读数用。
- **设备连接**：端口、状态、最近通信。
- **快捷操作**：系统复位、回中心点、发送测试点、查询位姿、刷新设备、急停、解除急停。
- **运行开关**：Dry Run、自动描边、高质模式、蘸墨功能、顿笔、运行日志记录；右侧可调 速度档（－/＋）、字间距、Z 偏移（改完点“应用”）。

### 5.2 设备连接

- 串口下拉框（协议固定 `9600 / Even / 8 / 1`，不可改）、连接设备、断开连接、刷新串口、发送心跳。
- 右侧连接诊断：串口句柄、最近 ACK、连续失败、读取超时、最近通信、最近操作。
- **端口注意**：下拉里列的是系统注册的所有串口，包含蓝牙虚拟串口等无关设备。只选你实际接的 USB-RS485 转接器对应端口（见第 9 节）。

### 5.3 书写任务

- **输入与排版**：左侧输入汉字或诗句；可调 **字号**（≥60 mm，比赛每字≥6×6cm）、**书写方向（字体朝向）** 四档（沿 Y 向下 / 沿 Y 向上 180° / 沿 X 向上 90° / 沿 X 向下 90°，仅整字旋转不改位置）、**每页字数**、**总页数**。
- **自由拖拽布局**：下方“实时轨迹”画布内，每个字块可**逐字拖拽**定位（自动磁吸对齐、夹取在可写视野内）；**每页布局相互独立**，用画布上方的翻页条 `◀ 第 k/N 页 ▶` 切换编辑哪一页。预览与书写所见即所得。
- **分页 + 翻页**：一页写完后，程序向翻页机构发信号并等待翻页完成，再自动书写下一页。**翻页已接真机**（2026-09-21）：蓝牙 BLE 透传 → STM32 + TB6612 → 直流电机拖纸，板子回 `DONE <档位> <时长>` 才算翻页成功（闭环）；画布上方翻页条第二行可调「蓝牙翻页」开关 / 档位 / 时长，全部持久化。**取消勾选**「蓝牙翻页」则回退为固定等待（「翻页等待(ms)」，纯模拟）。超过“每页字数 × 总页数”的文本会被截断，只写前若干字（界面有“超出容量”提示）。
- **实时轨迹面板**：任务运行时只画**当前页**的下发轨迹（紫=落笔、灰=抬笔、绿=真机实测点、红=游标）；翻页后自动清空、从空白重播。上方“四角标定”可输入纸面四角设备坐标并逐角“预览/保存”，用于标定可写区域。
- **操作**：**预检任务**（只算排版，不发任何运动指令）→ **开始书写** / **停止任务**；右侧显示当前阶段（预热/蘸墨/书写/翻页/描边）、字符与轨迹点进度、笔状态、蘸墨次数。
- 关闭窗口时若任务仍在运行，会先中止任务再退出。**停止任务**走急停通道：抬笔后结束，审计里为 `task_end result=canceled error_code=estop`；翻页等待期间点停止会**立即中止、不再翻页**。

### 5.4 书写平面

- 专调落笔 Z 深度，解决“书写平面低于桌面”。显示当前 Z；输入新 Z 后“**模拟悬停**”把臂移到 (0,0,Z) 目视确认笔尖高度、“**保存并应用**”固定后续所有书写深度并持久化（重启仍生效）。未保存前沿用默认三层落笔深度。

---

## 6. 控制台程序（Robot.exe）

启动后在项目根目录运行 `Robot.exe`（可加 `--dryrun`）。

**主菜单**

| 序号 | 功能 |
|------|------|
| 1 | 写字并自动 2D 描边（上方写字、下方作画，读取 `themes/` 最新 JSON） |
| 2 | 调整速度（v=1~6） |
| 3 | 复位到中心点 |
| 4 | 退出 |
| 5 | 调整 Z_OFFSET（mm） |
| 6 | 调整字间距（mm） |
| 7 | 打印当前 SAFE 区 |
| 8 | （保留）加载 calib.json —— 尚未接入 |
| 9 | 开关：写字完成后自动 2D 描边 |
| 10 | 2D 描边模式 |
| 11 | 开关：高质模式 |
| 12 | （保留）容量评估 |
| 13 | 根据文字生成山水线稿 JSON |
| 14 | 设置蘸墨位（记录 X/Y/Z） |
| 15 | 开关：蘸墨功能 |
| 16 | 调试菜单（上机分步验证） |
| 17 | 设置中心点（X/Y/Z） |
| 18 | 开关：日志记录（`logs/Robot_时间.log`） |
| 19 | Z 层校准（抬笔 Z、书写 Z，自动推算各层） |
| 20 | 设置设备 Z 行程范围（可动上下限） |
| 21 | 开关：顿笔（起收笔按压、点画深压） |

**调试菜单（菜单 16）**：1 单点发送 / 2 L 形方向与镜像测试 / 3 Z 轴步进标定 / 4 抬落笔循环 / 5 方框批量测试 / 6 急停演练 / 7 查询最后已知位姿 / 8 循环单点（逐行输入，`q` 退出）/ 0 返回。

**急停：按 `ESC`**。控制台只认 ESC（早期版本的空格触发已移除，避免输入文本时误触）。

---

## 7. 配置与运行时文件

以下文件都在**项目根目录**，且全部按**进程当前工作目录**解析：

| 文件 | 说明 |
|------|------|
| `robot_config.json` | 配置持久化：中心点、Z 层、Z 行程上下限、速度、字间距、自动描边/高质/蘸墨/顿笔开关、蘸墨位、书写平面 Z、四角标定 `corners`、分页与每页字块坐标 `page_cells` 等。控制台菜单与 GUI 的参数改动都会写入这里 |
| `calib.json` | **保留文件，尚未接入加载流程**；安全边界目前来自代码内常量与 `robot_config.json` |
| `last_task.txt` | GUI 上次书写任务的文本，下次打开书写任务页自动预载 |
| `gui_window.txt` | GUI 窗口尺寸记忆 |
| `logs/` | 运行日志与审计日志 |

> ⚠ **必须从项目根目录启动**两个程序。这些状态文件和 `logs/` 都是相对路径，从其它目录启动会读不到上次的任务与配置，审计日志也会写到别的地方去。（字库 `HANZI_BASE_DIR` 是绝对路径，不受此限，见第 4 节。）

---

## 8. 日志与审计

- **运行日志** `logs/Robot_<时间>.log`：由**控制台程序**启动时创建，记录全部输出与 DRYRUN 帧，带毫秒时间戳（菜单 18 控制开关）。GUI 自身不写这个文件。
- **审计日志** `logs/audit_<时间>.jsonl`：GUI 与控制台的结构化操作记录，首行 `event=session`，之后每个操作一行，逐条 flush 落盘。

审计字段：

| 字段 | 含义 |
|------|------|
| `source` | `GUI` 或 `CONSOLE` |
| `actor` | `HUMAN`（GUI 操作）或 `AGENT`（控制台/脚本） |
| `mode` | `DRYRUN` / `REAL` / `OFFLINE` |
| `operation` | 操作名，见下 |
| `operation_id` / `request_id` / `session_id` | 关联标识 |
| `parameters` | 操作参数（如书写文本、翻页页码） |
| `result` / `error_code` | 结果与错误码 |
| `tx_frame` / `rx_frame` / `software_pose` / `target_pose` / `ack_valid` / `retry_count` | 仅发送类事件携带 |

事件类型：`session` / `connect` / `disconnect` / `send_point` / `send_batch7` / `task_preflight` / `task_start` / `task_abort` / `task_end` / `page_turn` / `page_turn_result` / `corner_save` / `estop` / `estop_clear`。

**示例**（GUI 预检一个字）：

```json
{"actor":"HUMAN","mode":"DRYRUN","operation":"task_preflight","operation_id":"op-20260918_000443-1",
 "parameters":{"text":"书"},"request_id":"req-1","result":"ok",
 "session_id":"sess-20260918_000443","source":"GUI","timestamp":"2026-09-18T00:04:43.452"}
```

---

## 9. 上机前必读

1. **先 DRYRUN、再空载、最后落笔**。任何 Z 层或行程改动后都要重新验证。
2. **Z 可动范围实测为 -320 ~ -385**，超出即不可动。`Z_DOWN_HEAVY` 默认 `-388` **超出已实测点**，上机前务必用菜单 19/20（或“书写平面”页）按实际设备重新校准。
3. **保持硬件急停可及**：软件急停在串口等待期间不响应，只在下一次发送点检查；正式运行请手放在电源/硬件急停旁边。
4. **串口参数固定 `9600 / Even / 8 / 1`**，不要修改。
5. 运动前先看菜单 7 打印的 SAFE 区，确认工作区余量。
6. **蘸墨流程**（抬笔 → 沾墨 → 落笔）需要赛前反复验证；比赛要求书法与国画均自主蘸墨至少一次。
7. **翻页机构（2026-09-21 已接真机）**：蓝牙透传 → 电机拖纸，档位/时长可调。程序**不做偏移补偿**，下一页与当前页世界坐标完全重合，**走纸量必须≈版面高**（当前排版 字高 60 + 行距 8 = 68mm）——上机前须先用命令行发 `RUN<档位>,<时长>` 实测走纸距离并反算时长（详见《真机实测步骤.md》第 5 步）；偏差会导致整页错位。
8. 比赛执行期间禁止人工干预，所以所有参数必须提前固化、并留下审计记录备查。
9. **Dry Run 管不到蓝牙翻页**：`Dry Run` 只让串口/机械臂变成模拟，翻页走蓝牙独立通道。所以「Dry Run + 勾着蓝牙翻页」= **臂不动、电机真转**。要 100% 纯模拟请取消勾选「蓝牙翻页」。
10. **BLE 模块一次只服务一个中心设备**：GUI 翻过一次页后会一直占着链路，此时别再用蓝牙调试工具或命令行自检程序连它，否则必然失败（`open=4 SharingViolation`）。

---

## 10. 已知限制与常见问题

**输入汉字后报“找不到 X.json” / 提示跳过某字**
字库未放好或路径不对。程序从 `D:\objects\hanzi-writer-data\data\<汉字>.json` 读取，请确认该目录下有对应 JSON（见第 4 节）。这是最常见的“能开程序却写不出字”的原因。

**点“连接设备”后界面卡死**
如果选中的是蓝牙虚拟串口（注册表里为 `\Device\BthModemN`），Windows 打开该端口会等待 RFCOMM 连接、在内核里长时间阻塞，界面随之无响应；期间进程连 `taskkill /F` 都杀不掉，只能重启系统。**请只选你实际接的 USB-RS485 转接器端口**。（枚举串口本身已改为纯查询、不再打开端口，所以切到“设备连接”页不会再卡。）

**从别的目录启动，配置和上次文本都不见了**
状态文件与 `logs/` 按当前工作目录解析，请始终在项目根目录启动。

**界面显示“未接入 / 协议待确认”**
控制器是否提供真实位置、状态、报警、限位寄存器尚未确认。这些字段在协议确认前不是读数，不要当作设备状态使用。

**`calib.json` 改了没反应**
该文件尚未接入加载流程，安全边界仍来自代码常量与 `robot_config.json`。

**“运行日志记录”开关在 GUI 里看不出变化**
该开关的值会写进 `robot_config.json` 供控制台使用；实际的 `logs/Robot_*.log` 由控制台程序创建，GUI 只写审计 JSONL。

**没有强制单实例**
同时开多个 GUI 会各自持有串口与审计文件，请只开一个。重编覆盖 `RobotGUI.exe`/`Robot.exe` 前也要先关闭正在运行的实例，否则链接会报 `Permission denied`。

---

## 11. 安全边界（当前实现）

- 所有运动指令在下发前经 SAFE 区与 Z 行程校验，越界点被拦截并记录。
- 急停为常驻通道：GUI 急停按钮、控制台 `ESC`，任务运行中触发后抬笔并结束任务；逐点/逐块与翻页等待期都会轮询急停标志。
- 未接入硬件时的所有“读数”一律显示为“未接入 / 协议待确认”，不会伪造数值。
