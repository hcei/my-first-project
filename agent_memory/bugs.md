# 问题与风险

> 本文档维护已知问题与风险记录，供 agent 参考。
> 只保留当前有效条目，不写成无限追加日志。
> 发现无关问题只记录风险，不顺手重构或扩大范围。

## 已知问题
<!-- 格式：状态 | 描述 | 影响范围 | 复现步骤 -->

| 状态 | 描述 | 影响范围 | 复现步骤 |
|------|------|----------|----------|
| 已修复 2026-09-16 | 急停线程触发一次即 return（第二次急停失效）、_getch 窃取控制台输入、空格误触急停 | 真机安全、控制台交互 | 见修复提交（estop_poll 重写为 PeekConsoleInput 常驻轮询，仅 ESC 触发） |
| 已修复 2026-09-16 | inZRange 校验原始 z，未含 Z_OFFSET，偏移过大时越界指令可下发 | 真机安全 | 菜单5设 Z_OFFSET=-95 后 dryrun 写字：现报"点越界 Z=-415"并拦截 |
| 已修复 2026-09-16 | sendPoint/packPoint5 忽略 Point.speed（全局 SPEED_LEVEL 打包），蘸墨"上慢下快"不生效 | 蘸墨动作质量 | dryrun 开蘸墨，TX 帧速度字节现 0x00/0x02 交替 |
| 已修复 2026-09-16 | calli_end 分界按原轨迹下标传入，去重收缩与书法段重采样后未更新，混合轨迹会把描边点当书法逐点发送 | 混合轨迹发送 | 自测：书法段(含0.2mm去重收缩)+描边段混合轨迹，描边 8 点正确批量 7+1=2 帧 |
| 已修复 2026-09-16 | 书法区去重间距 1.5mm→0.3mm（小于 0.6mm 重采样步长），密集笔迹不再在重采样前丢失细节 | 书法点密度 | 自测：0.4~0.6mm 点距落笔段，重采样后保留 0.9/2.0/3.0mm 点（旧逻辑仅剩 0/2.0） |
| 已确认 2026-09-16 | 设备 Z 可动范围实测 -320~-385（-320/-330/-340/-350/-385 均可动，-310 及以上 ACK 但不动）；写寄存器 0x0008 即触发运动，协议帧格式正确 | 全部 Z 相关运动 | 调试1单点实测；Z 层/行程已改为可配置（菜单19 + config.json） |
| 已修复 2026-09-16 | 菜单3复位不动：中心 Z=-300 在设备死区（高于-320），旧 inZRange(-410~-250) 校验过宽未拦截；Z_UP=-320 恰在可动边界 | 菜单3/全部抬笔动作 | Z_UP 调至 -325 留裕量；inZRange 改按实测行程校验 |
| 待确认 | HanziWriter 数据 y 轴向下、代码未翻转，若机械臂 Y+ 朝纸上方则字上下镜像 | 首次真机写字 | 真机写"十"字验证方向（调试2 L形测试） |
| 待确认 | 源码默认 g_enableDip=false，但比赛要求书法和国画均自主蘸墨至少一次 | 比赛合规性 | 启动程序查看默认菜单状态 |
| 待修复 | calib.json 没有接入实际加载流程，设备/安全边界仍来自硬编码（Z 行程已先行可配置化） | 运动安全和标定 | 选择菜单 8 只显示保留提示 |
| 部分确认 2026-09-16 | Modbus 协议：单点写寄存器已上机验证（设备 ACK 且执行运动）；批量7点帧、坐标方向/镜像、速度编码仍未验证 | 全部运动 | 调试5 方框批量 + 调试2 L形方向测试 |
| 待确认 | 长纸张自动输送尚无机构、定位和控制方案 | 纸张布局和比赛稳定性 | 先完成固定纸张版本，再做纸张输送样机 |
| 待修复 | 现有布局默认偏向 5 列，七个最小 60mm 字符和画面区域可能无法同时舒展 | 比赛构图和合规性 | 重新设计为按实际工作区计算的 4+3 或 3+4 布局 |
| 方案已形成，待实测 | 尚不能确认控制器是否提供真实当前位置、状态、报警和限位寄存器 | 测量结果可信度 | 先确认寄存器映射；未知地址不得盲读 |
| 方案已形成，待确认 | 自动拖纸机构可能被组委会视为额外执行机构 | 比赛合规性 | 赛前向组委会书面确认；固定纸张版作为保底 |

## 风险记录
<!-- 格式：可能性 | 影响 | 描述 | 缓解措施 -->

| 可能性 | 影响 | 描述 | 缓解措施 |
|--------|------|------|----------|
| 高 | 中 | 仓库已跟踪 x64/ 构建产物（历史遗留），.gitignore 只能阻止新增 | 需所有者确认后执行 git rm -r --cached x64 |
| 中 | 高 | 急停在 sleep_for 期间不响应，仅在下个发送点检查 | 上真机时保持手触硬件急停/电源 |
| 高 | 高 | 正式比赛开始后禁止人工干预，蘸墨流程失败将导致判零或作品中断 | 蘸墨动作必须纳入自动流程并在赛前反复验证 |
| 高 | 高 | 20 分钟正式执行时间较短，通信响应和点数过多会拖慢书写 | 预先生成轨迹，优先使用已验证的 7 点批量协议 |
| 中 | 高 | 规则要求每字至少 6x6 cm，不能沿用小字号测试参数 | 布局算法固定最小字号为 60x60mm，并按试题字数重新排版 |
| 中 | 高 | 比赛试题现场抽签，不能只准备单一固定作品 | 预置规范笔顺字形和主题化画面模板，现场仅输入/选择抽到的内容 |
| 高 | 高 | 纸张输送打滑会导致整幅作品坐标漂移，且无人工纠偏机会 | 采用刚性纸张载台、机械基准、单向索引和输送后校验；未经连续测试不用于正式比赛 |
| 中 | 高 | 自动拖纸机构可能被裁判理解为额外执行机构，规则未明确说明 | 赛前向组委会书面确认；默认比赛版本优先采用固定纸张 |

## 本轮审查补充
| 状态 | 描述 | 影响范围 | 复现步骤 |
|------|------|----------|----------|
| 已修复 2026-09-17 | 预定位发送后才读取 `g_last_pose`，导致 `dXY` 恒为 0；作画发送结果和蘸墨结果被忽略；菜单部分输入失败后继续使用无效值 | 到位节拍、任务完成判断、配置安全 | 静态审查发现；已分别传播返回值、发送前保存旧位姿并统一拒绝无效输入 |
| 已修复 2026-09-17 | 预热固定使用 `(0,0)`，调试 L 形/方框/抬落笔循环缺少有限性和 XY 边界校验 | 预热定位、真机调试安全 | 静态审查发现；已使用配置中心点并增加参数校验 |
| 待验证 | ACK 丢失/超时后自动重试可能重放已执行动作；批量末批填充语义、控制器到位语义未知 | 真机运动安全、节拍 | 需协议文档、抓包和上机验证，不能由静态代码确认 |
| 待修复 | 急停未形成锁存故障状态，软件仍会尝试抬笔并可清除标志；`calib.json` 未接入统一运动硬拦截 | 急停恢复、安全边界 | 需明确硬件急停/人工复位和标定数据格式后实现 |
| 待修复 | Hanzi/Polyline JSON 字段类型和有限性校验不足；缺字会跳过；无任务点数/时间预算 | 输入可靠性、比赛完整性、GUI 预检 | 构造异常 JSON 或缺失字形可触发；后续增加预检 |

## GUI 阶段（2026-09-17）
| 状态 | 描述 | 影响范围 | 复现步骤 |
|------|------|----------|----------|
| 已修复 2026-09-17 | `gs::preflight` 先持有 `g_mu` 再调用同样加锁的 `session_id()`，`std::mutex` 非递归 → 同线程二次加锁永久阻塞，GUI 主线程“未响应”；`connect`/`go_center`/`send_test_point`/`heartbeat`/`start_write` 五处同源 | GUI 预检、全部快捷操作、任务启动 | limit：写文本后点“预检任务”即挂死；`_t3.cpp` 直调 `gs::preflight` 8 秒超时复现；gdb attach 栈停在 `pthread_mutex_lock`。修复：`g_mu` 改为 `std::recursive_mutex`（31 处 `lock_guard` 同步替换） |
| 已确认 2026-09-17 | 跨进程 `SendMessage` 传缓冲区（WM_SETTEXT / WM_GETTEXT）会挂死 GUI 主线程 | 自动化测试脚本 | `SendMessageTimeout(WM_NULL)` 3 秒超时 + `Responding=False`；结论：只允许 `PostMessage`，禁止跨进程 `SendMessage` 传字符串 |
| 已确认 2026-09-17 | 跨进程 `PostMessage WM_CHAR` 同样触发 GUI 未响应；非 DPI-aware 进程投递 WM_LBUTTONDOWN 无法切页 | 自动化测试脚本 | 连续两次实测（PID 12600/44008）；切页改用 DPI-aware 真实点击 |
| 待修复 2026-09-17 | GUI 未限制单实例；未处理串口在任务中途被拔出；`logs/` 与 `last_task.txt` 无轮转/清理 | 长期运行稳定性 | 静态审查发现；需在实现阶段补单实例互斥与日志轮转 |
| 待确认 2026-09-17 | GUI 仅在 150% DPI 下核验；100%/其他缩放下的控件布局与导航命中未验证 | 界面可用性 | 调整系统缩放后启动 `RobotGUI.exe` 复核三页 |

## GUI 布局缺陷（2026-09-18 修复）
| 状态 | 描述 | 影响范围 | 复现步骤 |
|------|------|----------|----------|
| 已修复 2026-09-18 | 主页“设备信息”网格下标写反（`col=i/2, row=i%2`），实际排成 7 列 × 2 行；第 3 列起全部溢出面板右侧并被后绘制的右栏面板盖住，15 个字段只有 4 个可见 | 主页设备信息面板 | 打开主页即见（字段跑到面板外／被遮挡）。修复：改为 `col=i%2, row=i/2`，与同文件快捷操作按钮的写法一致 |
| 已修复 2026-09-18 | 文本宽度超出固定文本框导致截断：“输入与任务预检”→ “输入与任务…”；“最近通信”→“最近…”；“速度档／字间距／Z 偏移”→“速…／字…／Z …”；“控制器真实位置”（147px）、“存储 / 内存 / 电池”（169px）放不进 96px 标签列 | 主页、书写页、设备连接页 | 目视三页即见。修复：标题框 200→300、右栏标签 70→88、参数标签 52→76；两个超长信息标签改为 4 字（实际位置／资源占用）；“坐标依据”取值统一为“软件位姿（最后有效 ACK）”（原带时间戳的写法 384px 排不下） |
| 已修复 2026-09-18 | 书写页“任务进度”预览弧线（140×120）超出预览框并与底部“比赛要求”提示文字重叠，遮挡“自主蘸墨”等字样 | 书写页预览区 | 目视书写页即见。修复：图形区避开底部提示行（`gfxBot = pv.bottom - 28`），半径按可用高度推算 |
| 已修复 2026-09-18 | 主页“字间距／Z 偏移”输入框靠一次性标志 `s_cfgInit` 在 500ms 计时器里赋值；离开主页再返回时控件已重建而标志已置位 → 输入框永久空白 | 主页运行开关参数列 | 启动 GUI → 切到别的页 → 切回主页，两个输入框为空。修复：初值改在 `CreateHomeControls` 内随控件创建赋值，并删除一次性标志 |
| 已修复 2026-09-18 | 设备连接页底部说明条高度撑满页面剩余高度，单行文字在其中垂直居中，表现为整块琥珀色空白里浮着一行字 | 设备连接页 | 目视设备连接页即见。修复：`noteH` 上限收紧到 72px，说明条按内容成条 |

## GUI 运行时状态文件的 CWD 依赖（2026-09-18 发现，未改动）
| 状态 | 描述 | 影响范围 | 复现步骤 |
|------|------|----------|----------|
| 待确认 2026-09-18 | `load_last_task_text()` / `save_last_task_text()` / `load_window_size()` / `set_window_size()` / `cfg_load()` / `cfg_save()` 以及审计的 `logs/` 全部按进程 CWD 解析相对路径；从非项目目录启动时读不到上次任务与配置，审计日志也会静默写到别处 | 配置持久化、审计链路完整性 | 从 `D:/Code/华五` 启动 `RobotGUI.exe`（此时 CWD 不是项目目录）：书写页输入框为空、预检报“请先输入文本。”；同目录启动则正常预载“书”。修复方向：按可执行文件目录解析这些路径——涉及控制台与 GUI 共享的服务层，需先确认是否要一并改动 |

## GUI 切页卡死（2026-09-18 已修复）
| 状态 | 描述 | 影响范围 | 复现步骤 |
|------|------|----------|----------|
| 已修复 2026-09-18 | **切到“设备连接”页后整个界面卡死（UI 线程永久阻塞，CPU 0%，进程无法用 taskkill /F 结束）**。根因在 `gs::list_serial_ports()`：① `QueryDosDeviceW(nullptr, names, 4096)` 在本机返回 0 且 `GetLastError=122`（ERROR_INSUFFICIENT_BUFFER）——本机 DOS 设备名列表长 47349 字符，8KB 缓冲区根本不够，于是走进 ② 的兜底分支；② 兜底分支用 `CreateFileW("\\\\.\\COM1..32")` 逐个**打开**端口探测，而本机注册的 6 个串口 COM3/4/5/6/8/9 全是蓝牙虚拟串口（注册表 `\Device\BthModemN`），**打开未连接的蓝牙串口会在内核里阻塞**（实测 COM4 阻塞 5.2s、COM5 无限阻塞）。该函数由 UI 线程在 `CreateConnectControls` 中调用，一阻塞界面就“未响应” | 设备连接页（切页即卡死）、刷新串口按钮 | 启动 `RobotGUI.exe` → 点左侧“设备连接”：窗口立即无响应。用文件探针逐段定位到 `cc:list_ports_begin` 之后不再推进，再在 `list_serial_ports()` 内打点确认卡在 `CreateFileW("\\.\COM5")`。修复：改为**只查询、不打开端口**——`QueryDosDeviceW` 按 `ERROR_INSUFFICIENT_BUFFER` 自动扩容重试（8K→16K→32K→64K），并补充注册表 `HKLM\HARDWARE\DEVICEMAP\SERIALCOMM` 枚举；彻底删除 `CreateFileW` 探测分支。同时给 DOS 设备名加了纯数字校验，避免 “COMDB” 之类被 `_wtoi` 转成 “COM0” |
| 已修复 2026-09-18 | **点“连接设备”曾会在 UI 线程上打开串口**（与切页卡死同源）。原 `gs::connect` 在持 `g_mu` 情况下 `g_port.open()`，选到蓝牙虚拟串口时 `CreateFileW` 在内核无限阻塞 → UI 线程卡死且 `g_mu` 被长期持有、连 500ms `snapshot()` 计时器一起阻塞。修复：改为异步 `gs::connect_async(port)`——前置校验（任务运行中/重复点击）在 UI 线程持锁快速完成后，把 `g_port.open()` 放到 detach 工作线程、**打开期间不持 `g_mu`**；线程仅在写审计与结果时重新持锁。连接状态经 `snapshot()["connect"]`（`pending`/`state`/`message`）回报，UI 点击后显示“正在连接…”，由 `WM_TIMER` 轮询到 `pending=false` 时刷新结果。`g_connect_pending` 原子标志防重复点击叠加多个挂起线程。仅改 `gui_service.{h,cpp}`+`gui_win32.cpp` | 设备连接页“连接设备”按钮 | 本机 6 个端口全是蓝牙虚拟串口；改后即便选中也会立即返回、界面保持响应（结果稍后经定时器回报）。`build.bat` 重编通过，`--dryrun` 启动 `Responding=True` 并优雅退出 |
| 环境备注 2026-09-18 | 本机因上述阻塞残留了 3 个 `RobotGUI.exe` 僵尸进程（PID 36508/36732/47116，0 MB、无窗口、`taskkill /F` 报“拒绝访问”），是卡在内核态无法终止的残留，重启系统才会清掉。排查时若 `FindWindow` 找到的是这类残留窗口会误判，应先确认窗口所属 PID | 排障环境 | `tasklist /FI "IMAGENAME eq RobotGUI.exe"` 可见；用 `EnumWindows` 枚举 `RobotGuiWnd` 类窗口并打印 PID 可区分 |
| 待确认 2026-09-18 | GUI 的“运行日志记录”开关只写 `robot_config.json`（供控制台读取），GUI 自身不创建 `logs/Robot_*.log`，只写审计 JSONL；因此在 GUI 里勾选该开关看不到任何日志文件变化 | 主页运行开关、日志 | 勾选后查看 `logs/` 无新增 `Robot_*.log`；`g_logPath` 仅由控制台启动流程赋值 |

## GUI 打开后整体/文字闪烁（2026-09-18 已修复）
| 状态 | 描述 | 影响范围 | 复现步骤 |
|------|------|----------|----------|
| 已修复 2026-09-18 | 打开 GUI 后界面与文字持续闪烁。两处根因叠加：① `OnPaint` 把背景 `FillRect`、标题栏、导航、页面、页脚及全部 `Text()` 直接画在 `BeginPaint` 屏幕 DC 上，无双缓冲，而 500ms 定时器每次 `InvalidateRect(g_st.hwnd,nullptr,FALSE)` 全屏重绘 → 背景先闪再叠字；② 主窗口创建样式仅 `WS_OVERLAPPEDWINDOW`，缺 `WS_CLIPCHILDREN`，父窗全屏 `FillRect` 覆盖按钮/输入框/下拉框子控件区域，子控件随之重绘。修复：`OnPaint` 改双缓冲（`CreateCompatibleDC`+`CreateCompatibleBitmap` 画完后一次 `BitBlt` 上屏，并释放内存 DC/位图）；`CreateWindowW` 加 `WS_CLIPCHILDREN`。`WM_ERASEBKGND return 1` 与 FALSE 擦除本已正确，未动 | GUI 主页/设备连接/书写三页整体 | 打开 `RobotGUI.exe` 即见闪烁；`build.bat` 重编后 `--dryrun` 启动，`Responding=True`、收 `WM_CLOSE` 优雅退出，闪烁消除 |

## 书写顿挫（写字一顿一顿，2026-09-18 修复，待上机确认）
| 状态 | 描述 | 影响范围 | 复现步骤 |
|------|------|----------|----------|
| 已修复(离线) 2026-09-18 | 机械臂书写沿笔画一路“点刹”。根因：书法落笔段 `motion.cpp:transmitTrajectoryWithSplit` 为“逐点绝对定位 + 发完再 `sleep_move` 固定空等”，每点 `estimateMoveMs` 取 `max(POINT_RATE_LIMIT_MS=60, t_xy, DELAY=35)` 作为额外 sleep，叠加在 9600 串口单帧往返(~42ms)之上，而 ACK 只代表寄存器收到不代表到位——机械臂在每 0.6mm 途经点减速到停、干等、再起步。全项目原无任何插补/前瞻/轨迹缓冲 | 真机与 dryrun 书写节拍、观感与耗时（单字数千点→数分钟） | 修复：方案A 补偿式计时——`estimateMoveMs` 改为返回“相邻指令目标间隔”，去掉 60/35ms 走停地板（仅留 `MIN_POINT_INTERVAL_MS=12` 下限），发送循环用 `wait_after_send(t0,…)` 从发送前时刻补偿等待到目标间隔（串口+运动耗时计入），拐角停顿折进间隔；保留抬落笔/Z 沉降等物理停顿。方案B——书法落笔段用 `resamplePolylineRDP`（Ramer–Douglas–Peucker，容差 `RESAMPLE_DEV_MM_CALLI=0.06mm`）误差有界抽稀，直段塌成两端点、弯曲与拐角保点；描边段仍定步长 1.2。**已离线验证**（`build.bat` RC=0 + 单测）：直线 121→2 点偏差0；R2~200mm 弧最大偏差恒≤0.06且急弯更密；纯点间隔 60→12ms。**未验证(须上机)**：控制器对相邻指令是到点全停还是可运动中接新目标连续走/缓冲，决定 A 的顺滑上限；9600 单帧 ~22ms 为硬吞吐天花板；方案C（书法走 batch7 队列）待真机小样。上机前空载/限位/手触急停 |
| 已否决 2026-09-18 | 方案B 曾用“转角自适应步长”（`resamplePolylineAdaptive`，按相邻点夹角在 min~max 取步长）：曲线探针证伪——HanziWriter 笔顺中线逐点转角极小，30mm 乃至 2mm 半径急弯的密采样点被判成“几乎直线”，仍按 ~2mm 大步长抽稀，会损失笔形保真 | 书写保真度 | 已删除该函数，改用误差有界的 RDP（方案B 现实现）。若日后要“直段稀疏+曲线致密”之外的需求，勿再走逐点夹角启发式，应以几何偏差为准 |

## 实时轨迹 / 四角标定（2026-09-19，须上机验证）
| 状态 | 描述 | 影响范围 | 复现步骤 |
|------|------|----------|----------|
| 待上机 2026-09-19 | `readPose` 用 0x03 读 0x0008×5，读回值语义未确认——是控制器实际位置、还是最后写入的目标值（若是后者，实测点=下发点，绿点叠加失去意义）。手册 6.1 只说“对坐标进行读操作”，未界定 | 实测点叠加的可信度、方向/丢步诊断 | 真机连接后：预览某角→readPose 看读回是否等于刚下发点；手动挪臂（若有 jog）后读回是否变化 |
| 待上机 2026-09-19 | `preview_corner` 抬笔移到角点用固定书写平面 Z（`g_writing_plane_z`），修“Y=-100 到不了”（原用 Z_UP 顶部 XY 收窄）。真实可达边界、以及移动路径是否会蹭纸，均须空载实测 | 四角预览可用性、真机安全 | 真机空载：逐角预览，确认能到位且不撞限位/蹭纸；手触急停 |
| 已验证(dryrun) 2026-09-19 | 四角标定 UI：角标签完整、值回填、连线绕质心极角排序成矩形（非八字）、提示不压字——`build.bat` RC=0 + `RobotGUI --dryrun` 截图核验 | 书写页四角面板 | 开 GUI→书写任务→看实时轨迹面板。注：DRYRUN 不移动臂、无实测绿点，仅能验 UI 与几何 |

## 分页 + 翻页（2026-09-20，须上机验证）
| 状态 | 描述 | 影响范围 | 复现步骤 |
|------|------|----------|----------|
| 待上机 2026-09-20 | 翻页走纸量假设“精确=版面高、下一页字块与当前页世界坐标重合”（用户确认）。若真机卷筒打滑/走纸量偏差，第 2 页起整页会累积偏移 | 多页书写落点准确性 | 真机两页：第 1 页写完翻页后，量第 2 页实际落点与预览框偏差；若偏则回头加“翻页偏移”参数（当前未做） |
| 待上机 2026-09-20 | 翻页后软件位姿 `g_last_pose` 不变（坐标重合前提下成立）；真机拖纸后臂的**物理**位置未变但**纸**动了，故复用坐标正确——前提须真机验证拖纸机构不动臂、且臂在翻页时已抬笔到安全高度 | 翻页安全、多页衔接 | 真机空载两页：翻页瞬间观察臂是否抬笔、拖纸时笔尖是否蹭纸 |
| 待接入 2026-09-20 | 翻页当前为**模拟等待**（`page_turn_wait_locked` 分片睡眠 `page_turn_wait_ms`），未接真实蓝牙模块。真实实现须在 `gui_win32.cpp::Run()` 锚点 `gs::set_page_turn_handler(真实信号+等完成回调)`；回调返回 false=翻页失败→任务 failed(page_turn) | 比赛真机翻页 | 蓝牙模块就绪后替换 handler；协议/超时/失败重试待定 |
| 已验证(dryrun+单测) 2026-09-20 | 翻页等待期点停止/急停立即中止不翻页（用户选定安全语义）：`verify_pages` 7.4 设 60s 等待、进入翻页阶段后 abort，实测 <5s 退出、停在第 1 页、第 2 页不写、error=estop | 翻页期安全 | 见 verify_pages.cpp 7.4（临时文件已移出项目，存 workspace .bak） |

## 笔画残缺（真机截断/缺笔）——第 2 轮真机验证后（2026-09-20 更新）
| 状态 | 描述 | 影响范围 | 复现/依据 |
|------|------|----------|----------|
| **已作废（第 3 轮实测推翻）** | ⚠ 本条已作废，勿再据此改代码：真机巡航实测（320mm 单指令）0.27/0.77/0.55/0.48/0.40/0.40s ⇒ **byte1(档2) 最慢、byte2..5 越高越快**，只有 byte0 符合手册"00 最快" ⇒「数字越大设备越慢」不成立。原推断（已不采用）：**速度编码反向**（详见 context.md「书写节拍」节）：档1→byte0=设备最快、档越大越慢，而 `speedLevelToXYmmPerSec` 假设档越大越快 ⇒ 高档＝设备慢+模型按快给时间＝**欠计时→拖影/截断**；低档＝设备快+模型按慢给时间＝**过计时→干等→连连看/抖**。**「抖」与「残」由此统一**，且**无法靠选档绕开**（调高档两者对着干） | 全部书写节拍与笔画完整性 | 代码（两处 `clamp(lvl-1)`）+手册（00 最快）+真机肉眼（速度1 最快、速度3 最慢）；**待补测：档1/3/5/6 的残缺对照（反转理论预测档6 最残）** |
| 已修复·真机已验证 | **Phase 1（Z dwell 解耦）已落地并真机有效**：`motion.cpp:100-103` 三个 `get_*` 去掉 `!g_enableDunbi` 短路与 `g_highQuality` 硬编码、只读 config；config 现为 `z_settle_ms=95/stroke_begin=0/stroke_end=0`、`writing_plane_z=-350`（**当前 `speed=6`**）。真机：**目 内横写出、无炸毛**（⇒ 通道 A 闭，且 dwell 95ms 在安全区），但 档2 下 里/口/千 仍残（⇒ 通道 B） | 端点 Z 过渡时间 | 真机 Phase 1/T2 对照 |
| 已修正归因（本轮） | **「里」属通道 B（XY 计时），不是通道 A**：T2（速度1、其余同 Phase 1）实测「**里标准**」⇒ 里**随速度变** ⇒ XY 侧问题。双方文档（《最终解决方案》§八/§九）曾把里归给"通道 A/Z 地板、慢速治不好"，与其自身 §十一 的 T2 结果矛盾，已在本轮复核意见中指出 | 归因、Phase 2 目标 | T2 单变量实测 |
| 已验证（地基） | **G1 三组：缺损确定性成立**（两张「欲穷千里目」缺/弱的笔同一批；五个「里」四个一致）；**几何被排除**（GUI 预览里「里」的「日」是正常高度方框 ⇒ 下发几何正确）；**供墨被排除**（每次任务前补满墨、缺损不随书写先后单调变化 ⇒ 供墨对照实验可撤销）。例外：「里#2」首笔竖整笔没写（5 个里唯一不同）判为 ~1/5 随机漏笔，待观察 | 归因可信度 | `test/test2` 真机 |
| 待确认（平整度，比四角预览更强的测法） | **「纸面平整已间接满足」结论偏乐观**：四角预览只测 4 个点，页面中部的局部凹凸看不到；而 G1 出现「**里#2 首笔竖整笔没写**」（1/35 笔完全无墨）——**整笔无墨更像"局部接触不足"**（纸的局部凹凸让笔尖悬空/压死），时序问题只会留拖影/淡痕。测法（零代码）：控制台 `调试1` 连发 `-160 -80 -350 1` → `160 -80 -350 1` = **一条 320mm 连续横线（单条 0x0008、无中间点、无每点停车）**，其**墨色沿 X 的变化 = 接触深度沿 X 的变化**（与计时完全无关）；沿线分段 ⇒ 平面假设破，先垫平/分区 Z | 平整度归因、G2 结论 | 控制台调试1 两次单点 |
| 陷阱（Phase 2 必读） | **"扫 v 找甜点"与"显式 C 项"是重复计，只能选一条**：路线甲＝测巡航 v（单指令长移动）+ 显式 `C`（C≈65~95ms，兜住短段加减速与串口地板）；路线乙＝不加 C、只扫一个"有效 v"（该 v 已被折进 C，会明显小于巡航速度、且随段长分布失准）。**两条同时用 ⇒ 过量等待 ⇒ 又抖回去**。另：`v` 不能在小移动上标定（7mm 移动仅几十~百毫秒，秒表测不了） | Phase 2 计时参数 | 量纲与重复计推理 |
| **已并入方案 v3（第 5/6 次往返）·写 diff 时注意 3 条** | **`C` 项形式与量级错了（其中"65~95ms"是本 agent 上一轮给的错数，已认）**：`wait_after_send(t0,…)` 只补睡到"从发送前 t0 起满 interval" ⇒ **每点周期 = `max(interval, 串口耗时)`**，串口 ~45ms **已经自动是下限** ⇒ 把 45ms 再塞进 `interval = d/v + C` 是**重复计**：v=60mm/s 时 d>2.7mm 就开始每点多等 C ≈ 77ms/点，169 点共多等约 13s，**每点都停 ⇒ 把"抖"请回来**。且由 Z 实测 25mm/60~70ms 反推 `a≈2.4e4 mm/s²` ⇒ `v/a` 仅 ~3~5ms（最短段真实耗时 17~36ms 也低于 45ms 地板）⇒ **不需要新增 `C` 键**。**正确最小改法：`g_min_point_interval_ms` 12 → 45~55ms + `speedLevelToXYmmPerSec` 换实测逐档值**（形式仍是 `base = max(min_point_interval, d/v_eff)`，与代码现状一致）；可选把 `v/a_xy`（若测得 a_xy 小）加进 `t_xy`，仍不加常数项 | Phase 2 Step 3 diff | `motion.cpp` `wait_after_send`/`estimateMoveMs` 语义 + Z 实测反推 |
| 写 diff 时补充（第 6 次往返，v3 已核正确） | ①**两张速度表要合并成一张**：`speedLevelToXYmmPerSec` 现为 `g_highQuality ? table_quality : table_normal`，但**设备不知道"高质"** ⇒ Step 1(a) 测的是"档→设备真实 mm/s"，与 `high_quality` 无关 ⇒ 只替换一张表会在开关翻转时用回未实测的表；应合成一张实测表，`g_highQuality` 此后**只**影响 `get_POINT_RATE_LIMIT_MS`（90/60，唯一使用点 `motion.cpp:398` 的 `move_up_to_and_wait` 守卫，量级可忽略）②**至少给一个档录像**：`a_xy` 判据要用"起步头 5mm 耗时"，秒表测不出 ⇒ 不能只在 `t_min<1s` 时才录 ③`move_up_to_and_wait` 的等待由 guard(250/600ms) 主导 ⇒ 改表后**不需要**跟着重扫。另两处小校正：Step 4 的"停下占比 26~41%"是旧 C=80ms 口径（按 45ms 重算更小，结论不变）；Step 1(a) 的 320mm 墨线要避开纸面中央。**delta 雅可比导致"位置影响速度"的顾虑已被 G1"五个里铺满整行、缺陷一致"排除** | Phase 2 diff、测量单 | 复核 `motion.cpp:98,107,398` + 方案 v3 |
| 建议补全（已并入方案 v3 §六） | **①缺验收判据**（"定义完成"）：选定档下同纸连写两遍「欲穷千里目」，35 笔逐笔对照基线 0 处缺失/截断/提钩、无炸毛、单字耗时 ≤ Xs；②**改前留底**：基线照片是 `-365/dwell=0` 时代，改代码前先用当前配置写一行留底、改后同纸再写一行（防深度+计时两变量混淆）；③**蘸墨合规复验**：Step1/2 全程 `enable_dip=false`，但比赛要求"书法与国画均需自主蘸墨至少一次且动作明显" ⇒ 计时定稿后须开 `enable_dip=true` 跑完整流程确认不撞纸/不拖墨/时间可控 | 调参何时停手、可比性、合规 | 方案 v3 §六 |
| 待确认（测量单，已复核） | **⚠ 速度档是全局 config（`SPEED_LEVEL` 单一键，`generateSingleCharTrajectory(ch, SPEED_LEVEL, …)`），一次任务内所有字必然同档** ⇒ "同一行 5 个字块各用不同档"**跑不出来**；档位交错只能**按行/按次**（5 行，每行一次任务，顺序 档2/档1/档6/档3/档2，首尾同档做漂移校核；建议插基准档成 7 行以分离 Y 梯度）。另：换文本（如「口千里」）会改变字序，而冷启动地板按字序生效 ⇒ 破坏与基线照片可比性，**沿用「欲穷千里目」**。另：审计每个 `send_point` 只有一个时间戳（在 ACK 后）⇒ 拿不到 send→ack 时长，只能取**相邻事件最小间隔**≈串口地板（用于确认 45ms 地板值，**不是**给 interval 加 45ms） | 档位实验设计、C 标定 | 复核 `gui_service.cpp:1139,1323` + `log_send_point` |
| 已量化（选型依据） | **放宽 RDP 容差只是"残余杠杆"**：真数据复算 0.5mm → 落笔点 −17%、**33 笔 0 笔塌成"两端一笔到底"**（每笔仍 2~7 次中途停）；0.8mm → −30%、2/33 笔成单段（这批中线是手绘抖动折线，顶点偏离多在 0.06~0.5mm）。⇒ 它减"停的**次数**（百分比）"，v/C 修"每停的**时长**（倍数量级）" ⇒ **先 v/C，RDP 排 Step 5 第一顺位**；拐角不会被削圆（按垂距保点），代价是弯笔折线化但横向误差 ≤ 容差（0.8mm ≈ 笔宽 1/5） | 平滑度治理顺序 | 离线复算（`_zoom_tmp` 脚本） |
| 方法学（待判） | **G1 双行照片上第 1 行偏干细、第 2 行偏饱粗**（同纸同配置）——但**不能排除是照片透视/光照**（第 2 行离镜头更近更亮）。若为真 ⇒ 平面/接触状态沿 Y 变化，且**跨行/跨次运行的墨色不可直接比较**。⇒ 任何 A/B 对比（档位、改前改后）应**写在同一行交错排布**（A、B、A、B），避免批间漂移与变体效应混淆 | 全部 A/B 结论的可比性 | 目视 `test/test2/d27d36*.jpg`；建议交错设计复验 |
| 待确认（文档一致性） | **《书写残缺-最终解决方案.md》是按时间累积的讨论稿，直接当操作单会被过期配置误导**：①§三 基准表写 `-365 / 120-70-90`，实际已是 §十 的 `-350 / 95-0-0`；②§五 写"Phase 1 待批准不实施"，实际已落地（§十）；③§八/§九 把「里」归通道 A 与 §十一 的 T2 矛盾；④§一 的"通道 A 40mm/45ms、Z 单程待量"已被 §九 实测更新；⑤"速度1 会自愈（仅里仍错）"在 §三/§八/§十一 出现三种结果，对应**三次不同配置**，需逐条标明配置。复核意见见同目录《书写残缺-最终解决方案-复核意见.md》 | 用户按文档执行会踩坑 | 逐节比对 |
| 已证实 | **Z 单程实测 ≈65ms/25mm**（60fps 录像 → 触纸；v_z≈385mm/s ⇒ 40mm 约 104ms），**且残留为圆点 ⇒ 纯 Z 行程无 XY 耦合** ⇒ "提"钩不是机械耦合、是 XY 时序 ⇒ **settle 是必须的**（第 1 轮"提钩可能来自 delta 耦合"的疑虑已排除） | settle 取值、提钩归因 | 真机 60fps |
| 待修（本轮新增） | **`estimateMoveMs` 缺"每段固定开销 C"**：即使把 v 标对，每段仍缺 串口地板 ~45ms + 加减速 ~20~50ms ⇒ 正确形态是 `interval = d/v_真实 + C`（C≈65~95ms），当前是 `max(12, d/v_model)` | 高档位残余拖影、平滑度 | 定性推理 + 实测 v_z 反推；C 待现场标定 |
| 已证实（结论，可省一次实验） | **ACK 语义 = 「寄存器已写入」，不是「已到位」**：Modbus `0x10` 标准应答；且若 ACK 代表到位则"截断"不可能发生（自相矛盾）⇒ **审计 `send→ack` 时间差不能用来测执行耗时**，速度标定只能走秒表/录像或 readPose | 所有基于 ACK 的到位判定、速度标定路线 | 协议语义 + 反证 |
| 已证实（推翻一个流传数据） | **「320mm 移动 0.1~0.2s」几乎肯定是错的**（=2.1m/s，与"每点必停"和总预算矛盾）⇒ 第 1/2 轮"60fps 数不了 XY 移动"的教训据此撤回：若实际是 1~3s，视频法/秒表法仍然可用 | 速度标定方法学 | 量纲核对；待秒表重测 |
| 待确认（污染项） | **"-350 下 95ms 安全、270ms 才炸毛"不是干净结论**：Phase 1 同时把深度从 -365 改到 -350（Z 行程 40→25mm），驻留时间与驻留深度同时变 ⇒ 若要硬阈值需固定深度只扫 dwell。另：`z_settle` 合适值随 Z 行程变（25mm≈65ms、40mm≈104ms）⇒ 建议 Phase 3 改为按 ΔZ 与 v_z 推导 | 炸毛阈值、settle 取值 | 变量未单值 |
| 待确认（深度未定论） | **书写平面该取 -350 还是 -365 未定**：-365 是第 1 轮确认过的"笔尖触纸"标定值，-350 是本轮改的（理由"目 短横能清晰写出"），差 15mm，影响 Z 行程、是否临界接触、墨量 ⇒ 需**重新标定并固定**，此后不再与计时同时改 | 全部质量、Z 相关参数 | 书写平面页视觉确认 |

| 状态 | 描述 | 影响范围 | 复现/依据 |
|------|------|----------|----------|
| 待修复（复核判定为主因） | **顿笔关时 Z 轴过渡没有时间**：`motion.cpp:100-102` 三个 dwell 在 `!g_enableDunbi` 时一律返回 0 → 每笔的落笔（40mm 下扎 Z_UP-325→平面-365）与抬笔（40mm 提起）各只分到 **12ms**（真机被串口往返撑到 ~45ms）。短笔画整笔 0.6~1.5s 而两端 Z 过渡需 0.4~0.8s ⇒ 笔整笔处在下扎/提起的瞬态 → **缺横、短横过短、围合太小、竖变提**。顿笔开之所以治好是给了 **626ms/464ms**（同 XY 分层点 = 端点强制重同步），与压力无关（`writing_plane_valid=true` 时顿笔开/关深度都是 -365） | 全部真机书写质量（比赛成品的笔画完整性） | 离线复刻 `motion.cpp` 逐条时间线（`D:/Code/华五/_zoom_tmp/one.py`）；三组真机对照 A/B/C |
| 待修复（本轮新发现） | **`high_quality=true` 时 `get_Z_SETTLE_MS/get_STROKE_BEGIN/END_DWELL_MS` 返回硬编码 160/110/120，忽略 config 的 `z_settle_ms/stroke_begin_ms/stroke_end_ms`**（GUI 书写路径全部 `isCalli=true`）→ 按项目约定"改 config 重启即生效"去扫参会**完全无效**，极易误判为"settle 无用" | 节拍扫参、所有 Z/端点 dwell 调整 | 静态核对 `motion.cpp:100-102` + `robot_config.json` 的 `high_quality:true` |
| 待修复（本轮新发现） | **RDP 抽稀在真数据上空转**：`resamplePolylineRDP(0.06mm)` 对 `欲穷千里目` 删 **0 点**（263→263；0.2mm 仍 0，0.5mm 才删 12%、笔形偏 0.49mm）。汉字中线本身是粗折线（段长 1.24~51.87mm、均 5~19mm）⇒ 每笔 3~9 点全部下发、逐个到点全停；"抽稀减点"这条路必须换策略（按弧长重采样）或放弃 | 书写顺滑度、每字耗时 | 离线复刻 + 容差扫描（`_zoom_tmp` 脚本） |
| 待确认（须上机） | **供墨是第三个混淆变量**：蘸墨关、笔头墨量有限；顿笔开（端点驻留久）与速度1（单位长度接触久）都会让落墨更实，与"给机器更多时间"在现象上不可区分 → 现有 A/B/C 三组**无法排除**"缺横其实是该段墨太干" | 归因结论强度 | 建议实验：同一文本在"充分蘸墨"与"开 enable_dip 每5字蘸"下各跑一次对比 |
| 待确认（须上机） | **笔画上有 mm 级周期点纹**（速度2 串珠状圆点、速度1 弹簧/线圈状且更明显、自相关 ~0.9mm 量级）——它**不等于命令点间距**（1.24~51.87mm），我倾向"Z 未到位 → 笔尖临界接触/粘滑"，也解释"速度1 更颤抖"；次要可能是机械/控制侧 mm 级周期振动 | 笔画质感、速度1 的锯齿观感 | 放大 `test/` 照片观察；判定实验：同路径扫速度（看点纹空间周期是否随速度变）、扫深度（浅档断续/深档连续则证实接触不足） |
| 待确认（须上机） | **`0x0008` 是抢占还是排队**：纸面出现"竖变提"的提钩 ⇒ 新指令会抢占正在执行的运动（排队+到点全停模型下不可能出现）⇒ 软件侧唯一的到位保障只有时间（settle 不是可选优化） | settle 是否"必须"、方案4（前瞻/缓冲）是否可行 | 建议实验：连发 A→B→C 三个远点（各隔 100ms、抬笔）肉眼/视频看是否依次经过 B |
| 已修正认知 | 简报"顿笔开=重压导致炸毛"在本次实验**不成立**：书写平面生效时深度恒为 -365；A 组炸毛应归因于"端点深压驻留 282ms + 同 XY 连续下扎"，而非更深的 Z。⇒ P0 改动（放开 dwell、去掉分层）的驻留 ~200ms **比 A 更轻**，炸毛风险应低于 A | 修法选择 | `hanzi.cpp:92` + `robot_config.json`(`writing_plane_valid:true`) |
| 陷阱（已证实，会让扫参白跑） | **冷启动地板按"每个字"重置**：`motion.cpp:207` `if (ms_since(t_start) < 1000) interval = max(interval, g_cold_start_min_ms=150)`，`t_start` 是每次 `transmitTrajectoryWithSplit`（=每个字）的开始 ⇒ **每个字的前 1~2 笔天然被保护**（≤150ms 的 dwell 被抬到 150ms）。任何 dwell 扫描若落在首笔上，改 40/80/120 都看不出差别 ⇒ 会被误判为"dwell 无用"。修：扫描必须在**靠后笔画**上做，或把 `cold_start_min_ms` 设 0（该键未被 HQ 覆盖，真正生效） | 节拍扫参、settle 实测 | `motion.cpp:207-211`；可用现有照片做免费 A/B（同一字靠后笔画应比前 1~2 笔更差） |
| 待确认（须上机，可能解释"里在速度1仍错"） | **书写平面 -365 在整页是否恒定**：纸翘曲/桌面不平/未压平 ⇒ 页面局部"悬空未触"或"压过头"，与速度无关 ⇒ 能解释"缺损集中在特定字/局部"以及"速度1 仍错"。**免费测法：按四角「预览」**（`preview_corner` 已按书写平面 Z 移动，`gui_service.cpp` 内 `float z = g_writing_plane_valid ? g_writing_plane_z : Z_UP`）肉眼比对四角触纸是否一致；再在整页画短横网格看缺损是否成**片状**（片状⇒平面/深度；随笔画几何⇒时序） | 全部书写质量的归因、深度标定 | 四角预览 + 整页网格 |
| 待确认（须上机） | **mm 级点纹是"纸/墨固定属性"还是"动态过程"**：宣纸/毛边纸的墨色不匀是最平庸也最可能的解释。**零成本判别：同一个字写两遍叠在一起**（拖拽字块磁吸对齐），同笔白点位置重合⇒纸/墨空间性；两遍不同⇒动态（接触/粘滑/振动） | 点纹归因、是否要动 settle | 同字叠写比较 |
| 待确认（须上机） | **纯 Z 竖直行程是否产生 XY 漂移**：delta 三臂联动 + 标定/尺度误差会让笔尖在 40mm Z 行程中画一小段 XY 弧，会同时污染收笔"提"钩、落点精度（"口不在人正下方"）与点纹。**测法：同 XY 落笔-停-抬笔**，看纸面残留是**圆点**还是**短线/弧**；是线则量长度 ÷40mm 得耦合率 | 收笔质量、落点精度 | 同 XY 落笔-停-抬笔 |
| 待确认（须上机，**是"提钩归因"的前提**） | **"提"钩有两个来源且在纸面痕迹上不可区分**：①抬笔指令抢占未完成的 XY 走行（⇒ 控制器抢占，settle 必须）；②纯 Z 行程自带的 XY 漂移（delta 耦合，机械问题）。判别只能用上一条的"同 XY 落笔-停-抬笔看残留形状"；**若②成立，"提"钩不属时序问题**，且会同时解释"口不在人正下方" | 归因、是否需要 XY 补偿 | 同 XY 落笔-停-抬笔（可与四角预览合并：蘸墨→四角「预览」留四角墨点→「回中心」→书写平面页输 -365→「模拟悬停」→「回中心」） |
| 待加测量工具（已证实缺入口） | **`readPose` 无任何控制台/GUI 入口**：唯一调用者是任务线程 `gui_service.cpp:1191`（每字一次）；控制台 `调试7` 打印的是 `g_last_pose`（**软件位姿，非设备回读**，用它测会得出"读回=写入值"的假结论）。⇒ "readPose 语义"与"连发 A→B→C 各隔 100ms"两项实验**在现有软件上跑不了** | readPose 闭环可行性、控制器抢占/排队判定 | 建议加 `调试9 读坐标（readPose 连读 N 次带时间戳）`、`调试10 快速三点（各隔 100ms）`；放控制台（单线程+串口独占）且不改书写行为 |
| 方法学（已证实） | **单张纸的结论不能当定论**：全部推理建立在三组单张纸照片上。**P0-0 复现性实验**（同文本同配置跑两遍两张纸，看缺的是否同几笔）是加地基与判定"修好没有"的基准，应排在所有实验之前 | 归因可信度 | 同配置跑两遍比对 |

## 笔画残缺 · 第 3 轮（Step0/1 真机 + 审计日志复核，2026-09-21）
| 状态 | 描述 | 影响范围 | 复现/依据 |
|------|------|----------|----------|
| **已证实（推翻旧口径）** | **真机每点实际预算 ≠ 模型表列**：审计 3063 条 `send_point` 实测落笔走行中位 **档1 193/186、档2 155~156、档3 153、档6 123 ms**（模型 `txy=d/v_model` 中位 134/90/68/29）。差在**三项与档位无关的常量**：Z 过渡 +95、拐角停顿（下一条）、**串口地板 43~45ms/点**（相邻 `send_point` 最小间隔）。⇒ 上一轮"interval=242/162/121/53"的对比表口径错；"档2 与档3 质量同级"其实对应"实际节拍只差 2ms" | 全部计时结论 | `logs/audit_20260921_182040.jsonl` 逐点复算（模型 vs 实测误差 ≤10ms） |
| **⚠ 已证实（代码 bug，本轮新发现）** | **拐角停顿判据语义写反 ⇒ 命中 100% 落笔内点**：`motion.cpp:130-138` `angle_between(a,b,c)` 返回的是 **b 点内角**（直线三点=180°），而调用处判据 `ang>75° → +80ms`（`motion.cpp:281-285`）⇒ 全会话 **1459/1459 = 100%** 命中，而真拐角（转折角>75°）只占 **0.8%**（转折角中位 18°）。**每个落笔点白拿 +80ms**，占实际预算 40%(档1)~75%(档6) ⇒ 既**掩盖**了残（意外兜底）、又是"抖/每点一顿"与时间浪费（~130 点/行 × 80ms ≈ 10s/行）的主因 | 书写节拍、平滑度、总时长 | 全会话三点组统计；修法 `180°-ang > 75°`，**必须与"改 v 或地板"同一次改动**（单独修＝每点少 80ms ⇒ 立刻变残） |
| 已证实（手册） | **无位置源**：`2(1).md` 寄存器表「适用产品 **3 轴步进**」⇒ 无编码器；全文仅 0x0008（坐标读写，读=0x03）、0x0064（7 点写=抓放宏槽，非轨迹缓冲）、0xA001。`V：高8位速度 0~9（00 最快）` ⇒ `speedHigh=lvl-1` 与手册一致 | 闭环可行性、速度标定 | 手册 6.1/6.2/6.3 |
| **已否决（原路线 B）** | **闭环"到位门控"不做**：①无位置源（0x03 读的是坐标寄存器/内部命令位置）；②开销：写+ACK 实测 43ms，一次 0x03 轮询 ≈ 请求 9.2 + 响应 17.2 + 控制器周转 30~40 = **55~70ms** ⇒ ≥1 次轮询即 ≥100ms/点 ≈ 要省的干等；③**治不了真抖源**：`0x0008` 是逐点 move-to，每条指令以减速到零收尾 ⇒ 每点必然一次速度过零（只能靠少发点/RDP 或连续路径模式，手册无此模式）。**仅保留为标定仪器**：加 ~20 行调试（发一条 320mm、运动中连读 0x03）判定寄存器是否为"内部插补流" | 路线选择、Phase 3 | 手册 + 审计实测 + 代码语义 |
| 强推断（待 P0 判定） | **候选零代码修法**：`min_point_interval_ms≈120` + `z_settle_ms=0`。地板只抬"短段"（落笔走行），**不碰抬笔长走行**（其 txy 300~1000ms ≫ 地板）⇒ 预测 档2 150→~184、档3 140→~184、档6 104→~184、**档1 不变**（txy 中位 134>120）= 把档1 已证干净的节拍复制到各档。⚠ 地板 ≥150 时**必须同时把 `z_settle_ms` 归 0**，否则 Z 过渡预算=地板+95 ⇒ 落笔静压越过"顿笔开 ~170ms 炸毛"红线 | Phase 2 定值 | `motion.cpp:125`（`base=max(min,txy)`）+ 审计节拍 |
| 待上机（全零代码） | **P0~P4 判决实验**：P0 = 档6+地板200+z_settle0 写一行（变干净 ⇒ 时间定论、byte 无关）；**P0' = 审计自检**（相邻 `send_point` 间隔应全 ≥ 地板 ⇒ 证明"设置真生效"，前几轮缺这一步）；P1 = 地板阶梯 200→150→120→90 找首次残；P2 = 同地板下 档1/2/6 对比（判 byte 效应、选工作档）；P3 = 同档同地板 字号 40/60/90（判"需求是常数"还是"正比于段长"）；P4 = 现成照片免费 A/B（档6 行内"首 1~2 笔"（冷启动 150ms 保护）vs"第 5 笔后"（~104ms），**同 byte** 天然 A/B） | Phase 2 定稿 | `书写残缺-第3轮交叉讨论-复核方回复.md` |
| 风险（无关但需清理） | `logs/` 残留两个 2026-09-17 控制台会话的超大日志：`Robot_20260917_205835.log` **4.46GB**、`Robot_20260917_213614.log` **506MB**（合计约 4.9GB，无轮转）。删除需用户确认 | 磁盘占用 | `ls -la logs/` |
| **已证实（第 3 轮第二次往返）** | **单独修拐角判据会让大量落笔点砸到 43ms 串口地板**：按整 run 复算（n=164/164/164/181），修后落笔走行中位 = **134/90/68/43ms（档1/2/3/6）**，<150ms 的点占 **54%/66%/80%/95%**（档6 95% 落在地板上）。中位点的角贡献是 ≈58ms 而非 80ms——`k+1<j` 让每笔**最后一个走行点本来就没有**角点停顿。⇒ **拐角修复必须与计时改动同一次提交**，任何"顺手修 bug"都会立刻毁掉现状 | Phase 2 提交顺序 | 审计复算（`_zoom_tmp/postfix_floor.py` 口径；对方独立复现判据与中位） |
| **已证实（第 3 次往返修正口径）** | **计时需求是仿射型：`v_req ∈ [28,42) mm/s`、`C ≈ 80ms`**。①**分桶表归属错了一格**（`gap(a_k,a_{k+1})` 度量的是终点为 p_k 那一段的预算，不是当前对）⇒ 档1 的 `>12mm` 桶正确值是 **710ms**（旧表 194ms 是错口径，对方复现的也是我这个偏差）；修正后档1 = 125/170/218/357/710、档6 = 107/108/125/153/216（<1.5/1.5-3/3-6/6-12/>12mm），模型逐点误差中位 9~11ms。②同 d 下拐角与地板项两档相同可约掉 ⇒ 档1 干净、档2 残 直接夹出 **`v_req ∈ [28,42)`**。③常数需求假设被排除（常数需求下亏空最大在短段，可档1 短段干净）。⇒ diff 形状 = `base = max(串口地板, d/v + C)`，v 初值 28、C 初值 80。**"加 C 项"与第 2 轮否决不矛盾**：当时反对的是把串口地板重复算进 C，此处补的是地板之上的 ~80ms | Phase 2 定值 | `_zoom_tmp/postfix_floor.py` 修正口径复算 + 档1/档2 夹逼 |
| **陷阱（会让 P0 系列白跑或误判）** | ①**地板会抬高 Z 过渡预算**（`max(地板,dxy/v)+z_settle`）⇒ 落笔静压驻留 ≈ 地板−70ms：200→130ms、280/300→210/230ms、**700→630ms 必炸毛**（顿笔开那组 ~200ms 即炸毛，还叠加分层下刀）⇒ 高地板行必须加"起笔处墨团/炸毛"列 + 30 秒单点探针；②**P1 低地板行**（150/120/90）若 `z_settle=0`，Z 预算只剩地板 ⇒ 通道A 复活、被误读成"长段残" ⇒ 逐行 `z_settle=max(0,107−地板)`；③**P0b 地板 700 喂不饱 30mm 段**（需求 ∈(794,1151]ms）⇒ 改用"字号 40 + 地板 700"；④**P2 地板 280 比 byte 仍被污染**（档2 `d>11.8mm` 由 d/v 主导、档6 `d>35.8mm` 才主导）⇒ byte 权威 A/B 移到压平表之后；⑤**改 config 前先关 GUI**（每个设置项都 `cfg_save()` 回写整个文件，`WM_DESTROY` 不写） | P0~P3 实验有效性 | `motion.cpp:111-127` + `gui_service.cpp:1385` + 审计模型复核 |
| **陷阱（写 diff 时必读）** | **"把速度表压平成一个值"会顺手废掉"抬笔走行提速"**：`p.speed` 同时决定**速度字节**（`sendPoint`/`packPoint5` 的 `lvl-1`）与**模型 v**（`estimateMoveMs` 的 `speedLevelToXYmmPerSec`）。若 6 档全压成 28，抬笔点即便给 `p.speed=6` 也只会拿到 28mm/s 的预算 ⇒ 20mm 抬笔走行要等 714ms（而非 156ms）⇒ 提速失效。**改法**：压 档2~5 到 28，**档6 保留 128** 并注释"仅抬笔段（Z_UP、无墨），落笔段禁用"。若要彻底解耦"字节"与"计时"，需另加显式抬笔常量对 | Phase 2 diff | `serial_port.cpp:83-85` + `motion.cpp:94-99` 共用同一 level |
| **注意（新形式下的 Z 预算）** | diff 把 base 改成 `max(串口地板, d/v + C)` 后，**Z 过渡点的预算 = C + z_settle**（dxy=0）⇒ 要保住 Phase 1 验证过的 ~107ms，应取 **`z_settle_ms = 107 − C`（C=80 时 = 27）**，别原样留 95（会变 175ms，静压 ~105ms，虽仍在 ~200ms 炸毛线下但不自洽） | Phase 2 定值 | `motion.cpp:111-127` 的 base+extra 语义 |
| **已复核通过（第 3 轮第 6 次往返）** | **Phase 2 计时 diff 五项全对**：①`base = max(floor, t_xy + g_point_fixed_ms)`（C 在 max 内）；②`z_settle_ms=27` ⇒ Z 预算 = 80+27 = 107ms（Phase 1 安全值）；③`speedLevelToXYmmPerSec` 档1~5→28、档6→128（`SPEED_MAX=6` 已核）；④拐角判据 `turn=π−ang` 与 C 同批；⑤新键 `point_fixed_ms` 全链路 + `geti` 自带 `clamp[0,3000]`。**独立语法检查**（`g++ -fsyntax-only -std=c++17 -Wall`，msys2 toolchain）对 motion/robot_common/gui_service **exit=0 零警告**。**离线重放同一轨迹**：预算 96.1s→101.7s（+5.8%）、拐角 +80 命中 131→**1** 次、落笔中位 193→≈226ms、最小间隔 43→≈100ms | Phase 2 验收 | 读工作区代码 + 自跑语法检查 + 审计重放（`_zoom_tmp`） |
| **陷阱（本轮新增，操作纪律）** | **档6 现在是"落笔禁用"的危险档**：`level>=6 → v=128` ⇒ 若全局 `speed` 被 GUI「±」调到 6（config 已锁 1，但 ± 可改且切页不重置），落笔点全部按 128mm/s 给时间 ⇒ **欠节拍残废直接复活**。验收跑前须确认主页速度档=1 且中途不碰 ±。**长久解**：抬笔提速那次提交里，让 `estimateMoveMs` 按 `cur.isPenDown` 选 v（落笔恒 28 / 抬笔恒快档），把"计时"从 level 里彻底解耦 ⇒ 档位只选字节、坑自动消失 | 上机验收有效性、产品易用性 | 读 `motion.cpp:100-103` + `gui_service.cpp` 的 `set_speed` |
| **注意（顺带影响，非阻断）** | **锚点预定位等待被顺带拉长**：`move_up_to_and_wait` 也用 `speedLevelToXYmmPerSec(up.speed)`（`motion.cpp:399`）⇒ `speed` 2→1 后 `t_xy` 由 d/42 变 d/28（×1.5），同时其 guard 含 `get_Z_SETTLE_MS`（95→27，−68ms）⇒ 每页首次预定位多等几秒（200mm 约 4.8s→7.1s）。**安全性无损失**（长移动 guard 仍 ≥627ms ≫ 设备实际 ~220ms，是多等不是少等）；按 `isPenDown` 解耦后锚点自动走 d/128，每页省 ~5s | 单页/多页总时长 | `motion.cpp:397-410` |
| **✅ 已确认收口（第 7 次往返，真机）** | **Phase 2 计时改动逐点验证通过**（审计 `logs/audit_20260921_215926.jsonl`，我独立 parse）：270/270 点全 档1；落笔走行中位 233ms（预测 226）、p25/p75 185/356；**最小间隔 108ms、<45ms 串口地板点 0 个**；拐角 +80 命中 **131→1**；**落笔走行逐点模型误差 中位 8ms / p90 15ms / 最大 26ms / >30ms 的点 0 个**（n=164）；`retry_count=0`、`ack_valid` 全 true、`task_end=ok`、点数 268（与旧档1 同）。**真实书写耗时 99.7s**（`task_start`→`task_end`；对方一度报"150s"是从 `set_writing_plane` 起算、含 48s 设置/预检） | Phase 2 收口 | 自 parse audit + 逐点模型对表 |
| **已定位（时间浪费，下一轮修）** | **逐字锚点等待过等 ~40 倍**：`move_up_to_and_wait`（`motion.cpp:397-410`）用 `speedLevelToXYmmPerSec(up.speed)` = **落笔**计时常数（档1 ⇒ d/28）去等一个**抬笔**移动（设备实际 byte0≈1185mm/s）⇒ 每字一次、1.5–6.0s，整行 **16.7s/99.4s（17%）**。加上抬笔走行 36 点（中位 859ms、共 ~31s）⇒ **下一轮按 `isPenDown` 取 v=128 后，整行 ~100s→~60s**，且落笔预算逐点不变（墨迹不动） | 比赛 20 分钟预算 | 审计大间隔定位 + `motion.cpp` 读码 |
| **陷阱（C 扫参必读·本轮新增）** | **C 与 Z 预算耦合**：Z 过渡点预算 = `max(12, 0+C) + z_settle = C + z_settle`（`motion.cpp:118-127`）⇒ 只降 C 不动 z_settle 时，C=40 → Z 预算 67ms、C=30 → 57ms，**低于 27mm Z 行程需要的 ~70ms** ⇒ **通道A（起收笔欠压：短横缺/淡、围合小）在低 C 行复活，症状与"截断"几乎不可区分** ⇒ 会把"Z 预算不足"误判成"C 压过头、下限=40"。**规矩：C 扫参时同步 `z_settle_ms = max(0, 107 − C)`**，让 Z 预算每行恒为 107ms（C=80→27 / 65→42 / 50→57 / 40→67 / 30→77） | C 扫参结论正确性 | `motion.cpp:118-127` 的 base+extra 语义 |
| **陷阱（审计判据）** | **低 C 时"小段掉回 43ms 串口地板"是正常现象**，不是故障：C=30、d=0.2mm 时 interval = max(12, 7+30) = 37 ⇒ 实际周期 = max(37, 串口 43) = 43。审计判据应为：①**逐点对表**（观测序列 = `max(12, d/28·1000+C)+拐角`）；②**有效下界 = max(43, C)**。不要写"不出现 43–45ms 点"（会去追不存在的故障） | C 扫参判读 | 代码语义 + 串口地板实测 43ms |
| **已判定（Step A 真机，2026-09-21，审计 `audit_20260921_222812.jsonl`）** | **串珠不在命令点上 ⇒ 不是"每点驻留"产物 ⇒ 扫 C（Step B）与 RDP 减点两轮都取消**。证据：①审计（我独立复算）——90mm 单字「里」的竖笔仅 **5 个命令点/63.0mm**，逐段间距 `[5.1, 6.2, **51.9**, 3.2]`；②照片（`test/test3/_z90.png`，我独立看）——那**51.9mm 单条指令段内部**有 ~12–14 颗珠（命令点 0–1 个）；③该行预算误差中位 14ms ⇒ C=80/z_settle=27 确已生效。**更硬的原理**：我们的计时模型（C/v）**不改变设备运动**（只决定发下一条前的空闲），设备速度剖面由**字节**决定 ⇒ **珠子间距对 C 与 v 都是不变量**，"靠改 v/C 走快点"原理上无效。**论据纠正**：对方 §3 的"不随字号 60→90 放大"没数据（60mm 行珠子数/间距未测出）、1.37mm 处在 2.91px/mm 的分辨率极限（≈4px/周期，只能当定性）、其"空白纸对照"只比了亚 mm 细织纹、**没有排除纸纹方向**（空白纸可见纹路 1–2mm ≈ 珠子间距） | 治串珠路线（已定：跳过 C 扫参与 RDP） | 自 parse audit + 看图 + 尺子标定空白纸纹路 |
| **新线索（双方都漏了）：珠子只长在"竖笔"上** | `_z90/_z60` 两图一致：**日左右两竖 + 贯通中央长竖 = 串珠明显**；**日上下横/内横/土中横/底部最长一横（≈90mm）= 光溜连续** ⇒ **最长的一笔光滑、63mm 的竖却有十几颗珠 ⇒ 是方向效应，不是长度效应**。两个可判决机制：**A 机械/驱动**（delta 雅可比按方向投影、皮带齿啮合 GT2=2mm、微步/电流纹波）；**B 纸/墨**（空白纸纹路=横向 1–2mm ≈ 珠子间距，竖笔"横穿纹路"被吸成周期斑、横笔"顺纹"光溜） | 治串珠的方向（机械 vs 纸墨） | 照片方向性对照 + 空白纸尺子标定 |
| **复核意见（判决实验单，第 11 次往返）** | ①⚠ **换字节也要核截断**：字节会改短段动力学（计时不变但设备剖面变）⇒ ② 的每行必须同时记"珠子 + 5 处截断"，某档珠子最少但出现截断 ⇒ **不合格**（"不能拿完整性换顺滑"这次换的旋钮是字节）；②**档5 是安全的**（代码 `level >= SPEED_MAX(=6) ? 128 : 28`，`motion.cpp:102`/`robot_common.h:84`）⇒ 应测 档1/2/3/4 + 可选档5，对方写的"别用 5、6 档"应改为"别用 6 档"；③② 末尾补一行**档1 复测**做漂移对照（否则 1→2→3→4 与"越写越干"共线）；④① 建议**同日两纸对照**（原方向 + 转 90°，因为 `test/test3/90.jpg` 是上次会话的，墨/纸/压平状态可能已变）；判据只看"珠子在哪个方向的笔画上"，不受深度/墨量变化影响 | 判决实验有效性 | 代码 `motion.cpp:102` + `robot_common.h:84` + 当前 config 核对 |
| **判决表（三格，判读别靠直觉）** | ①珠子**跟纸走** ⇒ **B 纸/墨** ⇒ 动墨稠稀/墨量/深度(往浅、盯变细变淡)/换纸；①**不跟纸** + ②珠子**间距/条数随字节变** ⇒ **A-时间型**（电机/电流纹波）⇒ 直接挑珠子最轻的档当工作档 + 厂商调电流/细分；①**不跟纸** + ②**不变** ⇒ **A-空间型**（皮带齿/丝杆机械周期）⇒ **改字节无效**、走机械侧（核对皮带齿距 GT2≈2mm 与珠子间距）。⚠ ②的判据要盯**间距/条数**（空间图案），**不要只看深浅**（深浅随速度/墨量变，即使机制 B 也会变）| 治串珠方向判定 | 两实验组合互补：① 分 机械/纸墨，② 把机械分成 时间型/空间型 |
| **（历史）待判别（两个零代码判决实验，各 5 分钟）** | ①**把纸转 90° 写同一个「里」**（机器人动作/配置全不变）：珠子跑到**横笔**上 ⇒ 机制 B（纸/墨）⇒ 动墨量/稠稀/纸/深度；珠子仍在**竖笔**上 ⇒ 机制 A（机械）⇒ 走字节扫参+厂商。②**速度字节扫参**（`speed=1/2/3/4`——表压平后档1~5 的 v 都是 28 ⇒ **只换字节、计时完全不变**，天然单变量）：珠子随字节变 ⇒ 机制 A 坐实、且可直接挑珠子最轻的档当工作档（零代码收益）；不变 ⇒ 回到机制 B。**这问同时把"byte 到底有没有用"这个悬案结掉** | 治串珠路线、byte 相关性 | 零代码；`motion.cpp:100-103`（档1~5=28）+ config `speed` |
| **（历史）待判别（决定下一轮做不做 RDP）** | **串珠（珠子）到底挂在"命令点"还是"摩擦/粘滑"**：C 只改**驻留时长**（珠子显著性），**不改珠子间距**；而第 1/2 轮离线测量里点纹自相关间距 **~0.9mm**，**远小于命令点间距（1.4~52mm）** ⇒ 很可能是粘滑。**零代码判别（加一行）**：C=80 不动、**字号 90** 再写一行，量同一笔画（千竖/目右竖）的珠子间距——随字号成比例 ⇒ 挂在命令点（⇒ RDP/减点对路）；不变 ⇒ 粘滑（⇒ **别做 RDP 那轮**，改谈 v/深度/墨） | 治串珠路线（RDP vs v/深度/墨） | 早期点纹自相关测量 + 命令点间距对照；照片可由我量 |
| **陷阱（Step A 珠子归属实验必读）** | ①**字号 90 时 5 字行会互相压**：自由布局格心间距 62mm（x=-154/-92/-30/32/94），90mm 字跨 ±45mm ⇒ 邻字笔画插进待测竖画；且 `clamp_cell`+`grid_fits` 会把摆位钳到视野框内、与基线不可比 ⇒ **改用单字「里」+每页字数=1**（其长竖是第 5 笔、天然在冷启动窗口外），并**同设置补一行 60mm 单字「里」**做同字对比。②量珠子间距要 **mm/px 标尺** ⇒ **两行都存审计**，用该笔画的毫米长度 ÷ 照片像素长度自校准，再与"命令点间距"直接比（比只看"随字号变不变"更硬）。③加一张**空白纸同倍率照片**做基底对照：若空白纸也有同量级 ~mm 周期纹理 ⇒ 珠子部分来自纸/墨纹理，C 与 RDP 都不相干（该动墨量/深度/纸） | Step A 测量有效性、治串珠路线判定 | 代码 `compute_initial_grid`（间距 62mm）+ `clamp_cell`/`grid_fits` 语义；墨迹测量方法学 |
| **代码细节（免得改错键）** | **书写路径的权威字号键是 `free_char_size`**（`gui_service.cpp:918 plan.used_S = g_free_char_size`，1139 行传给 `generateSingleCharTrajectory`）；`layout_char_size` 是旧网格布局键、当前路径不用 ⇒ 只改后者字号不会变 | 字号相关实验/操作单 | `gui_service.cpp:918,1139` |
| **用户前提锁定（2026-09-21）** | 用户拍板：只治**速度1 书写时的抖/串珠**、视为机器侧前提**不再验证**、**只用速度1一档**、不再上机做判决实验，改法走"改代码 → 写一行看效果 → 不行换切入点"。⇒ 判决实验①（转纸）与②（字节扫参）**作为正式实验取消**；其问题只能靠"改一行看效果"的方式顺带回答（字节试验 = 改 GUI 档位写一行，见下） | 后续路线 | 用户决策 |
| **预期管理（本轮必读）** | **这一轮（RDP+C）治的是"顿挫（停-走）"，治不到"串珠"**：Step A 已定珠子在**单条指令内部**（51.9mm 段内 12–14 珠、命令点 0–1 个）⇒ RDP 与 C 都碰不到它；串珠的软件侧唯一抓手是**字节**，物理侧是墨/纸/深度（用户已叫停那两项实验）。**若主诉是串珠，本轮大概率看不出改善** ⇒ 必须提前告知用户，别把"没变化"读成"方案失败" | 治抖验收判读 | Step A 结论 + 用户前提 |
| **改法修正（治抖方案复核）** | ①**RDP 提成 `rdp_tol_mm`（默认 0.35）✓ 但预期要下调**：真数据实测删点率 **0.06→0%、0.35→~10–12%、0.5→−17%、0.8→−30%** ⇒ 0.35 只减约一成"停的次数"，**真正的减停在 0.5–0.8**（对 60mm 字/3–5mm 笔宽，0.5–0.8mm 偏差仍不可见；0.8 有 2/33 笔成单段）；既然成键，免费扫。②**降 C 必须同改 `z_settle_ms = 107 − C`**（C=55 ⇒ z_settle=52）：只降 C 会让 Z 过渡预算掉到 82ms（≈27mm Z 行程需求 70ms+余量）⇒ 通道A 复活、被误判成"C 压过头"（这是 C 扫参单 v1 修过的坑，别回退）。③**字节解耦改成"按 `isPenDown` 选 v"**（落笔恒 28 / 抬笔恒 128，不再查 level）⇒ 落笔计时逐点不变（完整性零风险）+ 抬笔/锚点掉到 d/128（**整行 ~100s→~60s = #10 的时间收益，且不必改任何 `p.speed`**）+ 顺带消灭"档6 危险档"；**ink 字节继续由 GUI 的 `speed` 档决定** ⇒ "试 byte1"变成**纯 GUI 零代码操作**（= 被取消的判决实验②，用"改一行看效果"完成）。⚠ 换字节要同时核 5 处截断（字节改设备短段整定）。④小旋钮：`cold_start_min_ms` 150→0（C 已给每点兜底 ≥80ms，冷启动 150 只对每字前 ~6 点生效、属额外干等，可省 ~0.2s/字） | 下一次重编的 diff 内容 | 用户前提 + 真数据实测 + `motion.cpp:118-127` |
| **陷阱（操作纪律·2026-09-23 第 9 轮新发现，已实际造成一次误判）** | **dryrun/OFFLINE 与真机在"外观"上不可区分 ⇒ 会骗过执行者和复核者**：本轮用户按上机单跑的"② 三条 53.1mm 单指令竖线（档 1/3/5）"在审计里是 `mode=OFFLINE`（控制台 dryrun，`source=CONSOLE`，`audit_20260923_185822/185954.jsonl`）——**不发帧、不动臂、不落墨**，但控制台照常打印、照常写审计；对方 agent 据此得出"单线画满 ⇒ 排除臂程"的结论（不成立）。**规矩**：①凡"纸面证据"必须同时给**审计里的 `mode`**；②执行前确认界面/日志显示 REAL（GUI 标题或控制台的 `[错误] 串口打开失败` 提示）；③dryrun 只用来验证流程/坐标，**不能当物理结论** | 一切"上机实测"结论的有效性 | `audit_20260923_185822.jsonl` / `_185954.jsonl`（`mode=OFFLINE`）+ 真机同坐标无记录（全 85 个审计扫描） |
| **未验证环（第 9 轮点名，全项目仅剩这两个）** | 全链 12 环里，**只有两环从未被正面测量**：**第 8 环** `estimateMoveMs` 的落笔常数 `PEN_DOWN_MM_PER_SEC=28`（`motion.cpp:98`，反推值，非实测）——垂竖那笔实测等效需求 **25.2mm/s**、最紧单段 **27.3mm/s**，照片缺口 6.5mm ⇒ 反推设备等效 **≈23mm/s** ⇒ 常数偏乐观、长笔尾巴被后一条命令截掉；**第 11 环** 控制器对连续 `0x0008` 是**队列**还是**覆盖**（ACK 只证明寄存器已写，见本表第 3 行）——**决定"写短"是否可能是截断**（队列语义下只可能丢步/接触）。⇒ 判决实验：**B** 调试8 真机单指令线 96mm×档1/3/5（v = 实画长度 ÷ 审计实测间隔）；**C** 单字「垂」×`point_fixed_curve_ms` 25/150/400（时间够则变长 ⇒ 坐实时间不足） | 长笔"写短"（垂竖/钓竖/碧石口）的最终归因与修法 | `motion.cpp:98,117-138`；③0ms 审计逐点预算；③0ms 照片缺口 ≈6.5mm；`serial_port.cpp:109-141`（ACK 语义） |
| **陷阱（操作单必读·2026-09-23 第 11 轮新发现）** | **"改 `robot_config.json` 的 `page_text` 就会写那个字"是错的**：GUI 文本框的预填来源是 **`last_task.txt`**（`gui_win32.cpp:1242` → `gs::load_last_task_text()` = `gui_service.cpp:1711` 读该文件），开始书写用的是**文本框内容**（`gui_win32.cpp:1675` `gs::start_write(to_u8(t))`）。`page_text` 只在 `cfg_save` 记录（`gui_service.cpp:1472`）与**空页兜底**（`:1620-1623`）时用到。⇒ 只改 JSON 会把**上一次的文本**（当时 `last_task.txt` = `溪来`）重写一遍。**规矩**：要换写的字，必须**在 GUI 文本框里输入**（`每页字数`/`总页数` 两个框才吃 config：`snapI("cfg","page_chars",5)` `gui_win32.cpp:1266/1271`） | 一切"换字跑"实验（如 C 单字「垂」）的有效性 | `gui_win32.cpp:1242,1266,1271,1675` + `gui_service.cpp:1472,1620-1623,1705-1714` |
| **注意（B 的 Δ 现实值）** | 人手在控制台打一行 `X Y Z 落笔`（如 `26 -80 -350 1`）**实际要 1.5~3s**，做不到 0.5~1s ⇒ 上机单里"尽快（0.5~1s）"要改成"越快越好、**≤2s 都合格**"，否则执行者会以为白跑而重跑。判决不受影响：v≈23 时 Δ=2s ⇒ 46mm、Δ=5s ⇒ 115mm、队列 ⇒ 恒满长，一眼可分 | B 实验的执行与判读 | 控制台交互实测常识 + 审计时间戳口径 |
