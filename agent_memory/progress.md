# 任务进度

> 本文档维护当前任务进度，供 agent 在开始非简单任务前读取。
> 只保留当前有效信息，不写成无限追加日志。
> 出现阶段完成、方向变化、失败尝试、准备结束会话时，必须更新本文件。

## 当前任务
**（已实现·编译+启动+正常关闭全通过·待真机闭环验收）蓝牙翻页接入（2026-09-20/21）**：把 `Run()` 里的预留锚点换成真信号实现 —— 每页写完发 `RUN<档位>,<时长>` 给 STM32+TB6612+直流电机，**等板子回 `DONE`** 才算翻页成功（闭环，不是定时器猜）。用户四项决策：①翻页机构就用「STM32+TB6612+直流电机」那套；②完成判定必须闭合环（要回位信号），板子回「转完了」即可，不加位置传感器；③链路方法不限但**要集成进 GUI**；④翻页参数（档位/时长）做成 **GUI 上可调**。
- 新增 `ble_motor.{h,cpp}`（唯一新文件，未改 hanzi/motion/serial_port/gui_trail 的内核逻辑）：**纯 WinRT** 实现 BLE GATT 透传。单工作线程独占 WinRT（进 MTA），对外只暴露 `exec(line, want, timeout, canceled, got, err)` 阻塞式闭环调用 + 状态查询/连接控制。链路：`BluetoothLEDevice.FromBluetoothAddressAsync` → `GetGattService(FFE0)` → `IGattDeviceService3`（RequestAccessAsync + **OpenAsync(SharedReadAndWrite)**）→ `GetCharacteristicsWithCacheModeAsync(**Uncached**)` → FFE1 → `add_ValueChanged` + `WriteClientCharacteristicConfigurationDescriptorWithResultAsync(Notify)`；写用 `IGattCharacteristic3::WriteValueWithResultAsync`。
- `gui_service.{h,cpp}`：`page_turn_install()` 注册回调（`Run()` 里 `cfg_load()` 之后调用，替换原锚点注释）；回调内「确保链路（未连接则自动建链，最多等 40s）→ 发 RUN → 等 DONE」，**回调不持 g_mu**（调用点本就无锁），所以长阻塞不会卡 UI；取消语义沿用 `g_task_cancel||g_estop`，命中立即放弃。新增 4 个持久化配置键 `page_turn_ble/page_turn_gear(0~20,默认15)/page_turn_run_ms(100~600000,默认3000)/ble_addr(默认21F6473AD889)` + setter/getter + snapshot 暴露 + 审计事件 `page_turn_ble`（含 cmd/gear/run_ms/result/elapsed_ms/detail）。`page_turn_shutdown()` 退出时断开 GATT + 停工作线程。
- `gui_win32.cpp`：翻页条下新增第二行「[✓]蓝牙翻页　档位(0~20)：[ ]　时长(ms)：[ ]　[连接蓝牙]　状态文本」。档位/时长沿用**两段式整数框**（EN_CHANGE 合法即套用不回写、KILLFOCUS 夹取回写，同字号/每页字数教训）；`WritePlotGeo` 新增 `strip2` 几何并把下方四角标定区整行下移（避免盖住）；新增复选框必须紧邻 `IDC_CHECK_*` 块（按 `id-IDC_CHECK_DRY` 索引）；`IDC_BTN_BLECONN` 加入 `DrawBtn` 的 TEAL 组；`WM_DESTROY` 里调 `gs::page_turn_shutdown()`。
- `build.bat`：`COMMON` 加 `ble_motor.cpp`；两个目标都加 `-lruntimeobject -lwindowsapp -lole32 -luuid`（WinRT 需要；控制台目标之前没有任何 -l，同样要加）。
- 验证：两个目标 g++ 链接 **RC=0**（只剩改动前就有的 `g_auto_draw_used`/`shanshui_gen.hpp` 警告）；`RobotGUI.exe` 启动冒烟「已加载配置 robot_config.json」且 8s 存活；用 `CloseMainWindow()` 发 WM_CLOSE 走**正常关闭路径**，进程 4s 内退出、**无 terminate/崩溃**。另有独立验收工具 `ble_selftest.exe`（在 workspace `tmp/ble_poc/`，链工程内 `ble_motor.cpp`），可在不开 GUI 的情况下单独跑「建链→发 RUN→收 DONE」。
- **✓ 真机闭环验收已通过（2026-09-21 20:35）**：模块上电后 `scan.py` 15s 扫描到 `21:F6:47:3A:D8:89  rssi=-65  HC-05`，随即 `ble_selftest.exe` 一次跑通：**建链 2.30 s → 发 `RUN30,3000` → 3.33 s 后收到 `DONE 30 3000`，EXIT=0**（电机实际转了 3 秒）。**「模块不可达 = 没上电」的结论被验证实**，软件侧零改动即通。顺带印证：模块在广播时建链只要 **2.3 s**（昨天不可达时是 25 s 后失败），所以「**连不上先扫描**」这条方法论成立。剩余待办只有 GUI 整任务验收与手感标定。

**（「笔画质量」第 12 轮·用户改裁决"不测 B、是软件问题"·对方提案统一假设·我方判分支并升级 C）**：用户新裁决＝**剩余缺陷是软件问题、不测 B（控机测量）、没修好就是没找对地方**；**双方收敛前不改码**。对方曾擅自动码、**已全部回退**（我已复核：`motion.cpp:98-99`=28/128 ✓、无残留旋钮 ✓、双 exe 09-24 20:04/20:09 重编 ✓、③ 审计行为基线 = `max(12,d/28·1000+Cfix)+extra` ✓）。对方提案"统一假设"＝`28/128` 偏乐观 + 控制器抢占 ⇒ **写短**（落笔长段被截）+ **拖连**（抬笔/字间没走完就下扎 ⇒ 粘连）。我方第 12 轮回复稿：`C:/Users/ceiii/.qoderwork/workspace/mucqjq4ir3q11afi/outputs/笔画质量-第12轮-复核方回复-本项目agent.md`。**我方三处判定**：①**去掉"统一"、粘连分两族**——**R5 只改下降点驻留、未动任何移动段预算，却把「闲点↔横折」治成分开**（真机+dryrun+双审计）⇒ "点/短笔涨大"族＝**驻留墨团**（抓手：驻留/平面/字号/笔尖），拖连族只覆盖**跨笔长移动**（抓手：PEN_UP/预定位）；②**⑤ 阴性不判别机制**（墨团与拖连都能解释）⇒ 不能当"拖连"证据，判别只需 A（尺量 w）+⑤D（同尺度并排）；③**C 升级为"机制+v"双重判据**——按截尾模型 `实到=min(d,v·B)`，垂竖 42.35mm 段预算 1553→1678→1928ms ⇒ **预测缺口 ≈6.9/3.8/0 mm**（基线 6.9 与照片 ≈6.5 自洽）⇒ **三分支**：≈6.9/3.8/0 ⇒ 截尾成立 **v≈23** ⇒ 下调 `PEN_DOWN`；**三档一样 ⇒ 不是预算**（转接触/机械/字库，写死不再动计时常数）；**6.9/0/0 ⇒ "每点固定损耗"型** ⇒ 抓手换成**减点数**（`rdp_tol_curve_mm` 0.1→0.3~0.5）而非降常数。判据用"末端还差几毫米"（±1.5mm），**别用"看着连上没"**（墨团变胖可伪造 1~2mm 连接）；三档同字号（建议 `free_char_size` 回 70；留 90 则判据换 ≈1968/2093/2343ms）。**我方新增零代码一格：GUI 速度档 3→1**（依据：**档1/字节 0 是历史唯一在"落笔 v=28"下被判干净的档**〔`v_req∈[28,42)` 夹逼＝档1 干净/档2 残〕，当前档 3 从未验证，且 `estimateMoveMs` 不看字节）⇒ 竖变长/画满＝档 3 设备速度 <28 ⇒ 用档 1 **或**下调 PEN_DOWN（若档 1 带回串珠就只能后者）；不变＝与字节无关。**对四问**：①候选接受但去"统一"（并更正"我从不主张设备快于模型"，第 9 轮的 23 也是倒算）②C 认并升级用法 ③**PEN_DOWN 可调同意但延后**：放 C 之后、独立单变量验证；**per-mm 才是"长度"旋钮**，C 的 per-point 只当诊断（给每点都加驻留、短段拖慢、涨团风险↑；28→22 时 42mm 段仅 +≈80ms）④**抬笔归时间不归静态净空**（28mm 静空已排除，剩"Z 上升没在下条 XY 前走完"），抓手 `PEN_UP`/Z 预算（**不是**抬 Z_UP，−325 距上限 −320 仅 ~5mm）；判据＝湿照拖线形态 + 可证伪预测"**新笔起点沿'上笔末点→本笔起点'漂移**"（可能同源解释石不封口/来撇贴横）。**下一步**：一次上机零代码三件（C 三档 + 速度档 A/B + A/⑤D）⇒ 按判决落**单个**改动（`PEN_DOWN` 提为 config 键初值 22 / 或换档 1 / 或减点数，**三选一不叠加**）⇒ 粘连族另立一步分开验证。**"不测 B"我接受**，只记风险：v 只能由 C 缺口反算（±2~3mm/s，够用），最终取值"试出来"、每轮迭代一次真机跑。详见 context.md「笔画质量·第 1 轮」节第 20 条。
**（「笔画质量」第 11 轮·讨论已冻结·执行单已勘误）**：对方全部接受第 10 轮的分工更正（C 不能单独判第 11 环；**B 判第 8+11 环、C 坐实机制、B 先于 C**），并出《上机执行单-v2-ABC.md》，声明"讨论冻结、移交用户上机"。我方第 11 轮回复稿：`C:/Users/ceiii/.qoderwork/workspace/mucqjq4ir3q11afi/outputs/笔画质量-第11轮-复核方回复-本项目agent.md`。**本轮唯一实质产出 = 执行单的三处勘误（不改就会白跑）**：①🔴**C 的"改 `page_text` 即写「垂」"不成立**——GUI 文本框预填来自 **`last_task.txt`**（`gui_win32.cpp:1242`→`load_last_task_text()` `gui_service.cpp:1711`），书写用**文本框内容**（`gui_win32.cpp:1675`），`page_text` 只是 `cfg_save` 记录 + **空页兜底**（`:1472`/`:1620-1623`）；当时 `last_task.txt`=**`溪来`** ⇒ 只改 JSON 会把「溪来」重写三遍 ⇒ **正确写法：关 GUI → 改 JSON（`point_fixed_curve_ms`+`page_chars/page_count`）→ 开 GUI（项目根）→ 文本框输入「垂」→ 每页字数=1/总页数=1 → 书写**；顺带**字号设回 70**（当前 `free_char_size=90` 是 ⑤ 留下的；70 可与 ③0ms 缺口 ≈6.5mm 对比、判据 42.35mm 段 **1553→1680→1930ms**；若留 90 则判据改 **≈1968→2093→2343ms**，三档必须同字号）。②**B 的"尽快"现实是 1.5~3s**（人手打字极限）⇒ 执行单改写"越快越好、**≤2s 都合格**"，别因手速重跑（Δ 由审计精确记录；v≈23 时 Δ=2s⇒~46mm、5s⇒~115mm、队列⇒恒满，一眼可分）。③**补判读方向**：手册「00 最快」+`speedHigh=档−1` ⇒ **档1=字节0=最快、档5 更慢** ⇒ 若字节是速度旋钮，**线5(档1) 应比线2(档3) 长、线6(档5) 短**（同 Δ），三条一样长＝字节与速度无关；**结论要落回档 3**（当前 `"speed":3`）。另两处确认：红线文案与 `main.cpp:611` 一致但 **GUI 侧无此提示**（只表现为"不动"）⇒ REAL 判据一律以**审计 `mode`** 为准；A 缝表与 ⑤D 做法复核一致。**下一步 = 用户一次上机（B→C→A→⑤D）**，交付物 = B 审计(REAL)+六线照 / C 三档审计+三张湿照 / A 的 w 三个读数 / ⑤D 并排图；拿回后我算 v、判队列/抢占、给 `PEN_DOWN` 建议值与 G 组结论。详见 context.md「笔画质量·第 1 轮」节第 19 条。
**（「笔画质量」第 10 轮·对方认账并锁定 A/B/C·我方点名一处异议）**：对方认账三处（② 是 OFFLINE ⇒ "排除臂程"撤回；悖论真因是"**时间预算**"；T 组旋钮是 `PEN_DOWN` **不是** `PEN_UP`；G 组弃"尺度不变"改测墨迹宽 w），并主张 **"C 是唯一同时判第 8/11 环的决定性实验、应排在 B 之前"**、要我点名异议。我方第 10 轮回复稿：`C:/Users/ceiii/.qoderwork/workspace/mucqjq4ir3q11afi/outputs/笔画质量-第10轮-复核方回复-本项目agent.md`。**我方唯一实质分歧**：**C 不能单独判第 11 环**——C 的阴性只证明"预算不是这 6.5mm 缺口的原因"，缺口仍可能是**接触/墨（第 12 环）或丢步（机械）**；"队列"只是其中一支 ⇒ **第 11 环（队列 vs 抢占）的真正判据 = B 的"短间隔"格**：160mm 单线、第二点**尽快**打 ⇒ 实画明显短（≈v·Δ）＝**抢占**（截尾可能、第 8 环有解）；实画**满长**（Δ 再短也满）＝**队列**（截尾不可能，"写短"只能查接触/丢步）。**分工改定：B = 第 8 环真值 v ＋ 第 11 环队列/抢占；C = 第 8 环"时间就是瓶颈"的机制坐实；B 先于 C**（v 是 C 的解释前提）。**B 协议重排（只变间隔）**：线长 **160mm**（Y +80→−80）、档 3 固定、**Δ 阶梯 4 档（尽快/~1s/~2s/~5s，X=26）**＋**档 1 与档 5 各一条（Δ≈1s，X=46/66）**；Δ 用**审计"第3行→第4行"实测时间差**；只量长度、不判深浅。**C 三条防误读**：①审计先核预算真涨（1553→1680(150)→1930(400)ms）②判结构（越过倒数第二横/接到最后一横上方 3.7mm）**别量墨外沿**③单字「垂」+每页 1 字+先关 GUI+三张湿照。**A 与 ⑤D 并入同一次上机**：A 趁 C 还湿尺量墨迹宽 w（3 处中位）对缝表判"物理必粘否"；⑤D 把 ③0ms 与 ⑤0ms **缩放到同一字号**并排只看溪/来。**顺序：B（~5min）→ C（3 轮）→ A（趁湿尺量）→ ⑤D**；交付物 = B/C 审计（确认 `mode=REAL`）+ B 六线照片 + C 三张湿照 + w 三个读数。详见 context.md「笔画质量·第 1 轮」节第 18 条。
**（「笔画质量」第 9 轮·全链重开·已交稿）**：用户要求"从确定坐标到落笔逐环理清"。我方第 9 轮回复稿：`C:/Users/ceiii/.qoderwork/workspace/mucqjq4ir3q11afi/outputs/笔画质量-第9轮-复核方回复-本项目agent.md`。**四件净产出**：①**查出 `② 的"真机三条 53.1mm 竖线全部画满"不存在`**（只出现在 `audit_20260923_185822/185954.jsonl`，**`mode=OFFLINE`/dryrun**、source=CONSOLE；真机同段跑的是另一组 9 点 60~134mm 全档3）⇒ "② 排除臂程"不成立，且 ②/④ 的"满/准"都在**人手 4.1~21.4s 预算**下（④ = GUI `preview_corner` 3×3 点阵 + `preview_writing_plane(-320)` 回中心），而任务里长笔预算只有 1.3~1.8s ⇒ 混淆变量是"**时间**"；②**全链 12 环表**（唯二从未正面测量的环 = 第 8 环 v=28 常数、第 11 环 0x0008 队列/覆盖语义）；③**垂竖短的毫米级算术**（42.35mm 段仅 1553ms ⇒ 需 27.3mm/s、单段余量 2.6%；整笔等效 25.2mm/s；照片缺口 ≈6.5mm ⇒ 设备等效 ≈23mm/s）；④**G 组改判据**（字库最小缝表 + "湿时尺量 w vs 缝"），并复核 `⑤ 字号 90/0ms.jpg` 溪/来**是分得开的**（与对方"纹丝不动"不符）。详见 context.md「笔画质量·第 1 轮」节第 17 条。
**（【已被第 9 轮重开取代】「笔画质量」第 8 轮·当时判"讨论已结束·移交用户执行"）**：双方对"机制分类 + 旋钮选型 + 上机判据"一致，无异议收口。我方第 8 轮回复稿：`C:/Users/ceiii/.qoderwork/workspace/mucqjq4ir3q11afi/outputs/笔画质量-第8轮-复核方回复-本项目agent.md`。**已结案**：D1（来首笔）、R5（闲点）真机+dryrun+双方独立复算三重确认；`z_settle ≥ 230` 安全线已进注释；y 轴/镜像、calib.json、调试5、D3 定位。**待用户上机四步（②→③→④→⑤）**：② 调试8 单指令长线（**坐标已算好：(26.0,36.0)→(27.5,−17.1)、53.1mm、Z=−350、档1/3/5 各一条、X 各错开 20mm**）→ ③ `stroke_begin_ms` 0/30/60（**三向湿照判据**：石左下封口/闲点不连横折/溪三点不连撇）→ ④ R3 点阵静差 → ⑤ 字号 90（只判溪/来）。**三条执行前置（漏一条白跑）**：①③ 要手改 JSON ⇒ **先关 GUI**（`stroke_begin_ms` 无 GUI 控件、28 处 `cfg_save()` 会覆盖手改）+ 从项目根启动；②② 用控制台 ⇒ 串口独占 ⇒ 也先关 GUI，并确认**没退到 DRYRUN**（否则布上无墨）；③② 三条线错开画、干布面、一张照片拍全。②若三档线长都短 ⇒ **B1 坐实、B 组无软件解**（转厂商/机械）。详见 context.md「笔画质量·第 1 轮」节第 16 条。
**（「笔画质量」第 7 轮·计划已定稿·待用户执行一次上机）**：**用户裁决＝"350 平面是当前最佳写字面，不影响书写，不考虑影响"** ⇒ 平面定 −350、不作变量 ⇒ **删掉"−353 对照跑"与"平面阶梯"两格**。我方第 7 轮回复稿：`C:/Users/ceiii/.qoderwork/workspace/mucqjq4ir3q11afi/outputs/笔画质量-第7轮-复核方回复-本项目agent.md`。**本轮净变化**：①核销一条——`audit_20260923_000529.jsonl` 是 **DRYRUN**（对方的 dryrun，不是丢照片的真机跑），我上轮"缺一格"的说法作废；②**② 改为"纯字节阶梯"**（调试8 一条 65mm 单指令竖线、Z=−350、档 1/3/5）⇒ 判"臂到没到（线长）"+"字节的墨量效应"；③**新增 ③ `stroke_begin_ms` 阶梯 0→30→60**（零代码，旋钮位置＝`motion.cpp:126` 的"笔画起点驻留"，正是 R5 去掉的 80ms 所在）——**这是"封口 vs 粘连"共用的同一个墨团旋钮**，判据**三向**：石左下角封住 / 闲点不连横折 / 溪三点不连撇。**算账**：石左下角缝 5.4mm⇒墨团半宽需 ≥2.7mm；闲点↔横折 9.0mm⇒需 <4.5mm ⇒ 双赢区间 **[2.7,4.5)mm**，而 R5 把半宽从 ~3.0 推到 ~2.5 ⇒ 掉出下界 ⇒ **定量解释"闲点解决但石不封口"**。**执行顺序**：②→③→④(R3 点阵)→⑤(字号 90，只判溪/来)；②若三档线长都 <65mm ⇒ **B1 坐实、B 组无软件解**（转厂商/机械）。详见 context.md「笔画质量·第 1 轮」节第 15 条。
**（「笔画质量」第 6 轮·D1+R5 已真机落地·复核方已交稿）**：D1（每字预定位）+R5（去下降点伪 80ms）已改码并真机跑过——**闲点问题解决 ✓、来首笔未再提（推定修好）✓、其余"没有改善"**。我方第 6 轮回复稿：`C:/Users/ceiii/.qoderwork/workspace/mucqjq4ir3q11afi/outputs/笔画质量-第6轮-复核方回复-本项目agent.md`。**我独立 parse 审计验证 D1/R5 都生效**（锚点等待 1488~3087ms、下降预算 342/281ms，伪 80 消失）。**但我查出三件要紧事**：①**本轮不是单变量——书写平面被改成 −350（基线 −353）**（用户在 17:31/17:32 现场改的）⇒ "其余未改善"不能归给 D1/R5，且浅 3mm 对"粘连"有利、对"写短/不封口"有害；**09-23 00:05 那次 D1+R5+平面 −353 的运行没有照片** ⇒ 须先补"同代码+基线平面"这一格；②**归类改按"墨多/墨少"**——**石没封口改判为"墨少"**（字库口左下角缝 5.4mm 只能靠墨团填，R5 缩墨团可能正是真因；**字号 90 会让缝变 6.9mm ⇒ 更差**，故不能用它判字号 90 成败），溪(4.5~7.2mm)/来(2.3/6.8mm)才是"墨多粘连"（字号↑有用），垂竖缺 9.2mm、钓竖提端点 19.4mm 内无其它笔画 = "画不满"；③**B2（stroke_end_ms）否掉**——末端驻留 275~335ms 是全笔最富的，B 只剩 B1(臂没到)/B3(沿途接触失败)，而平面变浅使 B3 更可疑。**下一步四件套**：①同代码+−353 对照跑 →②**单指令长线"平面阶梯×字节阶梯"**（控制台调试8 两点画一条 65mm 竖线，扫 Z=−348/−350/−353/−356 与速度档 1/3/5；禁用调试5）→③R3 点阵 →④字号 90（只判溪/来）。详见 context.md「笔画质量·第 1 轮」节第 14 条。
**（「笔画质量」讨论已收敛·第 5 轮交稿·本轮未改任何项目源码）**：对方收口稿全盘接受我第 4 轮修正，并重排优先级＝**主力 D1（来首笔）+ R5（去伪 80ms 缩墨团）+ M0'/R3（测量）**；**R1（PEN_UP 减小）降为条件项**（只在视频证实 X<128 且湿测见长移动拖线时才上）。我方第 5 轮回复稿：`C:/Users/ceiii/.qoderwork/workspace/mucqjq4ir3q11afi/outputs/笔画质量-第5轮-复核方回复-本项目agent.md`。**我落了三处补丁**：①**门点残差挂账**——"角度很平"没有任何候选机制解释得了，湿时判"两个墨痕还是一个"（若是"点没上墨、看到的是横折起点墨团" ⇒ 与"来首笔"同族）；②**更正我上一份的算错**：R5 去掉伪 80ms 后余量**只有 20ms**（335 vs 已证够用的 315），**`z_settle` 下限从 150 抬到 230**，R5 后若见截断先把 z_settle 提回 250~300 别动 C；③**新增正面杠杆：字号 70→90**（线宽 2~3mm vs 驻留墨团 5~6mm ⇒ 粘连元凶是墨团；90mm 时溪三点缝 5.8~9.3mm > 墨团 4~5mm ⇒ 数量上解开；代价：每页 4→3 字、页数 +33%、用时 ×1.29）。**下一步＝用户执行 M0'（慢动作视频）+ R3（湿测点阵 + §3 三类位置 + 门点两痕/一痕）那一次上机**，拿回数据后再定 R1 值/是否走"改浅平面"分支。详见 context.md「笔画质量·第 1 轮」节第 13 条。
**（「笔画质量」第 4 轮·复核方已交稿·本轮未改任何项目源码）**：对方已独立复核并收回"接入 calib.json""方框用调试5"两条；我 §4.1（静态标定不可能造成字内错位）/§4.2（伪 80ms）被接受；双方对 §4.3（笔间拖墨）进入"有条件接受"。我方第 4 轮回复稿：`C:/Users/ceiii/.qoderwork/workspace/mucqjq4ir3q11afi/outputs/笔画质量-第4轮-复核方回复-本项目agent.md`。**本轮我改了四处**：①**M0 换仪器**——秒表法已失败一次（byte1–5 全落 0.4~0.6s）⇒ 改用**手机慢动作视频 240fps（一次同时量 X / Z 升降 / 抬笔是否真离纸）**+ 可选 ~20 行 0x03 运动中轮询；②**净空假设两处修正**——静态不可行（28mm 净空，布/笔尖不可能吃掉）、**"抬高 Z_UP"只剩 ~5mm 余量**（z_limits=[-320,-410]，-310 以上不动）⇒ 它只剩"Z 上升未完成"一支，抓手＝加 Z 预算/改浅平面；③**用"移动长度"当判别变量**并算出全部笔间起点移动距离（>25mm 31 处 / 12~25mm 15 处 / <12mm 7 处）⇒ 湿时只看三类代表位置（长移动 闲3笔69.3/垂5笔56.3/上3笔65.9；**短移动对照 溪5/6/7笔 6.7/11.0/16.7 = 用户报的"写到撇上"那两点**）；④**溪那一条判"与两者无关"**——字库距撇仅 **4.5~7.2mm** + 驻留**墨团宽 5~6mm** 就够连上 ⇒ 时序改动治不了，且它是"抢占"假设的反例风险。另提出**免费红利 R5**：去掉伪 80ms ⇒ 起笔墨团缩 ~20% 且结构零风险（z_settle 150 已证够，现 415−80=335>315）。**收敛计划**：M0'（视频）→ R3 湿测 → 定 R1 值（`PEN_UP=X·0.8`）→ R1+R2(D1) 同批 → 湿判 → R5 → R4(D2 0.8)；若视频显示抬笔没离纸 ⇒ 先改浅平面。详见 context.md「笔画质量·第 1 轮」节第 12 条。
**（「笔画质量」第 3 轮·复核方已交稿·本轮未改任何项目源码）**：用户新输入＝**时间够**（"字数少"）· 位置类**"机器走偏，查执行"** · 湿时观察（**来首笔"有动作但笔提前提起来了"**／**门点"平+靠右+与横折连一块"**／**溪右两点"写到上面的撇上"**）。我方第 3 轮回复稿：`C:/Users/ceiii/.qoderwork/workspace/mucqjq4ir3q11afi/outputs/笔画质量-第3轮-复核方回复-本项目agent.md`。**本轮两条新代码事实**：①**每个笔画下降点白拿 80ms 伪拐角停顿**（prev==cur 退化角 ⇒ `angle_between` return 0 ⇒ `turn=180°>75°`）⇒ 下降预算 = `Cfix+z_settle+80`、抬笔只有 `Cfix+z_settle`，**解开上轮"+86ms 未建模"**，也说明"Z 预算=Cfix+z_settle"口径不准；②**笔间移动预算余量仅 ~55ms**（按我们自己的 `PEN_UP=128` 给，而设备真实抬笔速度反推 ≲130mm/s）⇒ v_dev≲110 时**为负** ⇒ **笔在飞行中下扎、墨从上一笔拖到下一笔起点**，正是"点连横折""点写到撇上"的机制候选 ⇒ 建议 **R1 `PEN_UP_MM_PER_SEC 128→64`**（可一键回退）。另：**调试5 走 `0x0064` 抓放宏，不能做几何测试**（调试2/GUI 预览才是安全路径）；**`calib.json` 里没有任何标定数据 ⇒ 对方"接入 calib.json"撤销**；**字内相对错位在构造上排除静态标定**（静态误差是位置平滑函数）；给出字库客观几何（门点距横折 **9.0mm**、溪点距撇 **4.5~7.2mm**、来短撇居中）。建议顺序：**R1 → R2(D1 每字预定位) → R3(湿时点阵静差，GUI 预览任意坐标) → R4(D2 直组 0.8)**。详见 context.md「笔画质量·第 1 轮」节第 11 条。
**（「笔画质量」第 2 轮·复核方已交稿·本轮未改任何项目源码）**：介质澄清＝**水写布**（照片上的"淡/划痕"是风干假象，干照对墨量无解释力；但**来首笔真丢**、结构+位置类"真实存在"）。我方第 2 轮回复稿：`C:/Users/ceiii/.qoderwork/workspace/mucqjq4ir3q11afi/outputs/笔画质量-第2轮-复核方回复-本项目agent.md`。**本轮两条介质无关新事实**：①**字库对账**——复刻 `hanzi.cpp` 变换与审计 `target_pose` 逐笔比，**7 字 60 笔 Δstart=Δend=0.000mm**（offset 离散度 0.000；旁证：`rotate_local` orient=0 恒等、去重要求 pen 状态相同故不吃落笔点）⇒ 软件链无整笔位移/缩短 ⇒ **"位置不对"不在软件侧、"来首笔缺失"只剩执行侧 ⇒ D1（每字预定位）唯一化**；②**RDP 直组 2.1mm 形状偏差已量化**——中位 0.00 / p90 1.72 / **最大 2.08mm**、**22/60 笔 >0.8mm**，偏差最大两笔恰是**碧王竖 2.08 / 碧白竖 1.97（＝用户点名那两笔）** ⇒ **D2 改以"形状保真"保留、2.1→0.8**（离线 +6.9%/行、命令点 372→426）。**下一步首选＝零代码湿照/视频判决**（"走行少水 vs 干燥"，并看清溪前两点/来首笔/门点）；**顺手关掉 bugs.md 的 y 轴镜像悬案**。详见 context.md「笔画质量·第 1 轮」节第 10 条。
**（第 1 轮「笔画质量」交叉讨论·复核方已交稿·本轮未改任何项目源码）**：对象＝「闲来垂钓碧溪上」照片 + `audit_20260922_213935.jsonl` 第 2 次 run（379 点 / 155.1s / byte2 / z_settle250）。我方回复稿已生成：`C:/Users/ceiii/.qoderwork/workspace/mucqjq4ir3q11afi/outputs/笔画质量-第1轮-复核方回复-本项目agent.md`（等对方回应）。**四条承重结论**：①**口径**＝`gap(T_k→T_{k+1})` 是"到达 P_k"的预算，逐点残差中位 **+12ms**（软件计时完全按设计生效）；②**字首大跳 5 处**（来 54.3/垂 80.1/钓 80.1/**溪 69.3**/**上 81.5** mm），实得预算只有 **150ms**（`cold_start_min_ms` 冷启动地板，不是 12ms），按 v=128 欠 358~566ms，闲/碧 有锚点预定位保护 ⇒ 这是"划痕 + 首笔无墨"的唯一被证实入口；③**照片证明"缺笔"＝没上墨不是没执行**（碧石上横 43.7mm 单指令＝两端驻留墨团 + 中间连续极淡线；垂长竖同型）⇒ **"走行被截断"这一支本轮无证据，P-3 不做**；④**统一机制**＝墙钟 155.1s = 我们自己的间隔 140.2s + 13.3s 主动停顿 ⇒ 设备从未超时 ⇒ 笔"窜一下→停住等"，**墨只在驻留时落下**，而 **29/60 笔（48%）是"2 点=单指令"**（碧 9/14）⇒ 用户抱怨的"垂没写到位/碧短竖/钓竖提过短"全落在这一族。**下批建议**：D1 代码＝去掉 `if (ci==0)` 闸门让每字都预定位（播种 lastSent 余量不够）；D2 config＝`rdp_tol_mm(直) 2.1→0.8`；D3 可选＝把 `readPose` 写审计。**待用户答 4 问**（划痕在第几字／闲门点有无淡线／垂长竖末端有无淡线／时间预算增量上限），详见 context.md 新增「笔画质量·第 1 轮」节与回复稿 §6。
**（第 3 轮交叉讨论·进行中·本轮未改任何项目源码）真机「笔画残缺」**：第 1~6 次往返的历史台账已归档（`archive/笔画残缺-历史讨论-2026-09-20.md` + outputs 各文）。**本轮用审计日志把"每点真实预算"量了出来，推翻了上一轮收敛方案的两条前提**：①真机落笔走行中位 = **193/155/153/123ms（档1/2/3/6）**，不是模型表列的 242/162/121/53——因 Z 过渡 +95ms、拐角停顿 +80ms、串口地板 43ms 三项与档位无关；②**拐角停顿判据语义写反（`angle_between` 给内角，直线=180°）⇒ 命中 100% 的落笔内点（1459/1459），真拐角仅 0.8%**，每点白拿 +80ms：既掩盖残、又是抖的主因，修它必须与"改 v/地板"同一次改。③**「速度反向」实测不成立**（byte1 最慢、byte2..5 越高越快，只有 byte0 符合手册"00 最快"）⇒ 作废；质量与设备巡航无关的强证据改用"档6 设备更快却更残 + 档2/档3 实际节拍只差 2ms 而质量同级"。④**路线 B（闭环回读）否决**：手册「适用产品 3 轴步进」⇒ 无位置源；一次 0x03 轮询 55~70ms ≈ 要省的时间；且治不了"每条 0x0008 都减速到零"。⑤候选**零代码**修法 `min_point_interval_ms≈120 + z_settle_ms=0`（地板只抬短段）+ 审计自检。⑥对方已独立复现我的两条承重结论（判据推导 + 逐档中位 ≤3ms），并接受全部改口径；**第 3 轮第二次往返我再判他加的那条量化**：他的"修拐角后档1 塌到 193−80=113ms"数不对（中位角贡献 ≈58ms ⇒ 正确 134ms），但**真问题更大**——修后 <150ms 的点占 54%/66%/80%/95%（档6 95% 掉到 43ms 串口地板），故"必须同批落地"成立且理由更硬；他的"地板=190–200"是安全值但**形状不对**：按段长分桶，<1.5mm 桶四档几乎无差别（115~123ms）而差异全在长段 ⇒ **需求随段长增长**，正解是 `base=max(串口地板, d/v+C)`（v≈28、C≈80，"角点白送的 80ms"其实就是模型缺的每点固定开销）。⑦**第 3 次往返**：对方出 P0 上机单，我复核出 6 处要改（缺审计自检、地板抬高 Z 驻留会炸毛、P1 低行需逐行 z_settle、P0b 地板 700 喂不饱长段须换小字号、P2 比 byte 仍被污染、改 config 前先关 GUI），并**自查出我自己 §2.2 分桶表的归属错了一格**（`>12mm` 档1 正确值 710ms 而非 194ms）——修正后结论升级为 **`v_req ∈ [28,42)`、`C≈80`、常数假设排除**（用同 d 下两档夹逼，不依赖新实验）。⑧**第 4~5 次往返（路线定案）**：对方按正确归属独立重算复现了修正表、6 条全收，并提议重排序——**先写"锁档1 的安全 diff"，把 P0 系列后置为提速/去抖**。我选 **(A) 采纳**并给出三条可验证理由（档1 既干净又最快、新形式逐点 ≥ 当前档1 ⇒ 必然干净是可证的、byte 问题零决策价值），另补**两处 diff 约束**：①"表全压平"会让"抬笔走行提速"失效（`p.speed` 同时定字节与模型 v）⇒ 改为压 档2~5、留 档6=128 作抬笔专用快档；②`z_settle_ms = 107 − C = 27`（新形式下 Z 预算 = C+z_settle）。并给出**可证伪的验收目标**（离线算出：落笔中位 ≈226ms、最小 ≥100ms 不再有 43ms 地板点、单行 ≈102s）+ 预期管理（首跑应像今天的档1：完整但仍有轻微顿）。⑨**第 6 次往返（diff 复核通过）**：对方把 Phase 2 计时 diff 写进工作区，我逐项核了**工作区代码**（不是其描述）：`base=max(floor, t_xy+C)`、`z_settle=107−C=27`、`speedLevelToXYmmPerSec` 档1~5=28 档6=128、拐角 `turn=π−ang` 与 C 同批、新键 `point_fixed_ms` 全链路且 `geti` 自带 `clamp[0,3000]`——**五项全对**；另自跑 `g++ -fsyntax-only -std=c++17 -Wall` 三个 TU **exit=0 零警告**，并离线重放得**预算 +5.8%（96.1→101.7s）、拐角 +80 命中 131→1、落笔中位 193→≈226ms、最小间隔 43→≈100ms**。**三条注意项**：①档6 现在是危险档（GUI ± 调到 6 会让落笔按 128 给时间、残废复活）⇒ 验收前确认速度档=1，长久解 = 抬笔提速提交里按 `isPenDown` 选 v；②锚点预定位等待被顺带拉长（×1.5，安全方向，另可优化）；③`使用说明.md` 待补 5 句。⑩**第 7 次往返（确认跑 → 收口验收通过）**：对方重编后用户真机跑一行，我独立 parse 审计（`audit_20260921_215926.jsonl`）复验：270 点全档1、落笔中位 233ms、最小 108ms、**<45ms 地板点 0 个**、拐角命中 **131→1**、**落笔点逐点模型误差中位 8ms/最大 26ms/0 个 >30ms**、retry=0、`task_end=ok`、**真实书写 99.7s**（纠正了对方"150s"的口径：那是含 48s 设置/预检的墙钟）。**判收口**（墨迹以用户目视为准，手续=照片存档 + 标注"C=80/v=28 基线"）。**新发现时间结构**：99.4s = 节拍 82.6 + **逐字锚点等待 16.7**（`move_up_to_and_wait` 用落笔常数 d/28 去等抬笔移动 ⇒ 过等 ~40×）+ 重置 1.2 ⇒ 下一轮"`isPenDown` 解耦 + 抬笔提速"预计 ~100s→~60s 且落笔预算逐点不变（可证伪判据=预算序列逐点 diff）。⑪**第 8 次往返（治串珠 C 扫参单复核）**：对方改出《治串珠-C扫参上机单》（C 80→65→50→40→30、只动 `point_fixed_ms`），我审出 4 条要改：①**C 与 Z 预算耦合** ⇒ 必须同步 `z_settle_ms=max(0,107−C)`（否则低 C 行 Z 预算 <70ms、通道A 复活被误判成"C 压过头"）；②审计判据改成"逐点对表 + 有效下界 max(43,C)"（低 C 小段回落 43ms 地板属正常）；③加末尾 C=80 漂移对照；④加"字号 90、C=80"一行判**珠子挂在命令点还是粘滑**（决定要不要做 RDP 那轮；早期点纹间距 ~0.9mm ≪ 命令点间距 ⇒ 粘滑可能不小）。基线照片已存档、我已看图核对（5 处截断全消 ✓ 串珠可见 ✓）。⑫**第 9 次往返（v2 复核）**：对方出《治串珠-上机单-v2》，4 条修正全部并入且正确（`z_settle=107−C` 逐行配平、审计判据改"逐点对表+有效下界 max(43,C)"、B5 漂移对照、Step A 先判珠子归属）。我再补 3 条让 Step A 不白跑：**改单字「里」+每页字数=1**（90mm 时 5 字必互压、布局被 clamp 不可比；「里」长竖是第 5 笔、避开冷启动窗）、**两行都存审计**用 mm/px 自校准后与命令点间距直接比、**加一张空白纸同倍率照片**做基底对照；并纠正 key：权威字号键是 `free_char_size`（`layout_char_size` 当前路径不用）。⑬**第 10 次往返（Step A 结果复核）**：用户跑完 Step A（单字「里」60/90 + 空白纸，`test/test3/` + audit `audit_20260921_222812.jsonl`），我独立复算+看图确认：**珠子不在命令点上**（90mm 竖笔 5 点/63.0mm、最大段 **51.9mm**，而该单指令段内有 ~12–14 珠；预算误差中位 14ms ⇒ C=80/z_settle=27 生效）⇒ **Step B（扫 C）与 RDP 轮取消** ✓，原理上 C/v 都不改设备运动（只改空闲）⇒ 珠子间距对二者不变量。**纠正对方三条论据**（"不随字号变"无数据、1.37mm 在分辨率极限、空白纸对照未排除纸纹方向）。**我补的新线索**：珠子**只长在竖笔**上、横笔（含 90mm 底横）光溜 ⇒ 方向效应 ⇒ 候选 A 机械/驱动（GT2=2mm 齿距、雅可比投影）或 B 纸/墨（空白纸横向纹路 1–2mm ≈ 珠子间距）。**给两个零代码判决实验**：①纸转 90° 写同一个「里」（珠子跑到横笔⇒B / 仍在竖笔⇒A）；②速度字节扫参 `speed=1/2/3/4`（档1~5 的 v 都 28 ⇒ 只换字节、计时不变；随字节变⇒A 且可直挑最轻档）。⑭**第 11 次往返（判决实验单复核）**：对方出《治串珠-判决实验①②-操作单》并**坦白一处**（他的"横笔光溜"量化做错了轴、CV=1.57 是假信号，方向性改由实验①判）✓；我核过操作单（当前 config = `speed=1/point_fixed_ms=80/z_settle=27/free_char_size=90/writing_plane_z=-352` ✓ 与前提一致）并提 4 条修正：①**换字节也要核截断**（字节改短段动力学；某档珠子最少但出现截断 ⇒ 不合格）；②**档5 是安全的**（只有 6 档跳 128）⇒ 应测 1/2/3/4(+5)；③② 末尾补**档1 复测**做漂移对照；④① 建议**同日两纸对照**（原方向 + 转 90°；旧 `90.jpg` 是上次会话的）。另给**三格判决表**（跟纸走⇒B；不跟纸+间距随字节变⇒A-时间型；不跟纸+不变⇒A-空间型/皮带齿）与"② 只看间距/条数、别只看深浅"的判读纪律。⑮**第 12 次往返（用户改路线 + 治抖方案收敛）**：用户拍板——只治**速度1 的抖/串珠**、视为机器侧**不再验证前提**、只用速度1、**不再上机做判决实验**、改成"改代码→写一行看效果→不行换切入点" ⇒ 判决实验①②作为正式实验**取消**（其问题改用"改一行看效果"顺带回答）。对方据此出治抖三条（RDP 0.35 / C 55 / 落笔字节解耦试 byte1）；我复核定稿：①RDP 成键保留 0.35 但**预期下调**（实测删点率 0.35≈10–12%，真正减停在 0.5–0.8）；②**C 55 必须配 `z_settle_ms=52`**（Z 预算恒 107）；③**③改成 `estimateMoveMs` 按 `isPenDown` 选 v（落笔 28/抬笔 128）**——落笔计时不变（完整性零风险）+ 抬笔/锚点掉到 d/128（整行 ~100s→~60s = #10 收益，不必改 `p.speed`）+ 消灭"档6 危险档"；ink 字节留 GUI ⇒ 试 byte1 变零代码；④`cold_start_min_ms` 150→0。**并明确告知用户：本轮治顿挫、治不到串珠**（珠子在单条指令内部）。⑯下一步 = 对方按此实现+重编 → 我复核 diff → 用户写一行看效果（审计：落笔预算只按新 C+新点集变、抬笔/锚点应掉到 d/128；照片：5 处截断仍在完整档 = 刹车）。⑰**（已实现·RC=0·待真机）治抖合并 diff 落地**：`estimateMoveMs`/`move_up_to_and_wait` 改按 `cur.isPenDown` 选 v（落笔 28 / 抬笔 128，`speedLevelToXYmmPerSec` 降为兼容/字节语义不再参与计时）⇒ 落笔逐点计时不变（完整性零风险）+ 抬笔/锚点掉到 d/128（整行 ~100→~60s）+ 消灭档6危险档；RDP 容差提为 config 键 `rdp_tol_mm`（默认 0.35，`motion.cpp` 用 `g_rdp_tol_mm`）；`point_fixed_ms` 80→55 且 `z_settle_ms` 27→52（Z 预算恒 107）。**GUI 书写页新增两实时框**：「落笔C ms」(int，改它自动配平 z_settle=107−C，服务层 `set_point_fixed_ms`) 与「抽稀 mm」(float，`set_rdp_tol_mm`)，两段式 EN_CHANGE/KILLFOCUS、`WLGeo` 加 row4Y 并把按钮/状态框下移。两 exe g++ 重编 RC=0、dryrun 启动自检过。**本轮治顿挫+提速，治不到串珠**（珠子在单条指令内部=粘滑/机械，唯一软件抓手=换速度档=零代码字节试验）。参考表：outputs《书写调参与验收参考表.md》。⑱**（已实现·RC=0·GUI 实操核验过·待真机）参数按笔画直/弯分组**：实测"小参数利曲线(撇捺弯钩保形)、大参数利直线(横竖少点少停)"两组相反 ⇒ C 与 RDP 各拆两组：`point_fixed_ms`/`rdp_tol_mm`=直线段、新增 `point_fixed_curve_ms`/`rdp_tol_curve_mm`=曲线段。分类在 `transmitTrajectoryWithSplit` 两段式 RDP（先按直线容差 RDP，塌成≤2点判直线 `strokeKind=0`，否则按曲线容差重算判 `strokeKind=1`），`Point` 加 `strokeKind` 字段，`estimateMoveMs` 按 kind 选 C；`z_settle` 自动配平=107−min(C直,C弯)（`sync_z_settle`）。GUI 书写页扩成**四框两行**（落笔C直/弯、抽稀直/弯；`WRITE_PANEL_H` 346→384、`WLGeo` row4Y/row5Y+c1/c2 列）。**修两 bug**：①标签省略号→短标签"落笔C直/弯·抽稀直/弯"+列宽 78；②切页丢值→根因 `cfg` 快照未含这些键致 `snapI` 回退默认，改**预填读实时 getter**(`gs::point_fixed_ms()` 等)并把四键加进快照 cfg，文本框 EN_CHANGE 加 `save_last_task_text` 自动存。Computer Use 实操核验：四框显示 60/30/0.60/0.10、标签不截断、主页↔书写任务往返值不丢。默认 seed 直组大(60/0.6)、弯组小(30/0.1)编码用户发现。参考表同步更新为"直/弯分组版"。⑲**（用户真机新反馈·下一轮靶心）大字号连珠压不住**：字号 60→90 时**两组参数怎么调都减不了"停顿连珠"**——细笔画每个落点停一下留一粗点、再走(细)再停(粗点)交替，弯笔尤甚（穷·力·丿 一笔约 8–10 粗点），**字号越大越明显**。⇒ 连珠根因**不是每点 dwell(C 调不动)**，而是 `0x0008` 逐点 move-to「每点减速到零」的**协议固有底** + 大字号下点间距变大让粗点更稀疏显眼（与 Step A"珠子在单指令段内/方向效应"一致）。**下一轮（新窗口）专攻笔画顺滑/去连珠**，与另一 agent 交叉讨论，**只做必要验证、不钻牛角尖、不做多余测试**，产出合理有效方案。
> ⚠ **⑲ 的定性已于 2026-09-22 修正（原文作废，保留备查）**：⑲ 写的"协议固有底＝每点减速到零、每个落点停一下留一粗点"是 **Step A 之前的旧判据**，与 ⑬（Step A：珠子在单指令段内）自相矛盾，且已被下述 ⑳ 的像素级实测否定。**正确口径**：「落笔点**端点**有一次静置（实测仅轻微加宽 ≈1 个"胖头"，属**次因**）；主因是指令**内部**的 ~1mm 空间型调制 —— 首选"纸面纹理调制墨转移"，关节侧微步/机械纹波未排除。」对第 1 轮交叉讨论（对方指出 §3.3 与 §19 冲突）的回应：**纠偏成立**，本文件按此改口径。
⑳**（2026-09-22 第 2 轮交叉讨论·零上机·全部用已有审计+已有照片实测）去连珠的量化事实**：①**命令点数与字号无关**——同一次会话（`audit_20260921_222812.jsonl` 两次 `task_start`）60mm 与 90mm 各写「里」，**落笔命令点都是 38 个**；字号 ×1.5 ⇒ 段长 ×1.5（最长单指令段 44.73→**67.10mm**；"51.94mm"是第 5 笔第 3 段**不是最大段**，交接稿 §3.3 该处措辞需改）。⇒ 大字号改变的是**段长（笔尖速度/墨膜厚度）**，不是"点间距"。②**命令点处不产生珠**：第 5 笔（5 个命令点 y_img=248/260/288/525/536）逐行量墨量/宽度，命令点行宽 15/17/13/12 **平平**；51.94mm 单指令段内（无命令点）干净段宽 4–10px 波动；**唯一的宽斑在 y=325/390/480，全是与横笔的交叉**；笔末 y=525–540 宽 12–13 vs 干净段 7–10 ⇒ **端点静置确有加宽但量级很小（≈1 个胖头）**。③**细纹周期 ≈ 同图纸纹周期（本轮最重要）**：竖笔墨量自相关在 **4.36px** 有一梳峰（13/18/21/27/30/34 全是 4.36 的整数倍），**同图纸面纹理 4.60px**（自相关 k=4=0.897），同图 px/mm≈4.5–4.7 ⇒ 珠子空间周期 **≈0.93–1.0mm**、比值 **0.95**；即"12–14 珠"是照片目数，真实是**约 50 个周期**（对方 §A 的 3.7–4.3mm 尺度大了 ~4 倍，而尺度决定"哪些手段按构造治不到"）。④**横笔无周期性**：90.98mm 底横（含 46.76mm 单指令段）墨量 CV 0.185、自相关 3–70px **无任何峰**；而纸纹是强横向 ~1mm 结构 ⇒ 竖笔（夹角≈88.7°，纸纹预测 1.00mm）与底横（夹角≈2°，预测 28.6mm）**被同一个纸纹模型同时解释**。⑤**相位锁定证不了（如实记录）**：纸纹横向**不跨 x 自洽**（带1×带2 相关 0.497、带1×带3 仅 0.075）⇒ 带通互相关检验无效；墨量×纸纹 0.16–0.37 而零假设 σ=0.098 ⇒ 不显著。故"纸面纹理/墨转移"列为**首选而非定论**。⑥**"字号越大越明显"新解释**：60mm 墨量 CV **0.16** vs 90mm **0.31**（跨照片，仅提示性）⇒ 调制一直存在，段更长/线更薄时从"边缘毛边"变成"断线串珠"。⑦**代码事实**：`estimateMoveMs` 的 `Cfix` 按**该点自己**的 `strokeKind` 取，而 `sync_z_settle()` 用 `107−min(C直,C弯)` 配平 ⇒ 当前 config（C直=85/C弯=25、z_settle=82）下**直组点的 Z 过渡预算 = 85+82 = 167ms**（注释声称恒定 107 的只对弯组成立）；若见"直笔端点/起笔墨团、炸毛"根因在此，别怀疑 C。⑧**交接稿 §9 的 config 快照已过期**（实机为准）：`point_fixed_ms=85`、`point_fixed_curve_ms=25`、**`rdp_tol_mm=2.1`**、`z_settle_ms=82`、`writing_plane_z=-351`、`free_char_size=90/glyph_orient=3/speed=1`。
  ⑳**修正：全项目最贵的未验证假设 = "落笔笔尖按 28mm/s 慢拖"**。软件模型给 51.94mm 段 **1934ms**，而已实测巡航 **415–1185mm/s**（byte1 最慢 415、byte0 最快 1185）。若设备其实 ~0.1s 就走完，则珠子是"**高速拖笔飞白/断墨**"叠加"**每个命令点静置 1.5–1.9s 积墨**"，此时"减停/减点"与 C/RDP 都不是抓手，"**让笔尖真的走慢**"才是；同时整行 100s 里绝大部分是"让笔在纸上干坐"（潜在大额时间可省）。**三种仪器（成本递增）**：①问用户"写长竖时臂是匀速慢慢挪还是窜一下然后停住等"（零成本，能分流一半方案）；②手机慢动作数帧；③现有 `readPose`(0x03) 在一条测试线上连续采样（一次轮询 55–70ms，够分辨 0.1s vs 1.9s；占单总线、会扰节拍，仅诊断）。**本轮方案排序（给对方的收敛建议）**：①换纸/把纸转 90°（零代码、一次上机、直接判决调制源尺度）②字节扫参**扩到 0–9**（GUI `SPEED_MAX=6` ⇒ byte6–9 从 GUI 不可达，而实测编码非单调，"哪个字节最慢"从未真正遍历）③下刀**加深**一档（-351→-354/-357，与对方"抬浅"**方向相反**，互为对照，红线炸毛即退）④量设备真实走位时间；C/RDP/dwell 一律不再动。**另**：`enable_dip=false` 而比赛要求"书法与国画均需自主蘸墨至少一次" ⇒ **合规风险项，勿忘**。
**（已实现·离线验证通过·待真机）页尾自动复位抬笔 `UP-PAGEEND`（2026-09-20）**：每页（含末页）逐字循环结束后，在 `g_last_pose` 最末落笔点**原位显式再发一次 Z_UP 抬笔并短驻 300ms**，作为"写完本页"的确定收尾动作；放在页尾补蘸**之前**（抬笔不被后续动作替代，且"书写结束前至少一次蘸墨"合规语义不变）。用户定案：①复位方式=原位抬笔+短驻（不退页末安全点、不回 Home）；②与翻页前原 `UP-PAGETURN`（抬到当页末字中心）**合并去重**——翻页块只发信号+审计，不再重复抬笔（拖纸安全由笔已抬起保证，与 XY 无关）。改动仅 `gui_service.cpp::run_task_thread`（+12/-6 行），未碰 motion/hanzi/serial_port/gui_trail，无 config 新键（300ms 硬编码，需要时再参数化）。验证：g++ 直连重编 Robot.exe/RobotGUI.exe RC=0 无新 warning；`verify_pageend_lift` DRYRUN 端到端 **11/11 PASS**（审计判据=每页 page_turn/task_end 前最后两条 send_point 位姿完全相同且 pen-up=UP-PAGEEND 紧跟 UP-ENDCHAR；两页抬笔坐标互不相同=各停当页末字；page_turn 恰 1 次；无独立 PAGETURN 抬笔残留）。**待真机**：同坐标 Z_UP→Z_UP 空移在控制器侧是否瞬时完成不报错（协议每点到点停模型下应无害）；300ms 短驻手感是否合适。**已知副作用（可接受）**：UP-PAGEEND 与 UP-ENDCHAR 位姿相同，实时轨迹/审计各多一条重复点与一条 send_point 事件。
**（已实现·离线单测78/78+dryrun端到端+Computer Use实操全通过·待真机）GUI 书写任务「分页 + 翻页」（2026-09-20）**：一页写完→向翻页机构发信号→等翻页→抬笔清轨迹→写下一页。用户四项决策：①每页独立布局可逐字拖拽；②翻页走纸量=版面高、坐标重合（不做偏移补偿）；③超容量只写前 N×每页 字（截断）；④翻页等待期点停止/急停立即中止不翻页。实现要点（**仅改 `gui_service.{h,cpp}`+`gui_win32.cpp`**，未碰 hanzi/motion/serial_port/gui_trail）：
- 服务层分页模型：`g_full_all`(过滤后全量不截断)/`g_full_text`(截断到容量=实际书写)/`g_pages`(每页 `PageEntry{text,cells}`)/`g_page_chars`(1~50)/`g_page_count`(1~20)/`g_edit_page`+`g_write_page`(atomic)双游标。`rebuild_pages` 切页并**三级继承拖拽坐标**（同下标同文本→按文本认领→按字首次出现继承，全不中才补初始网格），保证改页数/每页字数/容量恢复不丢摆位。
- 翻页预留接口：`gs::PageTurnFn=set_page_turn_handler(fn)`，默认 `page_turn_wait_locked()` 持 g_mu 分片 50ms 睡眠并轮询 `g_task_cancel||g_estop`（命中返回 false→不翻页→任务 canceled）。GUI `Run()` 有注入锚点注释。审计事件 `page_turn`(发信号)/`page_turn_result`(ok/canceled/turn_failed)。
- `run_task_thread` 改多页循环：预热→蘸墨→逐页(本页首字预定位锚点→逐字书写→跨页全局每5字蘸墨→非末页抬笔到末字上方→翻页→trail::reset→下页)→末页收尾蘸墨→描边。`task::page_begin(idx,total,n)` 每页复位页内计数；snapshot.task 加 `page_no/page_total/display_page`，cfg 加 `page_chars/page_count/edit_page/page_turn_wait_ms/task_finished/text_total/text_written`。
- config 新键 `page_chars/page_count/page_turn_wait_ms/page_text/page_text_full/page_cells`(嵌套)；`cfg_load` 兼容旧单页键 `free_text/free_cells` 自动迁移为第 1 页。
- GUI 书写页：左栏加「每页字数」「总页数」两段式整数框（EN_CHANGE 合法即套用不回写、KILLFOCUS 夹取回写，同字号教训）+ 截断提示 STATIC；轨迹面板加**翻页条** `◀ 第 k/N 页（本次书写 M 页）▶ …翻页等待(ms)[输入]`（`WritePlotGeo` 新增 strip 几何、`WRITE_PANEL_H` 300→346）；拖拽门控 `gs::page_drag_enabled()`（空闲+显示页==编辑页+无历史轨迹），任务中画布跟随书写页禁拖、空闲跟随编辑页可拖；`RefreshLayoutPreview` 任务结束瞬间刷新。
- 验证：g++ 链接 RC=0 无新 warning；`verify_pages` 单测 **78/78 PASS**（切页/截断/容量恢复不丢字/每页隔离/坐标三级继承/旧键迁移/漂移重排/preflight/翻页成功+失败+默认模拟等待+**7.4 等待期 abort 立即中止且停在第1页第2页不写**）；dryrun 端到端审计链 `task_start→page_turn→page_turn_result=ok→task_end=ok`；Computer Use 实操：总页数=2 生效、▶◀切页、第2页拖「十」到左下、切回第1页布局不受污染（隔离）、开始书写后轨迹面板翻页时清空重播且只显示当前页笔画（十/二/六）所见即所得。临时测试文件 `verify_pages.cpp` 已移出项目目录（存 workspace `verify_pages.cpp.bak`），`robot_config.json` 测试运行态已 `git checkout` 还原。**待真机**：①翻页机构实际走纸是否精确=版面高（否则整页偏移，届时加偏移参数）；②翻页后软件位姿 `g_last_pose` 不变的前提是否成立；③真实蓝牙信号替换默认模拟（在 `Run()` 锚点 `set_page_turn_handler`）；④翻页等待期真机拖纸时臂是否安全抬笔避让。详见 context.md「分页 + 翻页约定（2026-09-20）」。

**（已实现·离线+Computer Use 实操核验通过·待真机）GUI 书写页排版重构为「自由拖拽 + 字体朝向」（2026-09-19，取代下方 Phase1/Phase2 网格方案）**：按用户要求把书写页左栏精简为**只留「字号」+ 新增「书写方向(字体朝向)」下拉**，删除 每行字数/上区占比/行距/手动/应用/恢复自动/竖排 全部网格控件；网格降为**初始摆位**，用户在下方画布内**逐字拖拽**定布局（磁吸对齐、夹取在固定视野内、按文本绑定持久化、所见即所得）。固定视野=`gs::view_bounds()`（四角标定框，未标定回退可达 X±162/Y±85），**预览与书写共用、不再随数据自动缩放**（修掉“写第二个字时画布缩小”的问题）。字体朝向 4 档仅整字旋转不改位置。改动仅 `gui_service.{h,cpp}`+`gui_win32.cpp`（未碰 hanzi/gui_trail/serial_port）。验证：g++ 链接 RC=0 无新 warning；纯数学单测 29/29 PASS；dryrun RESPONDING=True；Computer Use 实操确认拖拽落库、朝向仅旋转、固定视野不缩放。详见 context.md「GUI 排版（自由拖拽 + 字体朝向）约定」。**待办**：真机核验书写落点与预览一致；如某朝向 CW/CCW 与预期相反，改 `rotate_local`/`esc[4]` 一处。

**（已被上方自由拖拽重构取代，保留备查）GUI 字号/布局实时调整 Phase1（字号+网格，横排）**：并入「书写任务」页（不新开页），输入文字后可实时调字号/每行字数/上区占比/行距并在轨迹画布叠看版面预览。设计=自动/手动双模式，默认自动保持原行为零回归。实现：`robot_common.{h,cpp}` 新增手动全局 `g_layout_mode/g_lm_char_size/g_lm_cols/g_lm_top_ratio/g_lm_row_spacing` + `TextPlan.err/row_spacing` + `LayoutErr` 枚举（字号下限锁 60 比赛红线、上不封顶）；`hanzi.cpp` 新增 `plan_manual_layout`（按用户值严格排布+纯 fit 检查，放不下置 err 且**不自动缩放**），`prepare_layout_only` 按 `g_layout_mode` 分流；`gui_service.{h,cpp}` 新增 `set_layout_mode/char_size/cols/top_ratio/row_spacing` + `layout_preview`（不写审计的实时预检+计划字块世界坐标）+ cfg 五键持久化 + snapshot.cfg 暴露 + preflight 补 mode/row_spacing/err_code；`gui_win32.cpp` 书写页左栏加排版控件（`WLGeo` 共享几何防错位、"手动排版"复选框+应用/恢复自动）、`EN_CHANGE` 实时刷新、方案二把计划字块（紫点线框+自适应字形）叠画到 `DrawTrailPanel`（外接框并入 cells，任务中冻结不叠画），`IDC_BTN_WRITE` 加排版可行性闸门。验证：GUI+控制台 g++ 链接 RC=0；排版单元自测 20/20 PASS；`RobotGUI --dryrun` 启动冒烟 Responding=True。

**（已实现·离线验证通过·待上机）GUI "书写平面"页**（与主页/设备连接/书写任务同级，专调落笔 Z）。解决"书写平面低于桌面"。实现：新全局 `g_writing_plane_z/g_writing_plane_valid` + `robot_config.json` 持久化 + `snapshot["cfg"]` 暴露；`hanzi.cpp` 落笔 `zdown = valid ? g_writing_plane_z : zFor(三层)`（未保存前行为不变）；服务层 `gs::preview_writing_plane`(移 (0,0,Z) 悬停)/`gs::set_writing_plane`(校验+置位+保存)；GUI `ID_PAGE_PLANE` 页含 当前Z显示 / 新Z输入框 / 模拟悬停 / 保存并应用。整合编译 RC=0；单元验证 PASS（设 -365→落笔全 -365、过渡点合法；未设→heavy/normal 分层）。Z 语义为 raw（不含 Z_OFFSET，发送时叠加）；改动文件：robot_common.{h,cpp}、hanzi.cpp、gui_service.{h,cpp}、gui_win32.cpp、robot_config.json。

**（已归档、待上机）顺滑度方案 A/B + 逐点停止 + 节拍可配置 + dryrun 归因**
- 方案A 补偿式计时（`estimateMoveMs` 语义改"目标指令间隔"、去 60/35ms 走停地板、`wait_after_send` 扣掉串口/运动耗时）。方案B RDP(0.06mm) 抽稀（`resamplePolylineRDP`，直段塌两端、弯曲保点）。逐点停止（`estopLiftAt`+书法逐点/描边批量/回退逐点三处 `g_estop` 轮询）。节拍参数运行期可配置（`z_settle_ms` 等 5 键，`gs::cfg_load` 覆盖全局）。整合编译 RC=0。
- dryrun 归因（"书"，关描边）：prev 中位 186ms/min 62ms(地板钉死) → new 153ms/31ms → LOW 109ms/26%<50ms；顿挫大头是每笔起落 dwell+Z 沉降+冷启动，地板次之。RDP 对简单稀疏字不减点。字库缺失"找不到书.json"已定位非回归并派子代理下载 9574 字到 `D:/objects/hanzi-writer-data/data/`。
- 上一轮 Win32 GUI 已完成，本轮不动既有三页布局。

## 下一步 / 待办（新增或承接）
- **★ 2026-09-21 晚·BLE 判据修正（先读这条）**：`tmp/ble_poc/winrt_probe.py` 实测模块 **`connection_status = Connected (1)`** —— 模块**与 PC 已配对、Windows 常驻持着链路** → 它**不广播** → `scan.py`/bleak **必然扫不到（假阴性）**，而 WinRT 按地址访问是 0.1s 级。**「扫不到」不等于「模块不在」**（9-20 的结论已纠正，详见 `bugs.md` 末节）。CLI 闭环复测通过：建链 **0.56 s** → `DONE 30 3000`。
- **★ 蓝牙翻页 GUI 整任务 DRYRUN 已通过（2026-09-21 21:01）→ 当前处于「真机实测」阶段**：审计 `logs/audit_20260921_210129.jsonl`（524 事件，`task_preflight ok → task_start → page_turn → page_turn_ble ok(DONE 30 3000, 3308ms) → page_turn_result ok → task_end ok`），2 页 × 5 字「欲穷千里目更上一层楼」全程跑通。**注意 `task_end.progress.chars_total` 是「每页」计数不是总量**，5/5 属正常，勿误判为漏写。
  - **真机实测步骤见项目根 `真机实测步骤.md`**（本轮新增）：分 6 阶段递进 —— ①开工前核对（关 Dry Run / 选 COM4 / 手放电源）②通信链路（连接→心跳→查询位姿，臂不动）③小幅度动作（回中心→测试点）④落笔深度（书写平面页 模拟悬停 目视）⑤**走纸量标定**（臂不动，只发电机指令）⑥单页试写真落笔 ⑦多页+翻页整任务。
  - **真机阶段的第一个未知量 = 走纸量**：程序**不做偏移补偿**，下一页与当前页世界坐标完全重合，所以一次翻页走纸必须≈版面高。当前排版字高 60 + 行距 8 → 目标 **≈68mm**；须用 `RUN<档位>,<时长>` 实测距离反算时长（档位固定 30 只调时长，改档位不成线性）。
  - ⚠ 做 CLI 电机标定前**必须先取消勾选 GUI 的「蓝牙翻页」**：BLE 一次只服务一个中心设备，GUI 翻过一次页后会一直占着链路。
- 翻页档位/时长的**手感标定**：默认 30 档 / 3000ms 只是可跑通的起点，需按真实走纸机构的走纸量与速度微调（GUI 上直接改即可，会自动存进 `robot_config.json`）。
- **注**：`motor_ble_gui.py` 与本工程现在**抢同一个模块**，两者不要同时用。
- **Phase2：书写方向 已完成（离线验证过·待视觉核验）**——`g_write_dir`(0横排/1竖排右起)+config 键+`set_write_dir`+snapshot+preflight/`layout_preview` 回显 dir；hanzi 抽出方向感知 `grid_extents`/`fill_offsets`，`plan_manual_layout` 与 `plan_text_area_and_layout` 加 `dir` 参（`dir=0` 逐点保持旧行为；竖排右起=列内从上到下、列从右往左，fit 轴向交换）；自动模式竖排候选改偏好短列 `{4,3,2,5}`（横排仍 `{5,4}`）；GUI 书写页加“竖排(右起)”复选框 `g_chkVert`（独立于自动/手动），状态框随方向显示网格。编译链接 RC=0 无新 warning；方向单测 19/19 PASS（竖排字序、轴向交换、自动短列候选、横排回归）；p2 GUI 启动 Responding=True。未验证：书写页视觉叠画与方向端到端仍需关旧实例后用新 `RobotGUI.exe` 实操核验。
- 运行态提醒：`robot_config.json` 当前是某次手动模式测试写入的状态（`layout_mode:1/layout_cols:1/layout_top_ratio:0.9`，且无 `write_dir` 键——由 Phase1 期构建保存），非源码改动；如需回到干净默认，可勾“恢复自动”或在 config 里改。
- **可达极限已探明 + 实验已撤销（2026-09-19）**：把 `dev_limit` 临时放宽到 ±180 真机测试，臂仍**稳定停在 X±162 / Y±85** → 限制在**控制器/机构侧，非软件**。随后按用户要求**撤销整个可达边界实验回原版**：`g_devLimit` 恢复固定 ±180，删除 `cfg_save/cfg_load` 的 `dev_limit`、`snapshot.cfg.dev_limit`、`robot_config.json` 的 `dev_limit` 键；重编 RC=0。保留四角标定/轨迹面板/结果提示。**结论留存**：真机上电可达 X±162/Y±85（断电可手推越过），疑控制器工作区软限位（手册 ±180 但固件设小）；软件无解，需问厂商放参数或改机械。
- **速度编码疑似反向**：`serial_port.cpp` 发 `level-1` 与 `speedLevelToXYmmPerSec` 方向相反，手册 `00 最快`；上机先定真实"档→mm/s"方向再决定是否反转映射。
- **auto_draw 描边走 `0x0064` 是抓放宏（手册明确）**：真机会把"画山水"当抓放动作，风险高。修：把描边发送路径由 `sendPointsBatch7` 改回逐点 `sendPointRetry`；在改完前保持 `auto_draw=false`。
- **停止按钮 GUI 侧差异未定位**：静态看 `abort_task` 与 `request_estop` 都置 `g_estop`，用户报"停止没反应/急停有效"；上机若仍复现，看提示文本与 `task_abort` 事件确认是没触发还是段延迟。
- **HanziWriter y 轴方向**：项目未翻转 y，若真机字上下颠倒，是翻转字形 y 的问题（`bugs.md` 有记）。
- 上机阶段 3（判定控制器运动模型）由用户执行中。

## 成功标准
`gui_win32.cpp` 三页（主页/设备连接/书写任务）可交互且版面正确（无字段溢出、无文字截断、无图形压字）；所有 GUI 操作经 `gs::` 服务层写入结构化 JSONL 审计（`source=GUI, actor=HUMAN`）；与控制台程序共用配置、运动安全校验和日志链路；未接入硬件状态显示为“未接入/协议待确认”。

## 范围边界
本轮只做 GUI 与审计链路，不改控制器协议、不改运动学；真机运动参数（Z 深度、批量 7 点、蘸墨）仍需上机验证，不得据 DRYRUN 结果视为已确认。

## 停止条件
<!-- 什么情况下应暂停并汇报 -->

## 已完成
### GUI 实现（gui_win32.cpp / gui_service.cpp / gui_service.h）
- 三页 Win32 GUI：主页（只读状态快照 + 快捷操作 + 运行开关）、设备连接（串口枚举/连接/断开/刷新）、书写任务（文本输入 + 预检 + 开始/停止 + 进度）；WM_PAINT GDI owner-draw，紫色标题栏 + 左侧导航 + 底部状态栏，与 `gui_design_mockup.html` 视觉一致。
- 服务层 `gs::`：统一状态快照 `snapshot()`、配置读写 `robot_config.json`（与控制台共用）、串口连接、写字任务线程（detach + 原子取消）、急停 latching 标志、结构化 JSONL 审计（会话头 + 每事件 flush）。
- 审计事件覆盖：session / connect / disconnect / send_point / send_batch7 / task_preflight / task_start / task_abort / task_end / estop / estop_clear，全部带 `source`、`actor`、`mode`、`operation_id`、`request_id`。
- 构建：新增 `build.bat`（MSYS2 MinGW g++，静态链接）一次产出 `Robot.exe` 与 `RobotGUI.exe`；`Robot.vcxproj` 已加入 `gui_service.cpp/h`（`gui_win32.cpp` 作为 GUI 独立入口，不进控制台工程）。
- 运行时状态文件 `gui_window.txt`（窗口尺寸）、`last_task.txt`（上次任务文本）已加入 `.gitignore`。

### 操作核验（全部通过）
- 预检：写入 `last_task.txt` → 启动 GUI → 切页预载文本 → PostMessage WM_COMMAND 1009 → 审计出现 `task_preflight result=ok`，界面显示“排版可行：1 字，1 列 × 1 行，字号 60.0 mm，字间距 1.0 mm，速度 3 档”。
- 开始书写（DRYRUN）：WM_COMMAND 1013 → 审计出现 `task_start` 并持续增长 `send_point` / `send_batch7`，界面进度条与阶段（预热→书写→描边）实时刷新。
- 停止任务：WM_COMMAND 1014 → 审计出现 `task_abort` + `task_end result=canceled error_code=estop`，界面回到空闲、按钮重新可用。
- 控制台回归：同一批源码重建的 `Robot.exe` 与拆分前基线 `_robot_base.exe` 对比，DRYRUN TX/RX 帧前 1736 条逐帧一致。

### 已修复缺陷（本轮）
- **`gs::preflight` 递归加锁导致 GUI 主线程死锁**（详见 bugs.md）：`preflight` 先持有 `g_mu` 再调用同样加锁的 `session_id()`；MinGW pthread 下 std::mutex 非递归，主线程永久阻塞，表现为窗口“未响应”。已把 `g_mu` 改为 `std::recursive_mutex`（31 处 `lock_guard` 同步替换）。
- 排查中确认的注入链路结论：跨进程 `SendMessage` 传缓冲区（WM_SETTEXT / WM_GETTEXT）会挂死 GUI 主线程，**禁止使用**；跨进程 `PostMessage WM_COMMAND` 安全可用；跨进程 `PostMessage WM_CHAR` 不安全（同样导致未响应）；切页须用 DPI-aware 真实点击，`PostMessage WM_LBUTTONDOWN` 不可靠。

### UI 布局缺陷修复（2026-09-18，全部只改 `gui_win32.cpp`）
- 主页“设备信息”网格下标写反（`col=i/2, row=i%2`），实际排成 7 列 × 2 行，第 3 列起溢出面板并被右栏面板覆盖——这是“UI 错位”的主因，15 个字段原先只有 4 个可见。已改为 `col=i%2, row=i/2`。
- 一批固定文本框宽度不足造成的截断：面板标题“输入与任务预检”、右栏标签“最近通信”、“运行开关”参数标签“速度档／字间距／Z 偏移”、以及两个超长信息标签。已分别放宽标题框（200→300）、右栏标签（70→88）、参数标签（52→76），并把两个超长信息标签改成 4 字（实际位置／资源占用）、“坐标依据”取值统一为“软件位姿（最后有效 ACK）”。
- 书写页“任务进度”预览弧线超出预览框并压住底部“比赛要求”提示文字。已让图形区避开提示行，半径按可用高度推算。
- 主页“字间距／Z 偏移”输入框靠一次性标志 `s_cfgInit` 赋值，离开主页再返回后永久为空。已改为在 `CreateHomeControls` 内随控件创建赋值。
- 设备连接页说明条撑满页面剩余高度、单行字垂直居中浮在整块琥珀色空白里。已把说明条高度收紧到 72px。

### 操作核验（续，2026-09-18 复核）
- 三页版面逐页截图核验通过：截图前用 `RedrawWindow(RDW_UPDATENOW|RDW_INVALIDATE|RDW_ALLCHILDREN|RDW_FRAME)` + `UpdateWindow` 强制重绘，再用 `PrintWindow` 抓取，并以 md5 确认三页确实是三张不同画面（主机上 `CopyFromScreen` 会返回冻结帧，是上一轮“三页核验”证据失效的原因）。
- GUI 预检链路再次实测通过：切到书写页 → `PostMessage WM_COMMAND 1009` → 审计新增 `task_preflight result=ok`，`parameters.text="书"`、`source=GUI, actor=HUMAN, mode=DRYRUN`（`logs/audit_20260918_000443.jsonl` 等）。
- 上一轮遗留的“预检报请先输入文本。”已定性：不是逻辑缺陷，而是**从非项目目录启动**导致 `last_task.txt` 按 CWD 解析读不到（详见 bugs.md 与 context.md）；同目录启动可正常预载“书”。

### 书写顿挫修复（2026-09-18，方案A 补偿节拍 + 方案B RDP 抽稀）
- 根因：书法落笔段是“逐点绝对定位 + 发完再固定空等”，每点 `sleep≥POINT_RATE_LIMIT_MS(60ms)` 叠加在 9600 串口单帧往返（~42ms）之上，且每个 0.6mm 途经点都是一次到点全停 → 沿笔画一路“点刹”。全项目原无插补/前瞻/缓冲。
- 方案A（`motion.cpp` `estimateMoveMs` / `transmitTrajectoryWithSplit`）：`estimateMoveMs` 语义改为“相邻两指令的目标间隔”，去掉 60ms/DELAY 走停地板，只留 `MIN_POINT_INTERVAL_MS(12)` 下限；发送循环改用 `wait_after_send(t0,…)` 补偿式计时（串口+运动耗时计入间隔，只补睡到目标节拍），拐角停顿折进间隔不再单独叠加。抬落笔/Z 沉降等物理停顿保留。新增可调常量 `MIN_POINT_INTERVAL_MS`、`COLD_START_MIN_MS`（`robot_common.h`）。
- 方案B（`motion.cpp` `resamplePolylineRDP` + `resample_range`）：书法落笔段改用 Ramer–Douglas–Peucker 按最大弦高偏差 `RESAMPLE_DEV_MM_CALLI(0.06mm)` 抽稀，直段塌成两端点（一笔到底）、弯曲与拐角自动保点；迭代实现防深递归。描边段沿用定步长 `RESAMPLE_STEP_MM_DRAW(1.2)` 不变。（曾用“转角自适应步长”方案，经曲线探针证伪——密集平滑曲线被误判为直线过度抽稀，已弃用改 RDP。）
- 已验证（离线单测，`Robot.exe`/`RobotGUI.exe` `build.bat` 重编通过 RC=0）：完美直线 121→2 点、偏差 0；R=2~200mm 圆弧 RDP 后最大垂直偏差恒 ≤0.06mm 且半径越小保留越密（急弯 1.8 点/mm vs 近直 0.15 点/mm）；纯 XY 落笔点目标间隔 60→12ms、抬笔→落笔仍 202ms、100mm 定位仍按 ~1250ms。
- 未验证（须上机）：真实控制器对相邻指令是“到点全停”还是“可在运动中接受新目标连续走/缓冲”——决定 A 能消除死等但能否根除每点顿挫的上限；9600 波特率单帧 ~22ms 是硬吞吐天花板；方案C（书法落笔改走 batch7 队列）是否更顺滑，需真机小样验证。上机前先空载、限位、手触急停。

## 待办
- 决定是否把 `last_task.txt` / `gui_window.txt` / `robot_config.json` / `logs/` 改成按可执行文件目录解析（当前按 CWD，从别的目录启动会失效；见 bugs.md）。
- 接入 `calib.json` 或设计带验证状态的 measured safe area，并统一作为运动硬拦截边界。
- 设计急停锁存/人工复位语义；确认控制器状态、报警、限位、当前位置寄存器。
- 解决 ACK 丢失后的重放风险，确认命令幂等/序号/查询语义；批量 7 点协议需真机验证。
- Hanzi/Polyline JSON 字段类型/有限性/文件大小/点数限制与容量评估。
- 新增 `robot_cli.exe` 非交互命令入口，复用同一服务层与审计链路（当前 `main.cpp` 仍是菜单式交互）。
- 上机验证 Z 深度、方向、速度、批量、蘸墨和急停；当前 `Z_DOWN_HEAVY=-388` 仍超出已确认 -385 的实测点。
- `logs/` 与 `last_task.txt` 会持续增长，后续可加清理/轮转策略。

### 切页卡死修复（2026-09-18，`gui_service.cpp`）
- 现象：切到“设备连接”页后界面立刻无响应，CPU 0%（阻塞而非忙等），进程 `taskkill /F` 都杀不掉。
- 定位：gdb attach 被系统拒绝，改用临时文件探针（各阶段写 `_trace.log`）逐段收敛，确认卡在 `gs::list_serial_ports()`；再在其内部打点，定位到 `CreateFileW("\\.\COM5")` 永久阻塞（COM4 也阻塞 5.2s）。触发链：`QueryDosDeviceW(nullptr, names, 4096)` 因本机 DOS 设备名列表长 47349 字符而返回 0/err=122 → 走兜底分支 → 逐个打开 COM1..32 探测 → 本机 6 个端口全是蓝牙虚拟串口（BthModem），打开即阻塞。
- 修复：`list_serial_ports()` 改为只查询不打开——`QueryDosDeviceW` 按 `ERROR_INSUFFICIENT_BUFFER` 扩容重试 + 注册表 `SERIALCOMM` 补充，删除 `CreateFileW` 探测分支；另加纯数字校验过滤 “COMDB” 之类误解析。
- 核验：正常模式与 `--dryrun` 各连续切页 9 次全部 RESPONSIVE；“刷新串口”按钮即时返回并正确列出 COM3/4/5/6/8/9；`--dryrun` 下书写链 `task_preflight(ok) → task_start → 7×send_point + 20×send_batch7 → task_abort → task_end(canceled/estop)` 全部落盘且全程响应；控制台 `Robot.exe --dryrun` 菜单 4 正常退出码 0。
- 未修（已记录）：点“连接设备”仍在 UI 线程打开串口（选到蓝牙虚拟口会同样卡死），需改成工作线程 + 打开期间不持 `g_mu`，见 bugs.md。

## 阶段变更记录
- 2026-09-21（晚·真机实测阶段启动）：**GUI 整任务 DRYRUN 验收通过并转入真机实测**。①读出用户 Dry Run 那次的完整审计链（`audit_20260921_210129.jsonl`，21:01:29→21:03:16，524 事件）：`task_preflight ok(2页×5字) → task_start → page_turn(->第2页) → page_turn_ble ok(detail="DONE 30 3000", elapsed 3308ms) → page_turn_result ok → task_end ok`，并确认 **Dry Run 下翻页走的是真蓝牙**（`page_turn_ble` 事件带真实 `DONE` 回包），即 Dry Run 只模拟机械臂、管不到蓝牙翻页。②澄清 `progress.chars_total=5` 是**每页**计数（`task::page_begin` 语义），非漏写第 2 页。③核实真机现场：`USB-SERIAL CH340 (COM4)` 已插（此前无任何串口），GUI 实例 PID 12332（20:39 启动）；**`g_dryRun` 不落盘**，默认 false 仅 `--dryrun` 置位（`robot_common.cpp:45`），故重启 GUI 即真实模式。④核实书写 Z 实际取值：config `writing_plane_valid=true` + `writing_plane_z=-365` → `hanzi.cpp:92` 落笔全用 -365，抬笔 `Z_UP=-325`，**均落在文档实测安全区间 -320~-385 内**；但 config `z_limits=[-320,-410]` 与 `z_layers` 末位 `-388` **超出实测底限 -385**（仅在「书写平面」失效或开「顿笔」时才会触发，当前两者都关，故安全）。⑤新增 `真机实测步骤.md`（6 阶段递进 + 红线 3 条 + 审计事件对照表 + BLE 状态码处置表）；修正 `使用说明.md` 两处**已过时**表述（§5.3 与 §9-7 仍写"翻页为模拟等待"，已改为真实 BLE 闭环），并在 §9「上机前必读」补第 9、10 条（Dry Run 管不到蓝牙翻页；BLE 模块一次只服务一个中心设备）。**本轮代码零改动。**
- 2026-09-16：完成比赛/协议/设计方案核对及第一轮源码修复。
- 2026-09-17：完成 Terra 第二轮审查；修复错误传播、预定位估时、输入和调试几何校验；建立 CodeGraph 和 GUI 状态边界。
- 2026-09-17：完成编译、dryrun、CodeGraph 同步和差异检查。
- 2026-09-17：根据用户确认收敛为前三页静态 HTML 原型，补齐设计文档并完成差异检查。
- 2026-09-17：完成 Win32 GUI 实现（gui_service/gui_win32）、修复 preflight 递归锁死锁、完成 GUI 操作核验与控制台帧级回归，新增 build.bat。
- 2026-09-18：修复主页设备信息网格下标写反（“UI 错位”主因）及一批文字截断／图形压字／参数框空白缺陷，三页逐页重抓截图核验通过；定性“请先输入文本。”为 CWD 相对路径问题。
- 2026-09-18：修复“切到设备连接页卡死”——串口枚举误用 `CreateFileW` 打开蓝牙虚拟串口导致 UI 线程内核阻塞；改为纯查询枚举。同期未修：点“连接设备”仍在 UI 线程打开端口（见 bugs.md）。
- 2026-09-18：新增 `使用说明.md`（构建/运行/GUI 各页/控制台菜单/配置与审计/上机安全/已知限制），README 加入指引；提交并推送分支 `feat/gui-win32-assistant`。
- 2026-09-18：修复打开 GUI 后整体/文字闪烁——`OnPaint` 改内存位图双缓冲 + 一次 `BitBlt` 上屏，主窗口加 `WS_CLIPCHILDREN`（仅改 `gui_win32.cpp`）；`build.bat` 重编通过，`--dryrun` 启动 `Responding=True` 并优雅退出。
- 2026-09-18：修复“连接设备”在 UI 线程打开串口导致蓝牙口卡死（与切页卡死同源）——`gs::connect` 改异步 `connect_async`，open 移到 detach 工作线程且期间不持 `g_mu`，结果经 `snapshot()["connect"]` 由 `WM_TIMER` 回报，`g_connect_pending` 防重复点击（改 `gui_service.{h,cpp}`+`gui_win32.cpp`）；`build.bat` 重编通过，`--dryrun` 启动 `Responding=True` 并优雅退出。蓝牙口真实阻塞路径未做自动化实测（跨进程驱动风险高），需上机手动验证。

- 2026-09-18：书写中"停止任务"不跟手的 motion 侧加固——`transmitTrajectoryWithSplit` 原来只在外层段边界查 `g_estop`，一笔之内/描边整段批量循环不查→段中途停不下。新增 `estopLiftAt` 抬笔复位小函数，并在书法逐点 for、描边批量 while 每块、回退逐点 each 三处轮询 `g_estop`，命中即抬笔返回（`motion.cpp`）。注：急停与"停止任务"(`abort_task`)都置 `g_estop`，本改动让二者都能逐点打断；"停止按钮点了没反应 vs 急停有效"的 GUI 侧差异仍待上机确认（见 bugs.md）。整合编译 RC=0。
- 2026-09-18：本机缺字库致"找不到书.json"——非回归（hanzi.cpp/HANZI_BASE_DIR 未改），派子代理下载 HanziWriter 常用字库到 `D:/objects/hanzi-writer-data/data/`。
- 2026-09-18：节拍参数运行期可配置——新增全局 `g_z_settle_ms/g_stroke_begin_ms/g_stroke_end_ms/g_cold_start_min_ms/g_min_point_interval_ms`（默认取原 *_BASE 常量），`get_*`/`estimateMoveMs`/`wait_after_send` 改读全局，`gs::cfg_save/cfg_load` 增对应 5 键（`z_settle_ms` 等，0~3000ms 限幅），改 `robot_config.json` 重启即生效、无需重编（改 `robot_common.{h,cpp}`+`motion.cpp`+`gui_service.cpp`）。dryrun 归因（"书"，关描边）：prev 中位 186ms/最短 62ms(60ms 地板钉死)→ new(A/B) 153ms/31ms → LOW(压dwell) 109ms/26%<50ms；结论：顿挫大头是每笔 Z 沉降+起收笔 dwell+冷启动（省~20%），60ms 地板是次因（A 省~9%），RDP 对稀疏简单字不减点。LOW 版为测量极端值、勿上机（墨淡无锋）。

- 2026-09-19（本窗口）：实现「书写任务」页**字号/布局/方向 + 实时预览**（Phase1 字号/网格 + Phase2 竖排右起），自动/手动双模式默认自动零回归，参数经 `robot_config.json` 持久化、控制台共用。改动文件：`robot_common.{h,cpp}`（6 全局 + TextPlan.err/row_spacing/dir + LayoutErr 枚举）、`hanzi.{h,cpp}`（`plan_manual_layout` + 方向感知 `grid_extents`/`fill_offsets`，`prepare_layout_only` 分流）、`gui_service.{h,cpp}`（5 setter + `set_write_dir` + `layout_preview` + cfg/snapshot/preflight 扩展）、`gui_win32.cpp`（书写页 `WLGeo` 排版控件 + `g_chkManual`/`g_chkVert` + `EN_CHANGE` 实时刷新 + 计划字块叠画进 `DrawTrailPanel` + 开始书写可行性闸门）。**收尾修复**：叠画显示条件加 `gs::trail::commandedSize()==0`——原 `!task_active()` 会让任务跑完后计划字块重新显示、与保留的真实轨迹重叠冲突，现只在首次书写前的规划阶段叠画。验证：`g++` 链接 RC=0 无新 warning；排版/方向单元自测 Phase1 20/20 + Phase2 19/19 全 PASS；`RobotGUI --dryrun` 启动 `Responding=True`；正式 `Robot.exe`/`RobotGUI.exe` 已覆盖。未验证：书写页视觉叠画与方向端到端仍待关旧实例后实操核验（用户已反馈一次重叠问题并据此修复）。注：`robot_config.json` 的 M 是用户用 Phase1 GUI 测试手动模式写入的运行态（mode1/cols1/top0.9，无 write_dir 键），非源码改动。

- 2026-09-19：书写页新增**实时轨迹面板**（`gui_trail.{h,cpp}` 新文件 + `serial_port::readPose` 0x03 读坐标 + `gui_service` 钩子接线 + `gui_win32` 面板）。数据源 hybrid：任务期把已下发点（软件位姿）记为稠密轨迹（紫落笔/灰抬笔），真机每字 `readPose` 采样实测点（绿点）叠加，用于暴露方向/丢步；DRYRUN 无实测点。轨迹仅在 `g_task_active` 时记录，快捷操作不污染。改动文件：`serial_port.{h,cpp}`、`gui_service.{h,cpp}`、`gui_trail.{h,cpp}`(新)、`gui_win32.cpp`、`build.bat`(COMMON 加 gui_trail.cpp)。
- 2026-09-19：边界标定从"纸张长宽"改为**四角标定**（用户依次输入四角设备坐标，每角"预览/保存"）。`gs::trail` 存 `Corner[4]`，持久化 `robot_config.json` 的 `corners`；`gs::preview_corner`(抬笔移到该角，真机移动/DRYRUN 打帧)、`save_corner`(校验+审计+存)、`clear_corners`。修 4 问题：①角标签截断→`lblW 30→46`；②预览改用固定书写平面 Z（`g_writing_plane_valid?g_writing_plane_z:Z_UP`，非 Z_UP）；③`Y=-100` 到不了实为 Z_UP 顶部 XY 收窄，随②解决；④四角连线按绕质心极角排序，消除"八字"交叉成规整矩形。构建 RC=0；**已 dryrun 截图核验**：角标签完整、角值回填、橙框为矩形、提示不压字。**未验证(须上机)**：预览真实移动与可达边界、readPose 实测点语义(0x0008 读回是实际位置还是最后写值)。

- 2026-09-19：可达 XY 边界**可配置 + 全局拦截**。发现控制器把 Y 实际夹在约 ±85（比手册 ±180 紧，`y=-100` 发得出但臂不动=静默夹取）。做法：`g_devLimit` 从 `robot_config.json` `"dev_limit"`[xmin,xmax,ymin,ymax] 读写（`cfg_load` 最先应用、绝对 ±180 内校验），当前设 **X±162/Y±85**；`sendPoint`/`preview_corner`/`save_corner`/回中心/测试点据此统一 fail-fast。另修：四角预览/保存结果原走 `SetResult`（只画主页）在书写页不可见 → 改在轨迹面板标题行右侧显示。构建 RC=0；**dryrun 已验**：`y=-100` 预览被拦（本会话无下发/无新审计文件）、`y=-80` 放行且提示"已移到(160,-80)"可见。**注意**：收紧后超出可达框的书写点会被拒（排版须落在可达框内；`g_safeArea` 默认 Y 到 110 已超出，待后续对齐）。真机预览可达性仍待上机。

- 2026-09-19（本窗口）：GUI 书写页排版**重构为自由拖拽 + 字体朝向**（用户新需求，取代本窗口早前 Phase1/Phase2 网格方案）。①左栏只留「字号」+ 新增「书写方向(字体朝向)」下拉，删除 每行字数/上区占比/行距/手动/应用/恢复自动/竖排 全部网格控件；网格降为初始摆位。②画布每个字块可在**固定视野**（`gs::view_bounds`=四角框，未标定回退可达 X±162/Y±85）内**逐字拖拽**，磁吸对齐、夹取在框内、按文本绑定持久化到 `robot_config.json`（新键 `free_text/free_cells/free_char_size/glyph_orient`）。③固定视野使**预览与书写共用、不再随数据自动缩放**（修“写第二字时画布缩小”）。④字体朝向 4 档（0=沿Y向下/1=沿Y向上180°/2=沿X向上90°CW/3=沿X向下90°CCW）**仅旋转整字不改位置**，书写在 `run_task_thread::rotate_local` 绕字心整数旋转，预览 `DrawRotatedGlyph`+字顶标记。⑤`IDC_BTN_WRITE`/`preflight` 走自由布局可行性闸门；新增「重置画布」按钮(`reset_canvas` 清轨迹缓冲让可编辑叠画重现)。服务层新增 `view_bounds/set_free_char_size/set_free_cell/set_glyph_orient/glyph_orient/reset_canvas/prepare_free_layout` + `layout_preview` 改按文本绑定；`DrawTrailPanel` 缓存 `TrailView g_tv` 供 `HitCell/SnapCell` 复用同一 map。改动文件**仅** `gui_service.{h,cpp}`+`gui_win32.cpp`（未碰 hanzi/gui_trail/serial_port/robot_common，避让他窗口）。验证：g++ 链接 RC=0 无新 warning（仅历史 `g_auto_draw_used`/`#pragma comment` 警告）；纯数学单测 29/29 PASS；`RobotGUI --dryrun` RESPONDING=True；**Computer Use 实操核验**：导航到书写页控件齐全、拖拽“一”字到右上并成功落库(config cell0 x −146→+17)、选“沿X轴向上(顺时针)”后 4 字整字旋转而位置不变、固定视野不缩放。已把新 build 覆盖到 `RobotGUI.exe`/`Robot.exe`（exe 为 gitignore 产物），并清掉测试期间写进 config 的 free_* 运行态。未验证：真机书写落点与预览一致性；朝向 CW/CCW 语义若与预期相反改 `rotate_local`/`esc[4]` 一处。
  - 2026-09-19（同窗口·用户实机反馈后追加修复）：①删掉预览“字顶朝向小标记”（用户嫌像每字多一竖）；②修“实写偏下”——`generateSingleCharTrajectory` 以 bbox 左下角对齐使扁字(一/二/三)贴字格底，`run_task_thread` 加 bbox 居中偏移 `dcx/dcy` 把字形居中到 S×S 字格再旋转，与居中预览框一致（填满格字无影响）；③预览 `DrawRotatedGlyph` 由字面 escapement 改为 GM_ADVANCED 绕框心世界变换旋转以真正居中（escapement 在 90/270° 偏心）。Computer Use DRYRUN 写“一一”实测两笔已落在视野竖向中心、与预览框对齐。改动仅 `gui_service.cpp`(②)+`gui_win32.cpp`(①③)。
  - 2026-09-19（同窗口·字号交互两轮返修）：(a)修“改字号没反应/一删就补 60”回归——上一版在 EN_CHANGE 每次按键都把输入框夹回 60，导致打“80”首字符“8”就被改回“60”再也打不进；改为 **EN_CHANGE 只在值≥60 时实时套用且绝不回写文本、EN_KILLFOCUS 才夹到[60,500]并回写+提示**（`ApplyCharSizeField`/`CommitCharSizeField`）。(b)按用户要求**允许字块重叠**——字号大到网格排不下时不再隐藏字块也不再判“不可行”，`compute_initial_grid` 始终生成 n 块(夹取/重叠)，`grid_fits` 只判“单字塞得进视野”(S≤视野宽高)，叠画显示与拖拽门控由 `g_prevValid` 改为 `!g_prevCells.empty()`。Computer Use 实测 字号 80+5 字：字块可见可拖、不再“不可行”。改动 `gui_service.cpp`+`gui_win32.cpp`。所有轮次 g++ 链接 RC=0；`RobotGUI.exe`/`Robot.exe` 已覆盖为最新（gitignore 产物）；测试写进 `robot_config.json` 的 free_* 运行态已清除。

- 2026-09-20（本窗口）：书写任务**分页 + 翻页**功能落地（用户新需求，见「当前任务」首条详述）。一页写完→发翻页信号（当前模拟等待）→等翻页→抬笔清轨迹→写下一页；每页独立可拖拽布局、超容量截断只写前 N 字、翻页等待期停止/急停立即中止（用户四项决策）。改动仅 `gui_service.{h,cpp}`+`gui_win32.cpp`（未碰 hanzi/motion/serial_port/gui_trail）；config 新增 page_* 键并兼容旧 free_* 迁移。验证：g++ 链接 RC=0 无新 warning；`verify_pages` 单测 78/78 PASS（含 7.4 等待期 abort 立即中止）；dryrun 审计链 `page_turn→page_turn_result=ok`；Computer Use 实操确认切页/每页隔离拖拽/翻页后轨迹清空重播所见即所得。临时测试文件移出项目目录、config 运行态 git checkout 还原。待真机：走纸精度=版面高？软件位姿不变？真实蓝牙替换模拟（`Run()` 锚点 `set_page_turn_handler`）？详见 context.md「分页 + 翻页约定（2026-09-20）」。
- 2026-09-20（同窗口续）：**页尾自动复位抬笔 `UP-PAGEEND`**——每页（含末页）逐字循环结束即在 `g_last_pose` 原位显式重发 Z_UP 抬笔+300ms 短驻，置于页尾补蘸之前；与翻页前原 `UP-PAGETURN` 合并去重（翻页块只发信号不再抬笔，用户定案"原位抬笔+短驻/合并去重"）。仅改 `gui_service.cpp::run_task_thread`（+12/-6），无新 config 键。重编双 exe RC=0；`verify_pageend_lift` DRYRUN 端到端 11/11 PASS（页尾成对同位姿 pen-up 事件、末页亦有、无 PAGETURN 残留）。待真机：同坐标空移控制器行为、300ms 手感。详见「当前任务」首条与 context.md。
- 2026-09-22（去连珠·代码落地）：与另一 agent 16 轮交叉讨论 + 真机字节扫描/抬 z_settle 复测收敛。**连珠三成分**：粗珠＝命令点驻留（档3 字节治）、截断＝Z 下降预算不足（抬 `z_settle` 治）、~1mm 细纹＝指令内空间型/纸纹（另案）。**代码落地**：`robot_common.h` `SPEED_MAX 6→10`（byte6~9 可达、帧层早已夹 0~9、不碰计时）；`gui_service.{h,cpp}`+`gui_win32.cpp` 书写页新增"Z沉降ms"实时框（`set_z_settle_ms` 直设不调 `sync_z_settle`，改 C 框仍会打回）。双 exe RC=0、dryrun 截图核验新框在位。**test5 有效**（曾被我误判作废＝审计 off-by-one 二次踩坑）：z_settle 阈值 ∈(82,150)、150 已完整。**待办**：用户在 200/250 定工作点 → 写一行「欲穷千里目」终检（只看结构+5 处截断位）；本次提交＝4 代码文件+agent_memory，`robot_config.json` 运行态不提交。详见 context.md §9d/§9e。

- 2026-09-20（深夜·本窗口）：蓝牙翻页闭环验收**把失败根因定位到硬件**。关掉用户的 `motor_ble_gui.py`(PID 12428) 后复验，`OpenAsync` 由 `SharingViolation(4)` 变为 `Success(1)`（证明该工具确实是之前的占用者），但**仍失败**：特征枚举 `GattCommunicationStatus=1 Unreachable`、每次耗时 7.75s；`bleak` 主动扫描两次都扫不到模块；新诊断 `[link=0 ...]` 说明 PC 侧**根本没有链路**（排除幽灵链路）。→ **结论：模块在空口上不可达 = 没上电（或正被手机占着）；非软件问题**。同时修掉代码诊断盲点：`ble_motor.cpp` 原先把 `Unreachable` 笼统报成“找不到 FFE1 特征（模块未就绪或服务未广播）”，现按状态码分诊并附 `[link= open= comm= n=]`；新建 `tmp/ble_poc/scan.py`（bleak 扫描）作为“硬件在不在”的第一个检查项。`ble_selftest.exe` 重新编译 **RC=0**（逻辑未改，仅加诊断）。改动文件：`ble_motor.cpp`（仅诊断）。**未改**任何 GPIO/写字节/时序/协议代码。
- 2026-09-21：**蓝牙翻页真机闭环验收通过**。用户给模块上电后，`scan.py` 立即扫到目标（`rssi=-65`），`ble_selftest.exe` 一次跑通：建链 2.30s → `RUN30,3000` → 3.33s 收到 `DONE 30 3000`（EXIT=0，电机真转）。**证实 9-20 的定因正确（模块没上电），且从 9-20 到 9-21 软件侧一行未改即通**。对照数据：可达时建链 2.3s，不可达时要空等 25s 才报错 —— 可用于快速判断模块在不在。代码侧本轮无改动（只跑验收 + 更新记录）。
- 2026-09-23（跨工程）：**下位机档位总数 50 → 20**（另一侧工程 `桌面\电机代码` 把执行器由 130 电机换成 TT 减速电机）。本工程同步三处，否则**自动翻页会静默失效**：
  ① `gui_service.h` 新增 `constexpr int PAGE_TURN_GEAR_MAX = 20;`，`set_page_turn_gear()` 校验与 `cfg_load()` 迁移校验改用该常量（原为散落的 `gear > 50` / `v <= 50`，现只有一处定义）；
  ② 默认翻页档位 `g_page_turn_gear` 与输入框初值 30 → **15**（30 > 20 会被板子越界拒掉）；
  ③ `gui_win32.cpp` 两处夹取范围 `0, 50` → `0, PAGE_TURN_GEAR_MAX`，界面文案「档位(0~50)：」改为按常量动态拼出；
  ④ `robot_config.json` 的 `page_turn_gear` 30 → 15。
  **起因**：板子固件有 `if (GearVal > MOTOR_GEAR_MAX) → 回 ERR` 的越界检查，而本工程原校验是 `0~50` **不会当场拦**，表现只是"写到页末翻页没反应"，极难查。
  验证：`g++` 全量重编 `exit=0`（仅剩 1 个原有的 unused-variable 警告）；用户原 `RobotGUI.exe` 未被覆盖。

## 验证说明
- 已验证：GUI 预检/开始书写/停止三条操作路径的审计事件落盘正确；控制台 DRYRUN 帧序列与拆分前基线逐帧一致；`build.bat` 一次产出两个 exe 并冒烟通过。
- 已验证（2026-09-18）：三页截图三张互不相同且逐页目视检查通过；GUI 预检再次实测 `task_preflight result=ok`。本轮只改 `gui_win32.cpp`（`git status` 仅此一个文件），未触及控制台与共享服务层，故未重跑帧级回归。
- 已验证：`RobotGUI.exe --dryrun` 冒烟（控制台 `Robot.exe --dryrun` 菜单 4 正常退出码 0）。
- 已知环境限制：`Robot.exe` 交互式写字需约 8 分钟（单字 3749 点 DRYRUN），本轮以 5 分钟窗口截取前段对比；控制台 stdin 重定向在 EOF 后不退出，需显式输入菜单 4。
- 已知环境限制：本机 `CopyFromScreen` 截图会返回冻结帧，核验必须“强制重绘 + PrintWindow + md5 比对”三步并用。
- 未验证：真实硬件（无设备接入），所有运动均为 DRYRUN 模拟帧；GUI 未在 100% 缩放与高 DPI 之外的分辨率下核验布局；未从非项目目录启动 GUI 的路径解析问题尚未修（见 bugs.md）。

- 2026-09-21（晚·本窗口）：**修正 BLE「判在不在」的判据 + CLI 复测通过**。`ble_selftest.exe` 通过：建链 **0.56 s** → `RUN30,3000` → 3.31 s 收到 `DONE 30 3000`，EXIT=0（电机真转）。但同环境 `scan.py` 13s×2 **扫不到**、bleak 按地址连报 `DeviceNotFound(30s)`。用系统 Python 的 `winrt-*` 直读 WinRT：`FromBluetoothAddressAsync` **0.06 s** 拿到设备、`name='HC-05'`、**`connection_status = Connected (1)`**、`GetGattServicesWithCacheModeAsync(Uncached)` 0.36 s 返回 3 个服务（1800/1801/FFE0）→ **模块与 PC 已配对（`BTHPORT\...\Devices` 下有 `21f6473ad889` 记录），Windows 常驻保持链路，模块因此停止广播**。→ **新判据：扫描阳性可信、阴性不可信；决定性判据 = 「能否拿到设备对象 / 建链耗时」（0.1~0.6s=链路已在 / 2~3s=刚在广播 / 空等 25s=不在）。** 新增工具 `tmp/ble_poc/winrt_probe.py`（只读状态、不发运动指令，0.5s 出结果）；同步修正 workspace `电脑端蓝牙控制工具\蓝牙翻页验收清单.md` 与技能 `hc05-classic-bt-troubleshoot`（新增 1.8 节）。另发现并记录：**`g_dryRun` 管不到翻页**（翻页走 BLE 不是串口）→ Dry Run + 勾蓝牙翻页 = 臂不动、电机真转。**本轮代码零改动**（只新增诊断脚本 + 文档）。下一步：GUI 整任务验收（Dry Run 先跑）+ 手感标定。
- 2026-09-21（晚·本窗口）：**修 UI「按钮跑到别的页上」+ 跨进程探针验证通过**。
  - 根因：`MakeBtn` 的 `g_btns[64]`（下标 = ID−1000）在 ID ≥ 1064 时越界登记失败，`IDC_BTN_PAGE_NEXT`(1064)、`IDC_BTN_BLECONN`(1067) 永远销毁不掉 → 切页残留、逐次累积。
  - 修法：`DestroyPageControls(hwnd)` 改为枚举主窗口直接子窗口统一销毁（不再按 ID 记账）；并修 `DrawBtn` 同源越界判断导致的 `连接蓝牙` 配色错误；按实测字宽修该行尺寸（`连接蓝牙` 80px 原按钮 70px 被截成「连接…」）。
  - 验证：新写 `tmp/ble_poc/probe_children.exe`（跨进程枚举直接子控件 + 模拟点导航）做 A/B。旧实例主页 49→51 个控件且必现两个残留按钮；新实例各页恒为 19/5/35/3，无残留。
  - 新增工具：`tmp/ble_poc/probe_children.cpp/.exe`、`tmp/ble_poc/measure_text.cpp`（按 GDI 字体实测文案像素宽，避免拍脑袋定控件宽度）。
  - **未完成**：`RobotGUI.exe` 被运行中的实例（pid 12332）占用无法覆盖，修复版暂存为 `RobotGUI_fix.exe`；关闭旧 GUI 后需替换并改名。
