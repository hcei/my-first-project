# 项目上下文

> 本文档维护项目级上下文信息，供 agent 在开始非简单任务前读取。
> 只保留当前有效信息，不写成无限追加日志。
> 文件过长时先压缩为摘要，再归档旧内容到 agent_memory/archive/ 。

## 项目概述
基于自行组装的三自由度 Delta 机械臂，通过 Modbus RTU 控制刚性 3D 打印笔架上的动物毛毛笔完成现场书法和配套中国画。

## 技术栈
Windows / C++17 / Modbus RTU over RS485 / nlohmann::json。
构建：MSYS2 MinGW-w64 g++ 静态链接（`build.bat` 一键产出两个 exe）；`Robot.vcxproj` 保留 MSVC 路径但本机未安装 MSVC 工具链，实际以 MinGW 为准。

## 目录结构
- 控制台程序：`main.cpp` + `robot_common` / `serial_port` / `motion` / `hanzi` / `polyline`
- GUI 程序：`gui_win32.cpp`（Win32 入口，不进控制台工程）+ `gui_service.cpp/h`（GUI 与控制台共用的服务层：状态快照、配置、串口、任务线程、审计）
- 交付物：`Robot.exe`（控制台）、`RobotGUI.exe`（GUI）
- 文档：`README.md`（简介）、`使用说明.md`（构建/运行/上机注意事项，面向使用者）、`GUI_DESIGN_SPEC.md` + `gui_design_mockup.html`（GUI 设计依据）
- 配置与数据：`robot_config.json`、`calib.json`（未接入）、`shanshui_gen.hpp`、`themes/`、`D:/objects/hanzi-writer-data`（字库，由 `HANZI_BASE_DIR` 指定）
- 运行时产物：`logs/Robot_*.log`（仅控制台写）、`logs/audit_*.jsonl`、`gui_window.txt`、`last_task.txt`（后三者已 gitignore）

## 仓库与分支约定
- `origin` = https://github.com/hcei/my-first-project.git （本仓库），`upstream` = https://github.com/GuhuaiDawn/my-first-project.git （原作者）。
- 功能开发走新分支再推送，不直接推 `main`；本次 GUI 工作分支：`feat/gui-win32-assistant`。
- `gh` CLI 已登录（账号 hcei，https 协议）。
- **推送需要走本机代理**：`git push` 直连 github.com 会 `Recv failure: Connection was reset`；本机浏览器代理为 `127.0.0.1:7897`（WinINET 里 ProxyEnable=1），但 git/curl 不会自动使用它，WinHTTP 显示“直接访问”。可用一次性参数推送，不必写进 git 配置：
  `git -c http.proxy=http://127.0.0.1:7897 push -u origin <branch>`

## 关键约定
优先复用现有 C++ 控制流程；不得把协议假设当成已上机验证事实；涉及硬件运动必须先空载、限位和急停验证。原 CR-3040/G-code 方案仅作为需求和流程参考，不直接继承其机械、电控或夹具设计。

## 书写节拍与真机告警（2026-09-18 沉淀）
- 节拍可运行期配置：`robot_config.json` 新增 5 键 `z_settle_ms / stroke_begin_ms / stroke_end_ms / cold_start_min_ms / min_point_interval_ms`（默认 120/70/90/150/12），由 `gs::cfg_load` 覆盖全局 `g_*`，改后重启即生效、无需重编；真机扫参就调这些。
- dryrun 归因：顿挫大头是每笔 Z 沉降+起收笔 dwell+冷启动（压这些省 ~20%），60ms 走停地板是次因（方案A 省 ~9%）；RDP 只对密采样/长直笔画减点，简单稀疏字不减点。
- 顿笔独立开关 `g_enableDunbi`（默认 true=保持现状，仿 `g_enableDip`）：持久化键 `enable_dunbi`、snapshot `cfg.enable_dunbi`、GUI 主页复选框（`IDC_CHECK_DUNBI`，idx5，画在蘸墨下方）、控制台菜单 21。关闭时：①`get_Z_SETTLE_MS/get_STROKE_BEGIN/END_DWELL_MS` 一律返回 0（含高质模式），首落笔"重压加固"块跳过（`isFirstDownThisCall` 增加 `&& g_enableDunbi`）→ 落笔过渡间隔从 ~202ms 降到 min_interval(12ms)；②`hanzi.cpp::zFor` 的点画 dot 由 `Z_DOWN_HEAVY` 改 `Z_DOWN_NORMAL`，与书写平面无关。**不动** medians 骨架几何、不删分层下刀(UP→MID→PRE→DOWN)、不删拐角 dwell。单元验证 verify_dunbi.exe 全 PASS。真机待验证：关闭后是否更顺滑、小字点画是否不再糊成墨点。
- ✅ **速度编码反向已真机证实（2026-09-20 第 2 轮交叉讨论）**：手册 `0x0008` 的 V 高字节 0~9 中 **00 最快**，而 `serial_port.cpp` 发 `level-1`（⇒ 档1→byte0=设备最快、档位越大越慢），但 `speedLevelToXYmmPerSec` 假设 level 越大越快（quality 表 28/42/56/80/100/128）⇒ **模型与设备速度方向相反**。真机肉眼也确认"速度1 最快、速度3 最慢"。后果：**高档位＝设备慢+模型按快给时间 ⇒ 欠计时 ⇒ 拖影/截断；低档位＝设备快+模型按慢给时间 ⇒ 过计时 ⇒ 笔到点后干等 ⇒ 连连看/抖**。**两处打包点**：`sendPoint` 与 `packPoint5` 都要抽 helper 一起改，否则漏改一处。蘸墨 `do_dip_and_groom` 的 `SLOW=1/FAST=档`（"上慢下快"）同样反向。**〔2026-09-22 更新：本条前提已过时——`speedLevelToXYmmPerSec` 现为死代码、计时改按 `isPenDown` 解耦 ⇒ "模型 vs 设备方向相反"不再成立；"速度3 最慢"系秒表噪声，作废。速度对连珠的影响以"去连珠·最终结论"节为准：**档3＝工作点（配 z_settle≥150）**，勿据此回退档1。〕**
  - **修法三选项**：**C（推荐）**＝保留字节映射，把 `speedLevelToXYmmPerSec` 换成"实测每字节速度"查表值（不动已有实验记录的档位语义、天然容纳非单调）；A＝改字节映射（`byte=SPEED_MAX-level`，语义全正过来但"速度1"含义翻转）；B＝只反转模型表方向（代码意图/蘸墨继续反着，需额外修）。**选哪个都要先测出每档真实速度**。
  - **标定法（零成本，不依赖 readPose）**：用「回中心」→「四角预览」这类**只发一条 `0x0008`** 的操作做 320mm 单指令移动，手机秒表从点击到肉眼看到停下，**每档重复 3~5 次取最小值**；或同一操作节奏下比较不同档总时长（点击开销对消）。**注意：320mm/0.1~0.2s 这个流传的数据换算成 2.1m/s，几乎肯定是错的，需重测。**
- ⚠️ 描边/作画真机不安全：`auto_draw` 走 `0x0064` 批量，但手册明确 `0x0064` 是**固定抓放宏**（抓上/抓/抓上/放上/放/放上/等待），非任意轨迹；真机联调期保持 `auto_draw=false`，作画须先把描边改回逐点 `0x0008`。

## GUI 排版（自由拖拽 + 字体朝向）约定（2026-09-19 重构，取代旧网格方案，勿再走网格 UI）
- 书写页左栏**只保留「字号」+「书写方向(字体朝向)」两个数值/选择控件**（旧的 每行字数/上区占比/行距/手动/应用/恢复自动/竖排 网格 UI 已全部移除）。网格仅作**初始摆位**：输入文字后按字号在固定视野内自动居中排成网格，用户随后在画布上**逐字拖拽**定最终布局（所见即所得，写啥即摆啥）。
- **固定视野**（预览与书写共用、不再按数据自动缩放）：`gs::view_bounds()` = 四角标定外接框（4 角齐全时），否则回退可达框 **X±162 / Y±85**（真机实测硬极限，`gui_service.cpp` 常量 `REACH_X/REACH_Y`；注意 `g_devLimit` 仍是 ±180 校验用，二者不同、勿混）。拖拽夹取、磁吸、绘制 map 全以该框为准。
- **字体朝向** `g_glyph_orient`(0..3，随 config 持久化)：0=沿Y向下(0°正常)/1=沿Y向上(180°倒置)/2=沿X向上(90°CW)/3=沿X向下(90°CCW)。**仅旋转整字、不改变字块位置**。书写路径在 `run_task_thread` 用 `rotate_local` 绕字心 (S/2,S/2) 做整数旋转局部轨迹；预览用 `DrawRotatedGlyph`(GDI escapement)+字顶小标记。GUI 下拉 `IDC_COMBO_ORIENT`（CBN_SELCHANGE→`set_glyph_orient`）。若某向 CW/CCW 与预期相反，改 `rotate_local`/`esc[4]` 一处即可。
- **字形居中（所见即所得关键）**：`generateSingleCharTrajectory` 按最长边缩放到 S 且以 bbox 左下角对齐 (0,0)，扁字（一/二/三）会贴到字格底部→实写偏下、与居中预览框不符。修：`run_task_thread` 先求 local 轨迹 bbox，加偏移 `dcx=S/2-(minlx+maxlx)/2, dcy=S/2-(minly+maxly)/2` 把字形居中到 [0,S]² 再 `rotate_local`（填满格的字 dcx/dcy≈0 无影响）。预览 `DrawRotatedGlyph` 也改为“正立字形居中于框心 + `SetGraphicsMode(GM_ADVANCED)` 绕框心世界变换旋转”，勿用字面 escapement（90/270°会偏心）；MinGW 无 `ResetWorldTransform`，还原用 `SetWorldTransform` 存旧 XFORM。字顶朝向小标记已删（旋转字形本身可辨朝向，标记像多一竖被用户否掉）。
- 服务层自由布局状态（`gui_service.cpp` 匿名 ns，**不再走 hanzi 网格**）：`g_free_text`(宽字符，绑定键)/`g_free_cells`(每字左下角世界坐标)/`g_free_char_size`/`g_glyph_orient`。API：`view_bounds`、`set_free_char_size`(改字号→`compute_initial_grid` 重排复位)、`set_free_cell`(拖拽落点，夹取+cfg_save)、`set_glyph_orient`、`glyph_orient`、`reset_canvas`(清轨迹缓冲让可编辑叠画重现)。`layout_preview(text)`：**按文本绑定**——文本变则重排初始网格、否则复用已存坐标；返回 cells(世界坐标)+view+orient+valid(字号塞得进视野即 valid，放不下**不自动缩放**)。`prepare_free_layout` 供 `run_task_thread` 用自由坐标直出 TextPlan(text_area 原点 0、offsets=绝对坐标)。`preflight` 同步改走自由布局。**字号健壮性(2026-09-19)**：`compute_initial_grid` 改为**始终生成 n 个字块**(放不下则夹取/允许重叠，不再返回空→避免越界)。**允许字块互相重叠**：`grid_fits(n,S)` 只判“单字塞得进视野”(S≤视野宽/高)，字多排不下时不再判不可行而是重叠摆放、交给用户拖拽分开；仅当单字比视野还大才 valid=false。叠画显示与拖拽门控从 `g_prevValid` 改为 `!g_prevCells.empty()`(RefreshLayoutPreview 无论 valid 都取回 cells)，所以字号大到排不下时字块仍可见可拖。**字号输入务必分两段**（否则打字会被打断）：`EN_CHANGE`→`ApplyCharSizeField` 只在值 ≥60 时实时套用、**绝不回写文本**；`EN_KILLFOCUS`→`CommitCharSizeField` 才把值夹到 [60,500] 并回写生效值+提示。60mm 是比赛每字≥6×6cm 下限，故 <60 会被抬回 60（看着“没变”属正常，但输入过程不能被强制回写，否则打“80”会在首字符“8”就被夹回“60”再也打不出——这是踩过的坑）。
- config 新键：`free_text`/`free_cells`([{x,y}])/`free_char_size`/`glyph_orient`；snapshot.cfg 暴露 `free_char_size`/`glyph_orient`。旧 `layout_*`/`write_dir` 键与 hanzi `plan_manual_layout`/`plan_text_area_and_layout`/`prepare_layout_only` 仍在（控制台/回归用），GUI 已不调用。
- GUI 拖拽（`gui_win32.cpp`）：`TrailView g_tv` 缓存固定 map（`DrawTrailPanel` 每帧写、`tvMapX/Y`+`tvUnmapX/Y` 复用）；`CanDragCells`=书写页+视野就绪+`!g_prevCells.empty()`+`!task_active`+`commandedSize()==0`；`HitCell`/`SnapCell`(≈6px 磁吸到其它字边/中心+视野边/中心)。`WM_LBUTTONDOWN` 命中→SetCapture；`WM_MOUSEMOVE` 拖拽(磁吸+夹取)；`WM_LBUTTONUP`→`set_free_cell` 落库。可编辑叠画显示条件 = `!g_prevCells.empty() && !task_active && gs::trail::commandedSize()==0`（**允许重叠也照画**；跑过一次任务后隐藏，点「重置画布」再编辑）。`WRITE_PANEL_H` 372→300。
- 验证：g++ 链接 RC=0（GUI+控制台，无新 warning）；纯数学单测 29/29 PASS（网格居中/换行/夹取/字号超视野不可行/旋转四向保框且可逆/回退视野）；dryrun 启动 RESPONDING=True；Computer Use 实操核验：拖拽移动+落库、字体朝向仅旋转不改位、固定视野不缩放均通过。改动文件仅 `gui_service.{h,cpp}`+`gui_win32.cpp`（未碰 hanzi/gui_trail/serial_port，避让他窗口）。

## 分页 + 翻页约定（2026-09-20）
一页写完→向翻页机构发信号（当前为模拟等待，蓝牙未接入）→等翻页→抬笔清实时轨迹→写下一页。用户四项决策：①**每页独立布局**可逐字拖拽；②翻页走纸量=版面高、**下一页字块与当前页世界坐标重合**（不做偏移补偿，简化为同坐标系复用）；③超容量**只写前 每页字数×总页数 字**（截断，不报错阻断）；④翻页等待期点停止/急停**立即中止不翻页**。
- **服务层分页模型**（`gui_service.cpp` 匿名 ns）：`g_full_all`=过滤后全量有效文本（**不截断**，缩小容量后再放大可无损恢复）；`g_full_text`=截断到容量的实际书写文本；`g_pages`=每页 `gs::PageEntry{std::wstring text; std::vector<Offset> cells}`；`g_page_chars`(1~50)/`g_page_count`(1~20)；`g_edit_page`(空闲时 GUI 拖拽页) 与 `g_write_page`(atomic，任务线程书写页) 双游标；`display_page()`=任务中取书写页否则编辑页；`page_drag_enabled()`=空闲+显示页==编辑页+`commandedSize()==0`。
- **`rebuild_pages(full)` 三级坐标继承**（防改页数/每页字数/容量恢复丢摆位）：①新页 k 与旧页 k 同文本→整页沿用；②`same_order`(旧有字页序列==新前缀)时按文本认领未占用旧页；③按字在旧全文首次出现位置继承该字旧坐标（整页每字都能继承才用，否则留 `ensure_page_grid` 补初始网格）。`layout_preview`/`preflight`/`run_task_thread` 均 `filter_page_chars`→`g_full_all`→截断 `g_full_text`→`rebuild_pages`。`partial`=编辑页超出有字页范围（容量外空页），有字末页不满额不算 partial。
- **翻页接口**：`gs::PageTurnFn=std::function<bool(int page_no_1based,int total)>`；`set_page_turn_handler(fn)` 注入真实蓝牙（传 nullptr 恢复默认）；默认 `page_turn_wait_locked()` **必须在持 g_mu 的任务线程调用**，分片 50ms 睡眠并轮询 `g_task_cancel||g_estop`，命中返回 false。`set_page_turn_wait_ms(500~60000)` 持久化。GUI `Run()` 有注入锚点注释。
- **`run_task_thread` 多页循环**：预热→(蘸墨)→逐页{`prepare_page_plan(页)`→`task::page_begin`→逐字(居中/旋转/发送，每页首字抬笔预定位锚点，跨页全局 `gci%5` 蘸墨)→**页尾自动复位抬笔 `UP-PAGEEND`**(逐字循环结束即在 `g_last_pose` 原位发 Z_UP+短驻 300ms，每页含末页必有，先于补蘸/翻页)→页尾补蘸→非末页:审计`page_turn`→`g_page_turn()`→成功则 `g_write_page=下一页`+`trail::reset()`，失败/取消则 canceled}→末页收尾蘸墨→(描边)。审计事件 `page_turn`(发信号,带 turn_to_page/wait_ms)/`page_turn_result`(ok/canceled/turn_failed)。原翻页前"抬笔到当页末字上方"(`UP-PAGETURN`)已并入 `UP-PAGEEND`，非末页不再重复抬笔（2026-09-20，用户定案:原位抬笔+短驻、合并去重）。
- **config 键**：`page_chars/page_count/page_turn_wait_ms/page_text`(截断后)/`page_text_full`(全量)/`page_cells`(嵌套 `[{text,cells:[{x,y}]}]`)；`cfg_load` 兼容旧单页键 `free_text/free_cells` 自动迁移为第 1 页。snapshot.task 加 `page_no/page_total/display_page`，cfg 加 `page_chars/page_count/edit_page/page_turn_wait_ms/task_finished/text_total/text_written`。
- **GUI**（`gui_win32.cpp`）：左栏第三行「每页字数」「总页数」整数框（`ApplyIntField`/`CommitIntField` 两段式，同字号教训）+ 截断提示 STATIC；轨迹面板顶加**翻页条** `◀ 第 k/N 页（本次书写 M 页）▶ …翻页等待(ms)[输入]`（`WritePlotGeo` 新增 strip/btnPrev/btnNext/pgLab/waitLab/waitEd 几何，`WRITE_PANEL_H` 300→346；**页码标签 `pgLabW=480`、等待标签 `waitLabW=150`**——初值 360/120 太窄，`Text` 的 `DT_END_ELLIPSIS` 会把“本次书写 2/2 页”“翻页等待(ms)”截成省略号，2026-09-20 加宽修复）；`RefreshLayoutPreview` 缓存 `g_pgEdit/g_pgTotal/g_pgWritten/g_pgPartial` 并在任务结束瞬间刷新；`IDC_BTN_PAGE_PREV/NEXT` 空闲切编辑页(任务中拒绝)。
- **验证**：`verify_pages` 单测 78/78（切页/截断/容量恢复不丢字/每页隔离/三级继承/旧键迁移/漂移重排/preflight/翻页成功+失败+默认模拟等待+7.4 等待期 abort 立即中止停第1页）；dryrun 审计链完整；Computer Use 实操切页/隔离/翻页后轨迹清空重播所见即所得。**待真机**：走纸精度是否=版面高（否则加偏移参数）、翻页后 `g_last_pose` 不变前提、真实蓝牙替换模拟、拖纸时臂抬笔避让安全性。

## 蓝牙翻页接入（2026-09-20/21 已实现；上面「分页+翻页约定」里的“蓝牙未接入/模拟等待”已被本节取代）
翻页真信号 = **STM32+TB6612+直流电机** 收到 `RUN<档位>,<时长>` 后，先回 `OK ...`，转满时长再回 `DONE <档位> <时长>`；**等在到 `DONE` 才算翻页成功**（闭合环，不用定时器猜）。
- **模块事实（省得重新体检）**：这块「HC-05」**实为 BLE 模块**（固件 `hc05V2.3_le`），Windows **永远不建 COM 口**（全系统 SPP 实例 0 个）→ 不能走串口，只能走 BLE GATT 透传。地址 **`21:F6:47:3A:D8:89`**（Windows 枚举名 `BTHLE\DEV_21F6473AD889`）。GATT：服务 **FFE0** → 特征 **FFE1=0x0010（write + notify）**、FFE2=0x0013（只写）。**FFE1 不可读**，所以回包只能靠 notify。
- **传输层选型（已实测定论，勿再试）**：**全部用 WinRT**。Win32 老 API（`BluetoothGATT*`）读缓存能成功但**写入/订阅一律立刻 E_FAIL(0x80070001)**，两套栈不能共用设备。WinRT 侧：`BluetoothLEDevice.FromBluetoothAddressAsync` → `IBluetoothLEDevice::GetGattService(FFE0)` → `IGattDeviceService3`（`RequestAccessAsync` + **`OpenAsync(SharedReadAndWrite)`**）→ **`GetCharacteristicsWithCacheModeAsync(Uncached)`**（缓存可能是冷的，`GetCharacteristicsAsync` 会给 0 个特征）→ FFE1 → `add_ValueChanged` + `WriteClientCharacteristicConfigurationDescriptorWithResultAsync(Notify)`；写用 `IGattCharacteristic3::WriteValueWithResultAsync`。**判据要看状态码，别只看 `OpenAsync`（2026-09-20 深夜修正）**：`GattOpenStatus` 只说明“服务对象能不能打开”（`Success=1` / `AlreadyOpened=2` / `SharingViolation=4`=被别的程序占着 / `AccessDenied=5`），**它可能完全从本地缓存返回 `Success`，而空口其实根本没连上**。**判“模块在不在”的首选办法既不是扫描、也不是 `ConnectionStatus`，而是「能不能拿到设备对象 / 建链多快」（2026-09-21 晚再修正，反例见 `bugs.md` 末节）**：`BluetoothLEDevice.FromBluetoothAddressAsync(addr)` 拿到对象 = 系统认识它 = **在**（`Connected` 与 `Disconnected` **都算在**，后者只是“此刻没连着”）；返回 `null` = 不在。等价且更省事的判据是**建链耗时**：链路已存在 ≈ **0.1~0.6s**、刚在广播 ≈ **2~3s**、**不在时空等 25s 才报错**（差两个数量级）。`GattCommunicationStatus` 仍用于分诊：`0`=Success / **`1`=Unreachable＝无线层够不到** / `2`=ProtocolError / `3`=AccessDenied；`GattOpenStatus` 的 `4`=`SharingViolation` 才说明被别的**程序**占着。`ble_motor.cpp` 失败信息会把 `[link= open= comm= n=]` 一并报出。
- **`ble_motor.{h,cpp}`（新增，唯一新文件）**：单工作线程独占 WinRT（进 MTA），对外只有 `blem::exec(line, want, timeout_ms, canceled, got, err)` 这个**阻塞式闭环调用** + `state()/state_text()/last_error()/request_connect()/request_disconnect()`。`start()` 幂等；**工作线程一律 `detach()`**（可 join 的静态 `std::thread` 会在静态销毁阶段 `std::terminate` 崩溃），优雅收尾靠 `started/workerDone` + `stop()`**所有发给板子的字符串须纯 ASCII**。
- **接入点**：`gs::page_turn_install()` 在 `Run()` 里 `cfg_load()` **之后**调用（就是原来那条锚点注释的位置），注册的回调做「确保链路（未连接自动建链，最多等 40s）→ 发 RUN → 等 DONE」。**回调不持 `g_mu`**（页循环调用点本就无锁），所以长阻塞不卡 UI（UI 靠 snapshot 的 500ms 定时器刷新）。取消 = `g_task_cancel||g_estop`，命中立即放弃（板子仍会把那 N 毫秒转完，属机械固有一致性延迟）。**链路建一次常驻复用**：实测首次 **0.1~3s**（模块刚在广播约 2~3s；被 Windows 攥着链路时 0.1~0.6s），之后一次翻页几百毫秒。（早期记的“十几秒”是模块尚未广播时的最坏情形。）
- **GUI（翻页条第二行）**：`[✓]蓝牙翻页　档位(0~20)：[ ]　时长(ms)：[ ]　[连接蓝牙]　状态文本`。档位上限**只由 `gs::PAGE_TURN_GEAR_MAX` 定义**（原 6 处散落的硬编码 `50` 已收敛到这一处），界面标签 `档位(0~N)：` 由该常量动态拼出；档位/时长沿用两段式整数框（EN_CHANGE 合法即套用不回写、KILLFOCUS 夹取回写）；`WritePlotGeo` 新增 `strip2` 并把下方四角标定区整行下移；新增复选框**必须紧邻 `IDC_CHECK_*` 块**（`MakeCheck` 按 `id-IDC_CHECK_DRY` 索引）；按钮要加进 `DrawBtn` 的颜色组。`WM_DESTROY` 调 `gs::page_turn_shutdown()`（断开 GATT + 停线程；不做的话下次启动可能撞 SharingViolation）。
- **config 新键**：`page_turn_ble`(默认 false)/`page_turn_gear`(0~20，默认 15)/`page_turn_run_ms`(100~600000，默认 3000)/`ble_addr`(默认 `21F6473AD889`)。审计新事件 `page_turn_ble`（cmd/gear/run_ms/result/elapsed_ms/detail，建链失败也记一条）。
- **构建**：`build.bat` 的 `COMMON` 要含 `ble_motor.cpp`，且**两个目标都要加** `-lruntimeobject -lwindowsapp -lole32 -luuid`（WinRT 需要；控制台目标原先没有任何 `-l`）。标准做法：需要 `NTDDI_VERSION 0x0A000000`（`ble_motor.cpp` 顶部已自带 define，不必改全局 `STD`）。
- **独立验收工具（workspace `tmp/ble_poc/`）**：`ble_selftest.exe`（链工程内 `ble_motor.cpp`）不开 GUI 就能跑「建链→发 RUN→收 DONE」；`winrt_probe.py`（**现在“判模块在不在”的首选**：用 Python 的 `winrt-*` 直读 `name`/`connection_status`/GATT 服务，**不发运动指令**，0.5s 出结果，且不会假阴性）；`scan.py`（bleak 主动扫描，**只当阳性证据**：扫到=一定在广播；**扫不到 ≠ 不在**，见下条）；`poc_winrt.exe`（逐项打印 OpenAsync / 每次枚举的状态与耗时）；`ref_bleak.py`（Python `bleak` 参照实现）。**注意：本机 `bleak` 3.0.2 只装在系统 Python**（`C:\Users\zby\AppData\Local\Programs\Python\Python313\python.exe`），WorkBuddy 的隔离 venv 里没有 —— 跑参照测试要用那个解释器。
- **★ 模块与 PC 已配对 ⇒ Windows 常驻持链路 ⇒ 模块不广播（2026-09-21 晚实锤）**：`BTHPORT\Parameters\Devices` 下有 `21f6473ad889` 链接密钥记录。后果是**扫描假阴性**——`scan.py` 扫不到、bleak 按地址连报 `DeviceNotFound`，但 WinRT 按地址 0.06s 拿到设备、0.36s 枚举完 GATT、写特征收通知全正常。**想恢复广播**：Windows「设置 → 蓝牙和其他设备」里删除 `HC-05X`（取消配对），或给模块断电重启。
- **★ `g_dryRun` 管不到翻页**：翻页走 BLE、不是串口。所以「Dry Run + 勾蓝牙翻页」= 机械臂不动、**电机真转**。这是刻意利用（可在不动臂的前提下验 GUI↔BLE 集成），非 bug；若哪天要让 Dry Run 连电机也不动，在 `page_turn_install()` 回调开头加一行 Dry Run 短路即可。
- **每个坑的详细判据见 `bugs.md`「蓝牙翻页接入的坑」**（含 Win32 为何不可用、FORCE 标志无效、Uncached、OpenAsync、委托签名、GUID 未定义、`IBluetoothLEDevice3` 缺定义、关闭崩溃）。


## 实时轨迹与四角标定约定（2026-09-19 实现）
- 模块 `gui_trail.{h,cpp}`（`gs::trail`）：存已下发轨迹 `Cpt{x,y,pen}`、实测点 `Apt{x,y}`、四角 `Corner[4]`；自带独立 `std::mutex`，**锁序恒 `g_mu → trail`**，内部绝不回调 `gs::`（避免反向死锁）。GUI 用 `epoch()` 判任务 reset、游标增量 `fetchCommanded/fetchActual` 取数。
- 轨迹**仅在 `g_task_active` 时记录**（挂在 `device::log_send_point`/`log_send_batch7` 钩子），回中心/心跳/测试点等快捷操作不进图。
- 实测点：`SerialPort::readPose(x,y,z)` 走 Modbus **0x03 读 0x0008×5**（回帧 15 字节，坐标 0.1mm 编码 ÷10）；**只能在任务线程调用**（半双工单总线 + 反馈延迟 ≤300ms，且 `SerialPort` 由单写者占用），现每字采一次。**读回是实际位置还是最后写值待上机确认**。
- 四角标定：用户依次输入四角**设备 mm 坐标**（与轨迹点同系），每角「预览」/「保存」；持久化到 `robot_config.json` 的 `"corners"`（数组 {x,y,valid}），下次开 GUI 沿用，「清除标定」复位。绘图视口优先按已下发轨迹、无轨迹有角→按角框、都无→`g_devLimit`；四角齐全时**按绕质心极角排序连线**成简单四边形（防输入顺序导致交叉）。
- `gs::preview_corner(x,y)`：抬笔移到该角，**真机会物理移动**（属硬件动作）——任务中拒绝、未连接拒绝、`inXYRange`+`inZRange` 越界拦截、DRYRUN 仅打帧；Z 用**已固定书写平面** `g_writing_plane_z`（valid 时）否则 `Z_UP`。`save_corner` 校验后写角+审计 `corner_save`+`cfg_save`。
- **可达边界（已撤销实验，回原版）**：曾把 `g_devLimit` 做成 config `"dev_limit"` 可配置并全局收紧到 X±162/Y±85，后经真机验证**已撤销回原版**——`g_devLimit` 恢复固定 ±180（无 config 键、无 snapshot 项），`sendPoint`/`preview_corner`/`save_corner` 按 ±180 校验。**探明的硬事实（保留）**：真机上电可达极限就是 **X±162 / Y±85**，且断电可手推越过 → 限制在**控制器/机构侧，非软件**（疑控制器工作区软限位，手册 ±180 但固件设小），软件无法扩，需找厂商放参数或改机械；超出该框的点控制器会静默夹到边界。**四角预览/保存/清除结果在轨迹面板标题行显示**（`SetResult` 顶部横幅只画在主页）——此 UI 保留。

## 审计日志约定（已实现）
- GUI 操作写 `logs/audit_<yyyyMMdd_HHmmss>.jsonl`：首行 `event=session`，随后每操作一行，`fflush` 逐条落盘。
- 字段：`source`（GUI/CONSOLE）、`actor`（HUMAN）、`mode`（DRYRUN/REAL/OFFLINE）、`operation`、`operation_id`、`request_id`、`session_id`、`parameters`、`result`、`error_code`；发送类事件另带 `tx_frame`/`rx_frame`/`device_response`/`ack_valid`/`retry_count`/`software_pose`。
- 事件类型：session / connect / disconnect / send_point / send_batch7 / task_preflight / task_start / task_abort / task_end / estop / estop_clear。

## 并发与锁约定（重要）
- `gs::` 内部单把 `std::recursive_mutex g_mu` 保护串口写、任务状态、审计文件；必须保持递归锁，因为 `session_id()`、`op_event()`、`audit_write()` 等会在已持锁路径被复用（改回 `std::mutex` 会让 `preflight` 死锁）。
- 任务线程 `detach`，用 `std::atomic` 的 `g_task_cancel` / `g_estop` 协作取消；`abort_task()` 经急停通道，抬笔后结束。

## 串口枚举与端口打开（重要安全约定）
- **枚举串口只许查询，不许打开**：`gs::list_serial_ports()` 仅用 `QueryDosDeviceW`（按 `ERROR_INSUFFICIENT_BUFFER` 扩容重试）+ 注册表 `HKLM\HARDWARE\DEVICEMAP\SERIALCOMM`。禁止用 `CreateFileW` 逐个探测——本机 6 个注册串口全是蓝牙虚拟串口（`\Device\BthModemN`），打开未连接的蓝牙口会在内核里阻塞（实测 COM4 5.2s、COM5 无限），而该函数由 UI 线程调用。
- 本机环境：DOS 设备名列表约 47349 字符，4096 WCHAR 缓冲不够；串口为 COM3/4/5/6/8/9（全部 BthModem）。
- 打开串口属同类风险：`gs::connect()` 已改为异步 `gs::connect_async()`——把 `SerialPort::open()`（蓝牙虚拟口会在 `CreateFileW` 内核无限阻塞）放到 detach 工作线程，**打开期间不持 `g_mu`**，结果经 `snapshot()["connect"]`（`pending`/`state`/`message`）由 UI `WM_TIMER` 轮询回报。**规则：任何串口 open 都不得在 UI 线程、也不得在持 `g_mu` 时进行**，否则一并冻结界面与 500ms `snapshot()`。

## 跨进程驱动 GUI 的安全边界（调试经验）
- 可用：`PostMessage(hwnd, WM_COMMAND, id, 0)` —— 已用于预检 1009 / 开始书写 1013 / 停止 1014 / 急停 1011。
- 禁用：跨进程 `SendMessage` 传缓冲区（WM_SETTEXT / WM_GETTEXT）—— 会挂死 GUI 主线程。
- 不安全：跨进程 `PostMessage WM_CHAR` —— 同样导致 GUI 未响应。
- 切页用 DPI-aware 的真实点击（`SetCursorPos` + `mouse_event`）稳定可靠；`PostMessage WM_LBUTTONDOWN` 客户区坐标点击会时灵时不灵，不要作为切页依据（2026-09-18 复核）。
- 文本输入的正路：预先把文本写入 `last_task.txt`，GUI 创建书写页时经 `gs::load_last_task_text()` 预载进输入框。
- 截图核验：本机 `CopyFromScreen` 会返回冻结帧（多次不同页面截出同一 md5），`PrintWindow` 单独用也会拿到旧帧。可靠做法是先 `RedrawWindow(h, 0, 0, RDW_INVALIDATE|RDW_UPDATENOW|RDW_ALLCHILDREN|RDW_FRAME)` + `UpdateWindow`，再 `PrintWindow(h, hdc, 0)`；每次截图后用 md5 比对确认内容确实变化，别用重复帧当核验证据。
- 判定 GUI 是否卡死：`SendMessageTimeoutW(hwnd, WM_NULL, 0, 0, SMTO_ABORTIFHUNG, 3000, &r)`——返回 0 即无响应；配合 `Get-Process` 看 CPU 增量区分“忙等”（CPU 高）与“阻塞”（CPU 0）。注意 `IsHungAppWindow` 偏粗，启动期会误报。
- 排查阻塞位置：gdb attach 在本机被拒（拒绝访问），改用**临时文件探针**（在各阶段 `fopen("_trace.log","a")+fprintf+fclose` 打点），复现后看最后一条打点；定位完必须删除探针。
- 串口下拉框（CBS_DROPDOWNLIST）不选中时显示为空是正常表现，不代表没枚举到；用 `CB_GETCOUNT`/`CB_GETLBTEXT` 跨进程查询实际条目（`CB_GETCOUNT` 无缓冲区，跨进程发送安全）。

## 运行时状态文件的路径约定（须从项目目录启动）
- `gui_window.txt`、`last_task.txt`、`robot_config.json`、`logs/` 全部是相对进程 CWD 的路径：从别的目录启动 `RobotGUI.exe` 会读不到上次任务与配置，审计日志也会写到别处（详见 bugs.md）。当前约定：**在项目根目录启动 GUI/控制台**。

## 笔画残缺（书写质量）· 当前状态（2026-09-21 第 3 轮交叉讨论后）
第 1~6 次往返的历史台账已归档：`agent_memory/archive/笔画残缺-历史讨论-2026-09-20.md`（含旧口径 C≈65~95ms、"速度反向"、RDP 空转等已被推翻/修正的说法）。本节点只留**当前有效**结论。

**已落地**：Phase 1（`motion.cpp:104-106` 三个 dwell getter 只读 config、顿笔开关不再管时间）+ config `z_settle_ms=95 / stroke_begin=0 / stroke_end=0 / writing_plane_z=-350~-352`；真机验证「目 内横写出、无炸毛」。

**第 3 轮（Step0/1 真机 + 审计日志复核）确立的事实**：
1. **真机每点实际预算 ≠ 模型表列**：审计 `logs/audit_20260921_182040.jsonl`（3063 条 send_point，整场会话）实测落笔走行中位 **档1 193/186ms、档2 155~156、档3 153、档6 123**；模型 `txy=d/v_model` 中位 134/90/68/29。差在**三项与档位无关的常量**：Z 过渡 +95ms、拐角停顿（见 2）、**串口地板 43~45ms/点**（= 相邻 send_point 的最小间隔）。
2. **⚠ 代码 bug（本轮新发现）**：`angle_between` 算的是**内角**（直线三点=180°），而拐角判据是 `ang>75°` ⇒ **命中 100% 的落笔内点**（全会话 1459/1459），真拐角（转折角>75°）只占 **0.8%**。每个落笔点白拿 +80ms（占预算 40%（档1）~75%（档6））⇒ 既**掩盖**了残、又是"抖/每点一顿"与时间浪费的主因。修法 `180°-ang > 75°`，**必须与"改 v 或地板"同一次改动**（单独修会让每点少 80ms、立刻变残）。
3. **手册**（`2(1).md`）：寄存器表「适用产品 **3 轴步进**」⇒ **无编码器、无位置源**；全文只有 0x0008（坐标读写：写 0x10 / 读 0x03）、0x0064（7 点写=抓放宏槽，非轨迹缓冲）、0xA001。`V：高8位为速度 0~9（其中 00 为最快速度）` ⇒ `speedHigh=lvl-1` 与手册一致。
4. **「速度编码反向」作废**：真机巡航实测（320mm 单指令）0.27/0.77/0.55/0.48/0.40/0.40s ⇒ byte1(档2) **最慢**、byte2..5 **越高越快**，只有 byte0 符合手册"00 最快"。质量与设备巡航无关的**强证据改用**：档6 设备（800mm/s）比档2（415mm/s）快却**更残**；且档2 与档3 的实际节拍只差 2ms 而质量同级。
5. **闭环回读（原路线 B）否决**：无位置源（3 轴步进；0x03 读的是坐标寄存器）+ 一次 0x03 轮询 ≈55~70ms（请求 9.2 + 响应 17.2 + 控制器周转 ~30~40）≈ 要省的时间 + 治不了"每条 0x0008 都减速到零"的固有抖。**仅保留为标定仪器**（运动中读 0x03，判定寄存器是否为"内部插补流"）。
6. **候选修法（先零代码验证）**：`min_point_interval_ms ≈120` + `z_settle_ms=0` —— 地板只抬短段、不碰抬笔长走行（长段 txy 300~1000ms 远高于地板）；预测 档2/3/6 的落笔预算 → ~184ms（= 档1 已证干净的节拍），档1 不变（其 txy 中位 134 > 120）。⚠ 地板抬到 ≥150 时**必须同时把 `z_settle_ms` 归 0**，否则 Z 过渡预算 = 地板+95，落笔静压会越过"顿笔开 ~170ms 炸毛"红线。
7. **修拐角会砸到串口地板（第 3 轮第二次往返新增）**：按整 run 复算（n=164/164/164/181），修掉拐角误判后落笔走行中位变 **134/90/68/43ms（档1/2/3/6）**，**<150ms 的点占 54%/66%/80%/95%**（档6 有 95% 掉到 43ms 地板）⇒ **单独修拐角绝对不能发**，必须与计时同批。中位点的角贡献是 ≈58ms 不是 80ms（每笔最后一个走行点本来就拿不到角点停顿）。
8. **需求是仿射型（不是常数），且 v 已被闭区间框死**【第 3 次往返修正】：早前分桶表的**归属错了一格**——`gap(a_k,a_{k+1})` 度量的是"**终点为 p_k 的那一段**"的预算，我（与对方）当时按"当前对的长度"分桶 ⇒ 长段被摊到全局中位（档1 `>12mm` 曾显示 194ms，**正确值 710ms**）。修正后（模型逐点校验中位误差 9~11ms）：档1 = 125/170/218/**357/710**、档2 = 109/152/184/265/494、档3 = 109/137/155/218/400、档6 = 107/108/125/153/216（<1.5 / 1.5-3 / 3-6 / 6-12 / >12mm）。**同 d 下拐角与地板项两档相同可约掉 ⇒ 档1 干净 ⇒ v_req ≥ 28、档2 残 ⇒ v_req < 42 ⇒ `v_req ∈ [28,42) mm/s`；"需求是常数"被排除**（常数需求下亏空最大在短段，而档1 的短段干净）。⇒ 正解形状 `base = max(串口地板, d/v + C)`，**v 初值 28（安全端）、C≈80（= 现每点白拿的角点停顿）**，P1 再把 v 往 40 压、C 往下压。
9. **P0 单复核结论（第 3 次往返，需对方改 6 处后才可执行）**：①**必须加"设置真生效"自检**（`audit_check.py <新 audit> <本行地板>`，低于地板阈值的相邻间隔应为 0）；②**地板会把 Z 过渡预算一起抬高**（`base=max(地板,dxy/v)` 先取地板、`z_settle` 再加）⇒ 落笔静压驻留 ≈ 地板−70ms：200→130ms、280/300→210/230ms、**700→630ms 必炸毛**；故高地板行要先做 30 秒"落笔-停-抬笔"探针、判读表加"起笔处墨团/炸毛"列；③P1 低地板行（150/120/90）必须**逐行 `z_settle=max(0,107−地板)`**，否则 Z 预算只剩地板，通道A 复活污染"长段残"读数；④**P0b 换设计**：地板 700 **喂不饱 30mm 段**（需求 ∈(794,1151]ms）⇒ 改"**字号 40 + 地板 700**"（段长按 2/3 缩放后 700 可全覆盖），判读只看笔画几何、墨团不算残；⑤**P2（地板 280 比 byte）仍被污染**（档2 的 `d>11.8mm` 段由 d/v 主导、档6 的 `d>35.8mm` 才主导）⇒ byte 的权威 A/B **移到压平表之后**（那时各档 v/C 相同，只有 byte 不同）；⑥改 config **前先关 GUI**（每个设置项都 `cfg_save()` 回写整个文件）。
10. **路线已定（第 5 次往返）：先写"安全 diff"（锁 档1），P0 系列全部后置为提速/去抖阶段**。理由：①档1 既是**实测唯一已知干净**、又是**巡航最快**（byte0=1185mm/s）⇒ 锁它零代价；②新形式 `max(43, d/28·1000+80)` **逐点 ≥ 当前档1**（拿 80 拐角的点相同、每笔最后一个走行点多 80）⇒ **"锁档1 必然干净"可证、不是赌**；③byte 问题对交付物**零决策价值**（就算 byte5 有影响也只是"继续用档1"），且 diff 后各档 v 相同、可免费做干净 A/B。**diff 内容**：①拐角判据 `π-ang`；②`estimateMoveMs` 加显式 `C`（config 键，初值 80，形式 `base=max(串口地板, d/v + C)`）；③**"表压平"改为"档2~5 压到 28、档6 保留 128 作抬笔专用快档并注释"**——因为 `p.speed` 同时定字节与模型 v，全压平会让"抬笔走行提速"失效（20mm 抬笔要等 714ms 而非 156ms）；④`z_settle_ms = 107 − C = 27`（新形式下 Z 过渡预算 = C+z_settle，钉在 Phase 1 验证过的 ~107ms）；⑤抬笔走行提速（给抬笔点 `p.speed=6`）**单独一次提交**。
11. **✅ Phase 2 计时改动已真机确认收口（2026-09-21 22:00，审计 `logs/audit_20260921_215926.jsonl`）**：我独立 parse 复验——270/270 点全 档1；落笔走行中位 **233ms**（预测 226）、p25/p75 185/356、**最小间隔 108ms、<45ms 地板点 0 个**（C 生效铁证）；拐角 +80 命中 **1 次**（改造前同轨迹 131 次）；**落笔走行逐点模型误差中位 8ms / p90 15ms / 最大 26ms / |误差|>30ms 的点 = 0 个**（n=164）；retry=0、ACK 全有效、`task_end=ok`、点数 268 与旧档1 同。**真实书写耗时 = `task_start`→`task_end` = 99.7s**（不是 150s；150s 含 48s 设置/预检）。墨迹：用户目视"5 处截断全消、无炸毛、残留轻微串珠"（与"C=80 是安全值"的预测自洽）；**照片待存档到 `test/` 作新基线**（`Phase2确认-C80V28.jpg`）。
12. **时间结构（本轮新发现，关系比赛预算）**：99.4s = 节拍 82.6s + **逐字锚点等待 16.7s**（每字一次、1.5~6.0s）+ 字间重置 1.2s。锚点等待过大的原因：`move_up_to_and_wait` 用**落笔**计时常数 d/28 去等一个**抬笔**移动（设备实际 byte0≈1185mm/s）⇒ 过等 ~40 倍。**当前速度账：5 字 99.7s = 20s/字**（1200s 预算约 60 字）。
13. **下一轮（已与对方定稿，未开工）**：①`estimateMoveMs` 按 `cur.isPenDown` 选 v（落笔恒 28 / 抬笔恒 128）**不再查表** ⇒ 顺带消灭"档6 危险档"红线；②`move_up_to_and_wait` 用抬笔 v=128（锚点 16.7s→~4s）；③抬笔点 `p.speed=SPEED_MAX`（byte5 实测 800mm/s，与 v=128 自洽）、落笔点保持 byte0；④**落笔点预算逐点不变**（C=80/v=28/拐角全不动）。**预期整行 ~100s→~60s（−40%）、墨迹逐笔不变**。**可证伪判据**：新 audit 的落笔点预算序列与本轮逐点 diff，应完全一致；抬笔/锚点应掉到 `d/128+80` 与 ~200–400ms。**风险**：抬笔提速 15 倍后要专门看每字起笔落点有无偏移/过冲。再之后才是"平整节拍上把 C 往下压、v 往 40 试"治残留串珠；蘸墨合规复验（`enable_dip=true`）等计时定稿后做。
14. **治串珠（C 扫参）已开单并复核（2026-09-21）**：对方出《治串珠-C扫参上机单-纯config》（C 阶梯 80→65→50→40→30，只动 `point_fixed_ms`，基线照片 `test/Phase2确认-C80V28.jpg` 已存档且我已看图核对：**5 处截断全消、目右竖/千竖串珠纹理可见** ✓）。**我审出 4 条要改**：①**C 与 Z 预算耦合**（Z 预算 = C + z_settle）⇒ 必须同步 `z_settle_ms = max(0, 107 − C)`，否则低 C 行通道A 复活、被误判成"C 压过头"；②审计判据改为"逐点对表 + 有效下界 max(43, C)"（低 C 时小段回落到串口地板 43ms 是正常）；③阶梯单向下压与批间漂移共线 ⇒ 末尾补一行 C=80 复测做漂移对照；④**加一行"C=80 不动 + 字号 90"判珠子归属**（间距随字号成比例 ⇒ 挂在命令点、RDP 对路；间距不变 ⇒ 粘滑 ⇒ 别做 RDP 那轮，改谈 v/深度/墨）——因为早期点纹自相关间距 ~0.9mm ≪ 命令点间距，粘滑的可能不小。顺序：先扫 C、后做 #10，认可（#10 只改抬笔/锚点，不动落笔预算）。**v2 已并入 4 条并与我对齐；我再补 3 条（`治串珠-上机单v2-复核结论.md`）**：①**Step A 改用单字「里」+每页字数=1**（字号 90 时 5 字行会互相压、布局被 clamp 后与基线不可比；「里」的长竖是第 5 笔、天然避开冷启动窗口），并同设置补一行 60mm 单字「里」做同字对比；②**两行都存审计** ⇒ 用该笔画的毫米长度÷照片像素长度自校准 mm/px，再与**命令点间距**直接比（比"随字号变不变"更硬）；③加一张**空白纸同倍率照片**做基底对照（若空白纸也有 ~mm 周期纹理 ⇒ 珠子部分是纸/墨纹理，C 与 RDP 都不相干）。另：**书写路径的权威字号键是 `free_char_size`**（`gui_service.cpp:918/1139`），`layout_char_size` 当前路径不用。
15. **治串珠 Step A 结论（2026-09-21，审计 `audit_20260921_222812.jsonl` + `test/test3/`）**：**珠子不在命令点上 ⇒ 不是"每点驻留"产物 ⇒ Step B（扫 C）与 RDP 减点两轮取消** ✓（我独立复算：90mm「里」竖笔 5 点/63.0mm、最大段 51.9mm；照片在那 51.9mm 单指令段内有 ~12–14 珠）。**原理**：C/v **不改设备运动**（只改发下一条前的空闲），设备剖面由**字节**决定 ⇒ 珠子间距对 C、v 都是不变量。**新线索（双方都漏）**：珠子**只长在竖笔**上、横笔（含 90mm 底横）光溜 ⇒ **方向效应**；候选 = A 机械/驱动（delta 雅可比投影、皮带 GT2=2mm 齿啮合）或 B 纸/墨（空白纸纹路横向 1–2mm ≈ 珠子间距 ⇒ 竖笔横穿纹路被吸成周期斑）。**两个零代码判决实验**：①纸转 90° 写同一个「里」——珠子跑到横笔 ⇒ B、仍在竖笔 ⇒ A；②速度字节扫参（`speed=1/2/3/4`，表压平后档1~5 的 v 都是 28 ⇒ **只换字节、计时不变**）——随字节变 ⇒ A 并可直挑最轻的档当工作档；不变 ⇒ B。**判成 B** ⇒ 追墨量/墨稠稀/换纸/深度；**判成 A** ⇒ 追字节 + 厂商（微步/电流）。#10（`isPenDown` 解耦 + 抬笔提速）不受影响、照排。
16. **治抖方案已收敛（2026-09-21，用户锁前提：只治速度1、不再测机、只用速度1、改代码看效果）**：对方出三条（①RDP 提 `rdp_tol_mm` 默认 0.35、②C 80→55、③落笔字节与 speed 解耦试 byte1），我复核后定稿为：①**RDP 成键保留 0.35**，但预期下调（真数据删点率 0.35≈10–12%、0.5→−17%、0.8→−30% ⇒ 真正的减停在 0.5–0.8，成键后免费扫）；②**C 55 必须配 `z_settle_ms=52`**（Z 预算恒 107，否则通道A 复活被误判成"C 压过头"）；③**③改为"`estimateMoveMs` 按 `cur.isPenDown` 选 v（落笔 28/抬笔 128）"**——落笔计时逐点不变（完整性零风险）、抬笔+锚点掉到 d/128（**整行 ~100s→~60s = #10 的时间收益，无需改 `p.speed`**）、顺带消灭"档6 危险档"；ink 字节留在 GUI 档位 ⇒ "试 byte1"变成零代码操作（= 被取消的判决实验②，用"改一行看效果"完成）。④小旋钮 `cold_start_min_ms` 150→0。**预期管理（必做）**：本轮治**顿挫**（停-走），**治不到串珠**（珠子在单条指令内部；串珠的软件侧抓手只有字节）⇒ 若主诉是串珠，本轮大概率"看起来没变化"。
17. **diff 后验收目标（离线已算，可证伪）**：：对方把上述 diff 写进工作区（`motion.cpp` 4 处 + `robot_common.{h,cpp}`/`gui_service.cpp` 的 `point_fixed_ms` 全链路 + config `speed=1/z_settle_ms=27/point_fixed_ms=80`）。**我核验方式**：读工作区代码（非其描述）+ 自跑 `g++ -fsyntax-only -std=c++17 -Wall`（msys2 toolchain，三个 TU exit=0 零警告）+ 离线重放同一真机轨迹（预算 96.1→101.7s = +5.8%、拐角命中 131→1、落笔中位 193→≈226ms、最小间隔 43→≈100ms）。**上机验收核对表**：①`taskkill RobotGUI.exe`→`build.bat`（RC=0、exe mtime 更新）；②跑前确认 GUI 速度档=1、顿笔/蘸墨关、`writing_plane_z=-352`、同笔同纸；③对照基准 = 旧"档1 行"照片（目标是"至少一样好"）；④`audit_check.py <新audit>` 应见 中位≈226ms / 最小≈100ms / Z≈107ms，**若仍出现 43ms 或中位 ~190 ⇒ C 未生效**；⑤35 笔 0 缺失 + 无炸毛 + 记单字耗时；⑥预期"完整度=旧档1、仍有轻微顿"，**不以顺滑判成败**。
12. **diff 后验收目标（离线已算，可证伪）**：同轨迹重放预测——落笔走行中位 **≈226ms**、p25/p75 ≈171/344、**最小 ≥100ms（不应再出现 43–45ms 串口地板点）**、Z 过渡 ≈107ms、单行 ≈102s（现档1 实测 105s）；若审计里仍有 ~43ms 或中位仍 ~190ms ⇒ C 项没生效。**预期管理：首跑应像"今天的档1"——完整但仍有轻微顿/串珠**（C=80 是安全值不是最小值）；治抖 = diff 之后在平节拍上把 C/v 往下压（P1/P3）。
完整论证见 `C:/Users/ceiii/.qoderwork/workspace/mu9q48io11r958my/outputs/书写残缺-第3轮交叉讨论-复核方回复.md`。

## 去"连珠"/笔画顺滑 · 最终结论（2026-09-22，17 轮交叉讨论 + 真机收敛；**覆盖本节后所有旧口径**）
**一句话（最终）：连珠有两个独立成分，都被「档3 + 抬 z_settle」治住了——① 肉眼可见的粗珠/停顿＝命令点端点驻留（慢字节·档3 治好）；② 短横变点＝Z 下降预算不足（抬 `z_settle≥150` 治好）。工作点＝档3 + `z_settle`(200~250)。**
**⚠ 纠正一个正在误导新窗口的误读：下文 §9 字节扫描里"档3 结构严重丢失（穷首笔没写上、里三横变点）"是 test4 当时 `z_settle=82`（太低）造成的假象，不是档3 本身的毛病。§9b/§9d 已证实：`z_settle≥150` 后档3 结构完整且是最光滑的一档 ⇒ 档3 就是工作点，不要回退到档1（档1 反而粗珠最重）。**
**另：本节"第 2 轮"曾把 ~1mm 空间型调制（纸纹）当主因，已修正——~1mm 细纹只是次要成分（字节与 z_settle 都够不到、属另案），主因是端点驻留。以下第 2 轮各条保留作过程记录，但优先级低于上面最终结论。**
1. **命令点数与字号无关**：同一次会话 60mm 与 90mm 各写「里」，落笔命令点**都是 38 个**；字号 ×1.5 ⇒ 段长 ×1.5（最长单指令段 44.73→67.10mm）。⇒ 大字号变的是**段长（笔尖速度/墨膜厚度）**。注：交接稿 §3.3 说"51.9mm 最大段"不准，51.94mm 是第 5 笔第 3 段，最大段是底横的 **67.10mm**。
2. **命令点处不产生珠**（像素级实测，`test/test3/90.jpg`）：第 5 笔 5 个命令点行宽 15/17/13/12 平平；51.94mm 单指令段内（无命令点）干净段宽在 4–10px 波动；**唯一的宽斑（宽 24–30px）全在 y=325/390/480 = 与日·中横/日·底横/土·上横的交叉**；笔末宽 12–13 vs 干净段 7–10 ⇒ **端点静置确有加宽但仅 ≈1 个"胖头"，属次因**。
3. **珠子空间周期 ≈ 0.93–1.0mm**：竖笔墨量自相关在 **4.36px** 有一梳峰（13/18/21/27/30/34 = 4.36 的整数倍），**同图纸面纹理 = 4.60px**，同图 px/mm≈4.5–4.7 ⇒ 比值 **0.95**。"12–14 珠"是照片目数，真实**≈50 个周期**（对方 3.7–4.3mm 的说法大了 ~4 倍）。
4. **横笔无周期性**：90.98mm 底横（含 46.76mm 单指令段）墨量 CV 0.185、自相关 3–70px 无任何峰；纸纹是**强横向 ~1mm** 结构 ⇒ 竖笔（夹角 88.7°，纸纹预测 1.00mm）与底横（夹角 2°，预测 28.6mm）**被同一纸纹模型同时解释**（方向效应的自然解释）。
5. **相位锁定证不了（如实记录）**：纸纹不跨 x 自洽（带相邻相关 0.497 / 0.075）⇒ 带通互相关检验无效；墨量×纸纹 0.16–0.37 而零假设 σ=0.098 ⇒ 不显著。故"纸面纹理/墨转移"是**首选非定论**，关节侧微步/机械纹波**未排除**。
6. **"字号越大越明显"新解释**：调制一直存在，段更长/线更薄时从"边缘毛边"变"断线串珠"（墨量 CV 0.16@60mm → 0.31@90mm，跨照片仅提示性）。
7. **✅ "落笔笔尖按 28mm/s 慢拖"已于 2026-09-22 被用户目视否定**：用户答"写长竖是**窜一下然后停住等**" ⇒ **B 线确认**（笔尖突发快走 + 在命令点端头干等积墨）。三条后果：①软件那 ~1.94s 里绝大部分是**端点干等**，不是走行 ⇒ "让笔尖走慢"是本任务主抓手，而**唯一软件旋钮 = 速度字节**；②**截断史有一个待验证的解释（假设，非实证！）**——"下一条指令落进突发窗口会被控制器刹断"**是我们为贴合 B 线构造的假设，没有直接观测**；正确姿态：**"把 `PEN_DOWN` 放大到截断复现再退一档"本身就是量出"真实突发+余量"的手段，不需要先验建模**。`d/28` 只是安全余量不是设备实测；③60mm 那次 60 点跑了 72s 而落笔点间隔中位仅 247ms ⇒ 墙钟大部分不是节拍 ⇒ **大额时间可省**（去掉干等同时去端点积墨，与连珠同源，应单独立项）。仪器（成本递增）：①问用户（**已用**）②手机慢动作数帧③现有 `readPose`(0x03) 连续采样（55–70ms/次，够分辨 0.1s vs 1.9s；占单总线仅诊断）。
7b. **速度字节的手册事实（2026-09-22 核 `2(1).md`，推翻"byte6–9 不存在"的推断）**：手册两处（0x0008 与 0x0064）都写 `V：高8 位为速度 0~9（其中00 为最快速度）` ⇒ **合法范围 0~9、按手册应是"越大越慢"单调**；`serial_port.cpp:85` **帧层早已夹到 0~9**（`clamp(0,9,lvl-1)`），真正卡住 6~9 的只有 `robot_common.h` 的 `SPEED_MAX=6` 与 GUI 输入上限 ⇒ **开 0~9 只需改一个常量**。**推论（重要）：映射 档lvl→byte=lvl-1 + 手册"00 最快" ⇒ 本项目一直在用 byte0＝机器最高速度写字，而手册内嵌的厂商面板默认是 `0500`（高字节 05）**。另：手册自身在这一字段上自相矛盾（通信示例给出 `0500`，正文却写"速度级别值（1）"）⇒ **手册答得了"范围"、答不了"排序"**。而我们那把秒表实测（0.27/0.77/0.55/0.48/0.40/0.40s）**不可分辨**：byte0≈0.27s 明显最快（与手册一致），**byte1–5 全部落在 0.4–0.6s 同一区间内**（"byte1 最慢"很可能是 ±0.2s 反应误差）⇒ **"哪个字节最慢"至今空白，且必须用"墨"判读而非秒表**。
8. **代码事实**：`estimateMoveMs` 的 `Cfix` 按该点自身 `strokeKind` 取，而 `sync_z_settle()` 用 `107−min(C直,C弯)` 配平 ⇒ 当前 config（C直=85/C弯=25、z_settle=82）下**直组点 Z 过渡预算 = 167ms**（"恒 107"只对弯组成立）；见到"直笔端点/起笔墨团、炸毛"根因在此。
9. **🔴 第 1 步字节扫描已上机（2026-09-22，审计 `logs/audit_20260922_193637.jsonl` + `D:/Code/华五/test/test4/`，档1/3/6/8/10＝byte0/2/5/7/9）**——这是本项目第一次让连珠真正变轻：
   - **用户观察**：档1 完整但粗珠重（曲线成坨、长横 1/3 处有停顿）；**档3 最轻（"玻璃般光滑"、匀速无停顿）但结构严重丢失**（穷首笔没写上、里三个横变成点）**〔⚠ 已纠正：此"结构丢失"是 test4 当时 `z_settle=82` 过低所致、非档3 本身毛病；抬 `z_settle≥150` 后档3 结构完整且最光滑＝工作点，见 §9b/§9d，勿回退档1〕**；档6 中；档8 中重；**档10 最重 + 全程颤抖**、竖画断墨。
   - **审计复核（3 条硬事实）**：①**软件节拍与字节完全无关** —— 五个可运行行的分桶间隔逐毫秒相同（落笔段内合计 **22.7s 五行一致**、中位 246–248ms；起笔 359–364；收笔 249–260；Z 过渡 400–404；抬笔走行 169–171）⇒ 干净单变量，质量差异 100% 来自设备；②**总时长的 40.1 vs 49.8s 差不是"慢档更慢"** —— 是任务后「回中心」指令落进统计窗口（档6 末点 `(0,0,-320)` 间隔 9211ms）；③**每行字的世界位置不同**（档1 首落笔 `(-24.5,3.9)` vs 档6 `(97.3,6.1)`）⇒ 珠子画在纸上不同区域，**跨行叠图比较不成立**，只能行内判读。
   - **⚠ 坐标帧陷阱**：`glyph_orient=3` 生效（里末笔 90.98mm 的段长序列 `6.8/25.4/46.8/6.1/5.9` 在 test3 沿 X、在 test4 沿 Y，分毫不差 ⇒ 整字被旋转 90°，照片里看着正立是因为纸/机位也转了 90°）⇒ **别把审计的 X 当照片的"横"**；两次会话的"横/竖"在世界方向上不是同一组，**不能直接比**。
   - **结论（两成分）**：**粗珠＝命令点驻留**（直接证据：用户说长横"1/3 处"停顿，而该笔命令点在 0/6.8/**32.2**/79/85/91mm，32.2÷90.98＝35%＝正是 1/3 ✓）**＝速度依赖型，字节可治**；**细纹＝指令内 ~1mm 空间调制**（**档3 放大后长横上缘仍是一串细珠** ⇒ 驻留被字节消掉、细纹没被消掉 ⇒ **两者是不同成分**，字节治不到）；**慢端＝控制器排队颤抖**（档10 完整但抖）。三者分开记，**不要合并成单因**。
   - **（原"未解释"，现已解释）结构丢失最严重在中间档（档3）**：不是谜——test4 全档统一 `z_settle=82`，而档3 走行最快 ⇒ 26mm 的 Z 下降最来不及在预算内落地 ⇒ 短笔丢头最狠；**根因是 z_settle 不足、与档3 本身无关**，抬 `z_settle≥150` 后消失（见 §9b/§9d）。
9b. **✅ 第 2 步真机成功：档3 + `z_settle_ms=300`（2026-09-22 20:48，审计 `audit_20260922_204825.jsonl` 第一行）——"截断"与"粗珠"第一次同时按下**，用户判"总体优于其他组"（结构完整、光滑保住、底横细珠只略重一点点）。**我独立逐点复验过：单变量成立** —— 与 z=82 那次相比，**105 条间隔里恰好 29 条变化 = 每笔两次 Z 过渡（14 下降 + 14 抬笔）+ 任务后「回中心」**，每条 **+218ms（＝z_settle 增量）**，其余 76 条逐点相同（±5ms），**笔内段长合计两次都是 700.5mm（逐点相同）** ⇒ 轨迹与节拍未变、机制坐实。（`gui_service/gui_win32` 在 20:38 被改、exe 20:43/20:45 重编，但已证不碰书写时序，故对比仍干净。）
  - **机制**：每次 Z 过渡（每笔起点 26mm 下降、每笔终点 26mm 抬笔）的预算 = `Cfix + z_settle`，慢字节下不够 ⇒ 笔未落地 XY 已开跑 / 抬笔未完成后续动作已来 ⇒ 短笔丢头。
  - **判据纪律（重要）**：`z_settle` **只作用在这 29 条 Z 过渡间隔上，够不到笔内间隔** ⇒ 沿笔画的 **~1mm 细珠与 z_settle 无关**（"细珠略重"是墨/纸/照片差异，不是它造成的）⇒ **下探判据只看"结构"（最短的笔回没回），不许拿"细珠最轻"当判据**（那是追噪声）。
  - **下探目标区间 = 200~260**（实测夹逼：z=82 时下降实测 262ms 丢结构、z=300 时 432ms 完整 ⇒ 阈值在 (262,416)ms）⇒ 阶梯 300→250→200，150 只作边界确认。**同时盯弯笔**：`Z 预算 = Cfix + z_settle` 而 Cfix 分组取值（直85/弯25）⇒ **弯笔天生少 60ms**（审计里可见 262/250/262 与 199/198/198 两组差≈60）。若被耦合咬到，干净修法是在 `estimateMoveMs` 里让 Z 过渡预算脱离 Cfix（约 3 行）；**不要抬 C弯**（会把粗珠抬回来）。
  - **仍未定死的一点**：z=82 那次失败的是"短的直笔"（下降实测 262ms），而**弯笔预算更低（199ms）反而完好** ⇒ 纯"下降预算"解释不通 ⇒ **每笔两次过渡里哪一次是操作变量还没定**（配置上两次都是 `Cfix+z_settle`，无法分开）。**下探正好判别它**：先坏弯笔 ⇒ 预算量成立；先坏短直笔 ⇒ 要查"单条长指令被后续指令截断"那一支。z=300 的照片里恢复的三横**起点/终点都没有明显墨团** ⇒ 300 没明显越过阈值，往下探是安全的。
  - **`z_settle_ms` 现在=250**（20:52 已改），且**审计第二行就是一次 250 的运行**（238 点/落笔165/下降中位 369ms）——**若那次拍了照片就是下探的第一个数据点；没拍则要重跑补照片**。
  - **`z_settle` 的真实代价（第 10 轮自我更正，勿再搞错）**：等待 = `T_z + 触纸静置`；`W<T_z` 时它是给下降让路（收益），`W>T_z` 时多出的每一毫秒都是**起点静压＝墨团/顿笔风险** ⇒ 最优＝最小有效值。**抬 z_settle 恰恰治"设备 Z 下降慢"这一支**；治不了的是"笔画走行被后续指令截断"那一支——**判据看墨落在笔画起点侧还是终点侧**。
  - 备选杠杆（未用）：**减小抬笔高度**（Z_UP 距书写平面 26mm，缩到 ~10mm 则 T_z 只剩 ~38%，唯一不靠"多等"的办法；但 Z_UP 是全局常量、牵连翻页避让，需单独评估）。
  - **反对"先补测 speed2"**：它夹在档1 有驻留与档3 丢结构之间，**预测两头不讨好**。
  - ⚠ **工作区混着未提交改动**（`gui_service.{h,cpp}` +6/+2、`gui_win32.cpp` +23/−5、`robot_common.h` +6/−2，另有 agent_memory）—— `set_z_settle_ms` 已核为直设、不调 `sync_z_settle`；**建议继续下探前先提交一次**，否则两边窗口的改动会互相盖。
9d. **✅ test5（z_settle 150/200/250）是有效数据，不是"作废"（2026-09-22 第 15 轮复算，审计 `audit_20260922_204825.jsonl` 6 个 run）**：**审计里"下降这一刀的预算"在下降点后面那条的时间差里**（错一格陷阱——量下降点**自己那一行**得到的是它前一步抬笔预定位的间隔、Δz=0、不含 z_settle，所以三次看起来"都一样"，对方据此误判 test5 作废）。**正确口径下的下降预算**：z=82 → 最小 198/直笔 262；**run2/run3(z=150) → 260/324**；run4(z=200) → 306/372；run5(z=250) → 355/430；run0(z=300) → 416/480。两条独立佐证：①步长 +46/+49/+61 正是 50ms 梯队；②**两次 250 互相印证**（run1 文本「欲穷千里目」最小 353 vs run5「里」355，差 2ms）。
  - **⇒ 阈值夹在 z_settle ∈ (82, 150)**：82 丢结构、**150 已完整**（我在 `test5/150.jpg` 上逐笔核过：日部两条内横是**实线不是点**）⇒ 用户"三次看不出区别"是**"三档都在阈值之上，本来就该没区别"**，不是"三次没变"。
  - **⇒ 剩余决策 = 在三档都可用里挑工作点**：差别只在起点静置余量（`W−T_z`＝笔压）与抗漂移余量。**建议 200**（比实测最低可用高一档、约 50ms 余量，起点静压明显小于 250/300）；时间代价 ≈1.4s/字。想更稳就留 250；150 适合不再换笔换纸的场合。
  - **⚠ 别用"墨更深/更干净"选值**：`test5/150.jpg` 与 `250.jpg` 并排看，250 明显更黑更厚、点纹更明显，但按审计两者**笔内间隔逐点相同** ⇒ 那是**墨量/曝光**差（250 那张纸面更亮反差更高），**同机同日同笔两张照片的墨色差就盖过 z_settle 效应** ⇒ 这一刀只能靠结构判。
  - **机制合并（第 15 轮）**：**根因＝每次 Z 过渡在慢字节下不够快**（故 z_settle 有效）；**哪个笔画看得见＝该笔有没有"重画机会"**——直笔常被 RDP 塌成 1~2 点 ⇒ 整条一次 XY 指令、被截就整条变点；弯笔点多 ⇒ 只丢笔头、后续照描 ⇒ 肉眼看不见。⇒ **哨兵用"最短的孤立直笔"**（里三横/穷首点）；§4 那 3 行 Z 预算解耦**留作"真出现弯笔先坏"的应急，暂不做**。
9c. **✅ 已解决（2026-09-22 晚）**：`glyph_orient` 用户已在 config 改回 **0**（正立），坐标帧陷阱关闭；但**现场别再转错**——orient≠0 时审计坐标与照片差 90°（第 8 轮踩过）。
10. **当前实机 config（以工作区 `robot_config.json` 为准，未提交、会随现场变动）**：**2026-09-22 22:0x 实读**＝`speed=3`（工作档）· **`z_settle_ms=250`** · `point_fixed_ms=85 / point_fixed_curve_ms=25` · `rdp_tol_mm=2.1 / rdp_tol_curve_mm=0.1` · **`writing_plane_z=-353`** · `free_char_size=70` · `glyph_orient=0` · `enable_dunbi/enable_dip=false` · `page_text=闲来垂钓碧溪上` · `write_dir=1`。（旧值 `-351/90/orient3` 已作废；交接稿 §9 更旧。）
  - ⚠ **`-353` 与定 150/200/250 那批（当时 -351）深 2mm** ⇒ 法向压力更大、起点静置的墨团余量更薄 ⇒ **工作点宜偏保守（200 优于 250/300）**；若终检见起笔偏重/墨团，先怀疑 z_settle 偏大或平面偏深，**别去查 C**。
  - ⚠ `enable_dip=false` 与比赛"须自主蘸墨≥1 次"冲突 ⇒ **合规风险项**（见 §12）。
11b. **两条执行纪律（2026-09-22 双方签字，勿违）**：**约束 A**＝第 1 步字节扫描的判据要分流机制——**珠子周期随字节变**（或整线变厚把珠子填平）⇒ 速度依赖型（飞白/墨转移）⇒ 慢字节是真解，往 byte9 方向找拐点；**周期不动、只有整体深浅变**⇒ 空间型机械（皮带齿/微步锁在纸面坐标系）⇒ 慢字节治不了，转物理侧+厂商。三行（档1/3/6）**必须同纸、纸不动、最好拍进同一张照片**（跨照片比周期会被对焦/曝光/缩放污染）。**约束 B**＝第 1 步（字节）与第 2 步（`PEN_DOWN` 放大）**配对且 byte 先定**：放慢字节＝拉长突发＝抬高截断边＝能省的端点时间变少，两步并排或反序会互相污染。**另：`SPEED_MAX 6→10` 的爆炸半径已数清＝7 处引用全是档位校验、无一处参与计时/运动学（`speedLevelToXYmmPerSec` 已成死代码无调用点）⇒ 改 `robot_common.h:84` 一个数即可。**
12. **挂账（在册缺陷，与连珠无关，本轮不动）**：**蘸墨 `do_dip_and_groom` 的"上慢下快"是反的** —— `SLOW=1` ⇒ byte0 ⇒ 按手册 `00 为最快速度` **是最快**，`FAST=min(6,max(2,SPEED_LEVEL))` ⇒ byte1–5 反而更慢，与注释"上拉慢/迅速下"完全相反。**不要拿已作废的"速度反向"解读**（依据是手册原文 + 用户目视"档1 写字是窜一下"，两者一致）。`enable_dip=false` 遮着，改它必须上机蘸墨核动作 ⇒ 等蘸墨启用那一轮一起定（很可能 `SLOW` 取慢端、`FAST` 取 byte0），并顺手把硬编码 `6` 改成 `SPEED_MAX`。
11. 交叉讨论文档：`C:/Users/ceiii/.qoderwork/workspace/mucar501uhl5blop/outputs/回复-去连珠-第1轮.md`（对方）与 `回复-去连珠-第2轮-本项目agent.md`（我方，含全部实测细节与命令）；背景交接稿在 `.../mu9q48io11r958my/outputs/交接文档-去连珠顺滑度.md`。

9e. **✅ 代码落地 + 提交（2026-09-22，本 agent 侧，与 §9d 互补——§9d 是分析，本条是"码已进仓"）**：
  - **`SPEED_MAX 6→10`**（`robot_common.h:84`）已改、双 exe 编译 RC=0；档 7~10＝byte6~9 首次可达（帧层 `serial_port.cpp:85` 早夹 0~9、`speedLevelToXYmmPerSec` 死代码，均不碰计时 ⇒ 零完整性风险）。**工作档仍是档3。**
  - **书写页新增"Z沉降ms"实时框**（`gui_service.{h,cpp}` 加 `set_z_settle_ms/z_settle_ms`：直设 `g_z_settle_ms`+`cfg_save`、**不调 `sync_z_settle`**；`gui_win32.cpp` 加 `IDC_EDIT_ZSETTLE`/`g_editZSettle`、`WLGeo.row6Y`、`WRITE_PANEL_H 384→414`、`ApplyIntField/CommitIntField` 两段式、`DestroyPageControls` 销毁、预填读实时 getter 切页不丢）。⚠ **改任一 C 框仍会被 `sync_z_settle` 把 z_settle 打回 `107−min(C)`** ⇒ 调值只动 Z沉降框、别碰 C 框。已 dryrun 截图核验新框在位、预填 82、按钮/轨迹面板不打架。
  - **off-by-one 二次教训（钉牢）**：见 §9d——审计"下降这一刀预算"在下降点**后面那条**时间差里；量下降点自己那一行＝它前一步(Δz=0)不含 z_settle ⇒ 会把"三档不同"误读成"三档相同"（我第 14 轮据此误判 test5 作废，第 15/16 轮纠正）。**分桶归位别错一格。**
  - **本次提交范围**：4 个代码文件（`robot_common.h`/`gui_service.{h,cpp}`/`gui_win32.cpp`）+ `agent_memory/*`；**`robot_config.json` 是运行态（用户已把 glyph_orient 改回 0、free_char_size 80、页摆位改「飞流直下」等），不随本次提交**。
  - **连珠收口状态**：粗珠＝档3 字节治、截断＝抬 z_settle 治、阈值 ∈(82,150)、150 已完整；**待用户在 200/250 里定工作点 + 写一行「欲穷千里目」终检**。~1mm 底横细纹＝空间型/纸纹，另案。

## 笔画质量·第 1 轮交叉讨论（2026-09-22 晚，本 agent = 复核方）· 当前有效结论
对象＝「闲来垂钓碧溪上」真机照片 `test/testnew/207ae855879215d2dbcf5b107d059394.jpg` + 审计 `logs/audit_20260922_213935.jsonl` 第 2 次 run（379 点，wall 155.1s）。我方回复稿：`C:/Users/ceiii/.qoderwork/workspace/mucqjq4ir3q11afi/outputs/笔画质量-第1轮-复核方回复-本项目agent.md`（对方原稿同目录 `笔画质量-第1轮交叉讨论稿-本项目agent.md`）。
1. **口径钉死**：审计 `gap(T_k→T_{k+1})` = **"到达 P_k"的预算**（= `estimateMoveMs(prev,...)`，不是 P_k 自己那条）。按此对 377 点逐点对表，残差中位 **+12ms** ⇒ 软件计时按设计生效（byte2、C直85/C弯25、z_settle250 在位）。
2. **字首大跳**：每字单独一次 `transmitTrajectoryWithSplit` ⇒ 首点 `lastSent=nullptr` ⇒ 给 12ms、被 `cold_start_min_ms=150` 冷启动地板抬到 **150ms**（实测 151/153/156ms），**与距离无关**。整行 **5 处**：来 54.3mm/151ms、垂 80.1/153、钓 80.1/153、**溪 69.3/156**、**上 81.5/156**（后两处是对方漏的）；闲/碧 有 `move_up_to_and_wait` 保护（d=0，锚点 2277/3028ms）。按 v=128 应给 509~722ms ⇒ 欠 358~566ms。
3. **照片证据："缺笔"是"没上墨"，不是"没执行"**：碧「石」上横（43.7mm、2 点=单指令）3x 放大＝**两端各一个驻留墨团 + 中间一条连续极淡细线** ⇒ 终点有驻留 ⇒ 走到底了，不是被截断。垂长竖同型（上实→中珠→下极淡连续；x1090-1180/y390-420 与空白纸 F10 = 150 vs 161 ⇒ 近乎无墨）。⇒ **"走行被后续指令截断"这一支本轮无证据；P-3（收笔余量）不做**。
4. **统一机制**：墙钟账 155.1s = **我们自己的间隔 140.2s** + 13.3s 主动停顿（7×字尾 readPose+160ms 睡眠、翻页 4122ms、锚点）⇒ **设备全程没有一次超时 ⇒ 设备比模型快得多**（落笔 28mm/s 是大幅高估）⇒ 笔在每个命令点"窜一下→停住等"：停住上墨（墨团）、窜行几乎不上墨 ⇒ **一条笔画的外观 ≈ 它的命令点密度**。数字：**29/60 笔（48%）是"2 点=单指令"**，碧 9/14、溪 6/13；典型＝闲门点 11.2mm、**来首笔横 34.0mm**、垂长竖主体段 42.3mm、碧石横 43.7mm、王竖 17.7、白竖 21.3、口竖 16.7、口底横 23.1、溪第11笔 52.8mm ⇒ 用户抱怨的"垂没写到位/碧各短竖/钓竖提过短"全落在这一族；字号放大使单指令段更长 ⇒ "越大越明显"也被解释。
5. **额外机制（与抢占/排队无关）**：单条 `0x0008` 内三轴各走梯形 ⇒ ΔZ=28mm 约 73ms 先到底、ΔXY=54~81mm 要数百 ms ⇒ 笔尖先触纸再被拖完余程 ⇒ **落点前一段划痕、终点恰为笔画起点**；两种控制器行为下都成立，且同样只被字首大跳激活 ⇒ 修 D1 不依赖"抢占 vs 排队"悬案。（另：本照片**横笔也有珠**，与旧记「珠子只长在竖笔」不符，已请对方复核。）
6. **本批建议（变量隔离）**：**D1 代码**＝去掉 `gui_service.cpp` 的 `if (ci==0)` 闸门、**每个字都预定位**（"播种 lastSent" 在 v=128 下对 80mm 只剩 ~12ms 余量，不够；预定位复用已验证路径，+6~8s/行）；**D2 config**＝`rdp_tol_mm(直) 2.1→0.8`（唯一能"给笔画中段加驻留点"的软件旋钮，+10~20% 时间，超时退 1.2）；**不做** z_settle/C/速度档/P-3。**D3 可选插桩**：把 `readPose` 结果写审计（`read_pose` 事件）⇒ 一次性判定抢占/排队 + 顺手结掉"readPose 语义"，~10 行。
7. **顺带记下的结构性毛病（本批不修）**：`Z 过渡预算 = Cfix + z_settle`、`Cfix` 按该点自己的 `strokeKind` 取 ⇒ 现**直组 335ms / 弯组 275ms**，直笔起点静压天生大 60ms；要修就让 Z 预算脱离 Cfix（`estimateMoveMs` 约 3 行）。另 `move_up_to_and_wait` 的 guard 含 `get_Z_SETTLE_MS()` ⇒ z_settle 27→250 后 guard 悄悄长了 223ms。
8. **待用户确认（已写进回复稿 §6，由对方代问）**：①划痕到底在第几个字（若真在「闲」则字首机制不成立）；②「闲」门点处纸上有没有 ~1cm 淡线；③「垂」长竖最下 ~10mm 有无极淡线；④20 分钟预算能接受多少增量（现 155.1s/7 字）。
9. **时间预算风险（新）**：当前参数 **7 字 155s ≈ 22s/字** ⇒ 60 字约 1330s > 1200s 红线；后续任何"加时间换质量"的改动都要同时盯总额。
10. **第 2 轮（对方回稿 → 我方复核）：介质澄清是转折点，两条新事实（全部介质无关、零上机）** ——我方第 2 轮回复：`.../mucqjq4ir3q11afi/outputs/笔画质量-第2轮-复核方回复-本项目agent.md`。
  - **介质＝水写布**（用户原话）：照片上的"横竖中段变淡"是**风干假象**（"刚写完是正常的"），**起笔划痕也是干燥假象**；但 **「来」第一笔是实打实没写上（非干燥）**、**结构不完整与位置不对"都是真实存在的"**。用户取向：**"时间够，质量优先"、"尽量只走软件"**。
  - **⚠ 方法论铁律（钉牢）**：**水写布的照片对"墨量"没有解释力**——"少水"与"干了"产生同一种照片（同形歧义）⇒ 任何"淡/浓/珠/墨量"的结论**只能在湿照/湿时观察上成立**，干照只能判"存在/不存在"的结构。我的 §3"驻留上墨/窜行不上墨"**降级为"未判定"**（对方要"整条作废"，我只接受"照片不能证"，因为风干与少水不互斥、且比赛用真墨）；**一次性判决＝同文本再写一遍、写完立刻湿照或拍视频**（零代码，尚未做）。
  - **✅ 字库对账（我方新增，介质无关）**：复刻 `generateSingleCharTrajectory` 变换（bbox 归一→S=70→居中 dcx/dcy→`rotate_local`→+cell offset）与审计 `target_pose` 逐笔比 ⇒ **7 字 60 笔 Δstart = Δend = 0.000mm，各字 offset 离散度 0.000** ⇒ **软件链不产生整笔位移、不缩短笔画**（旁证：`rotate_local` 在 orient=0 是恒等 `default:break`；`filterDuplicatePoints` 要求 pen 状态相同 ⇒ **不会吃掉笔画起点的落笔点**）⇒ **"位置不对"不在软件侧**（只剩字库设计／执行／介质），**"来首笔缺失"只剩执行侧 ⇒ 唯一候选＝151ms 字首大跳 ⇒ D1 唯一化**。
  - **✅ RDP 直组 2.1mm 的形状偏差已量化（＝对方问"形状保真是不是 D2 的残留价值"，坐实为"是"）**：每笔字库中线顶点到命令折线的最大距离 —— 全 60 笔 **中位 0.00 / p90 1.72 / 最大 2.08mm**；**>0.8mm 有 22/60 笔**，>1.5mm 有 6 笔；**偏差最大的两笔恰是"碧的王竖 2.08mm"与"碧的白竖 1.97mm"＝用户点名的"王短竖写短/白横折竖画写短"**（但弦与弧**端点相同**，不能据此断言"写短"，只能说"这是软件侧唯一可测的真实形状偏差、≈一个笔宽"）。机理：两段式分类的**第一段用 2.1mm 判直线** ⇒ "带弧的竖/横"被误判为直线、塌成 2 点弦；降容差即让它们走 0.1mm 曲线分支。**离线代价（复刻时间线，绝对量偏高 ~18%，只看增量）**：2.1→**0.8 = +6.9%（+12.5s/行）、命令点 372→426**；→0.5 仅再 +1.4s、→0.35 饱和（故取 0.8，不取 0.35）。
  - **✅ 关掉 bugs.md 悬案**：命令坐标 vs 照片 ⇒ **世界 +y = 纸面向上、字库 y 向上、代码不翻转 ⇒ 字形上下不镜像**（bugs.md「HanziWriter y 轴向下…待确认」可结）。
  - **本批建议（已写进第 2 轮回复）**：①**先做湿照/视频**（零代码，判决"走行少水 vs 干燥"，同时看溪前两点/来首笔/门点——**干照判不了这三条**）；②**D1** 每字预定位（去掉 `ci==0` 闸门）+ 注释钉 `move_up_to_and_wait` 的 guard 含 z_settle（27→250 后 guard 悄悄 +223ms）；③**D2 保留但改名"直组容差形状保真"**、2.1→0.8；④**D3**（readPose 写审计）在"执行侧"分支下升为关键、在"字库设计"分支下降为可选；⑤**不做** z_settle/C/速度档/弯组/P-3。判据：位置类用我方出的**字库骨架图**（`_zoom3/font_skeleton.png`，按纸面朝向翻转、标笔序；"机器画的就是这张图"）对照片——一致 ⇒ 字库/期望差异（软件改不了，只能换字库或做"逐笔人工微调偏移"新功能）；不一致 ⇒ 执行侧。
  - **D1 的机制预测（可证伪）**：字首大跳欠预算 + 首笔为"单指令笔画"两条同时满足才受损 ⇒ **来（34.0mm/2点）已被证真丢 ✓；溪（12.5mm、10.6mm 两笔都是 2 点）应同样受损**（用户报的"三点/撇写到一起"正在那一片）；**上（59.5mm/5 点，可自愈）用户确实没抱怨 ✓ 自洽**。
  - **我方工具（`D:/Code/华五/_zoom3/`）**：`font_vs_audit.py`（字库↔审计逐笔对账）、`font_skeleton.png`/`font_skeleton2.png`（骨架图）、`probe_strokes.py`（照片逐笔采墨——**本轮已判作废**：水写布照片对墨量无解释力，不必再修）、`static_error_field.py`（用照片墨迹 bbox 中心拟合静态误差场——**同样不可信**：墨迹外框取决于"哪一段干了"，量出的残差（来 +3.9mm 等）不能当位置误差；位置测量必须湿测）、`bi_shi.png`/`chui_vert.png`/`xian_dot.png` 等 4x 放大图。
11. **第 3 轮（用户三条新输入 + 我方两条新代码事实）** —— 我方第 3 轮回复：`.../mucqjq4ir3q11afi/outputs/笔画质量-第3轮-复核方回复-本项目agent.md`。
  - **用户新输入**：①时间＝"字数少，时间够"（我上轮"60字>1200s"的警告对**实际工作量**不成立，但正式赛变字数仍需复算）；②位置类＝**"机器走偏，查执行。"**；③湿时观察——**来第一笔"没写上，有写的动作但笔提前提起来了"**（＝抬笔/时序抢掉落墨）；**门点"角度很平且靠右了，和横折连一块了"**；**溪右两点"都写上了，但写到了上面的撇上"**。
  - **⚠ 新代码事实 A：每个笔画的"下降点"白拿 80ms 伪拐角停顿**（`motion.cpp:307-315`）——拐角判据里的 `k` 是 `filtered` **绝对下标**，而下降点 `down@p0` 的 `k-1` 正是同 XY 的 `UP@p0`，`angle_between` 遇 `lu<1e-6` **直接 return 0** ⇒ `turn = π-0 = 180° > 75°` ⇒ `+hard_corner_ms(80)`。⇒ **下降点实际预算 = `Cfix+z_settle+80`（直 415 / 弯 355），抬笔点只有 `Cfix+z_settle`（直 335 / 弯 275）**。这**解开了上一轮我记的"+86ms 未建模"**（80+抖动），也说明**"Z 预算 = Cfix+z_settle"这条口径不准**；且起笔静压比以为的多 80ms（≈300ms 量级，"起笔墨团"的潜在来源，本轮只记账）。
  - **⚠ 新代码事实 B（本轮最有价值的机制候选）：笔间移动的预算余量只有 ~55ms、且可能为负** —— 两笔之间 `UP@pe → UP@p0'` 的预算 = `max(12, d/128·1000+85)`，而 `PEN_UP_MM_PER_SEC=128`（`motion.cpp:99`）**是我们自己的计时常数、不是设备实测值**；上一轮从用户观察反推的设备真实抬笔速度 ≲130mm/s ⇒ 余量 = `85 - 加速~30 - d(1000/v_dev-1000/128)`：v_dev=100 ⇒ **d>25mm 的移动会拖墨**；v_dev=80 ⇒ d>12mm；v_dev=70 ⇒ d>8.5mm。⇒ **若 v_dev ≲110mm/s，字内"一笔到下一笔"的长移动会在没走完时被下一条指令接管 ⇒ 笔在飞行中下扎 ⇒ 墨从上一笔被拖到这一笔的起点**，正是用户看到的"点与横折连一块""点写到撇上"，也解释"角度很平"（拖出的墨沿移动方向）。〔可证伪〕**`PEN_UP_MM_PER_SEC 128→64`（一个常量）** ⇒ 若连笔显著减少则机制成立。
  - **✅ 仪器修正（两条，都影响对方方案）**：①**控制台"调试5 方框批量测试"不能用于几何/位置测试**——它走 `sendPointsBatch7` = 寄存器 **`0x0064`**（`robot_common.h:138` `REG_BATCH7_START`，COUNT `0x0023`、BYTES `0x46`，与手册 §6.2 示例帧逐字节同构），而手册把 `0x0064` 定义为**固定抓放宏**（非轨迹），真机上可能执行抓放动作；**调试2「L形」走 `transmitTrajectoryWithSplit(traj, traj.size())` ⇒ 全量按书法逐点 `0x0008` ⇒ 安全可用**。②**`calib.json` 里没有任何标定数据**（只有 `dev_limit/ink_station/layout_center/probe/safe_area`）⇒ 对方"接入 calib.json 施加坐标变换"那条**撤销**（代码侧只有 main.cpp 菜单 8 的占位提示；gui_trail 的同名 `calibToJson` 是 `robot_config.json` 的 `corners`，不是一回事）。
  - **✅ 排除静态标定的硬理由**：静态误差（尺度/旋转/剪切/delta 标定误差）都是**工作区坐标的平滑函数** ⇒ 70mm 一个字内相邻几毫米的两笔误差几乎相同 ⇒ **静态标定只能整体平移/旋转一个字，不可能只把"点"挪到"横折"上** ⇒ 用户三条抱怨（字内、笔间、相对）**在构造上排除标定** ⇒ "查执行"要查**动态**（时序/飞行中下扎/拖墨），不是标定/尺度。
  - **✅ 字库客观几何（给用户拿尺子对照，零上机）**：**闲门点**＝长 11.3mm、走向 **−44°**、**距横折起点 9.0mm**、距门竖 15.3mm；**溪爫三点（5/6/7笔）到撇（4笔）的最小距离 = 6.7 / 4.5 / 7.2mm**；**来短撇（3笔）y 17.8~28.1，顶横 y 30.4~34.0，第二横 y 7.8~12.2**（字库里短撇**居中**）⇒ 三条抱怨的"被吃掉/挪动"量级都是 **4~9mm**，与"拖墨"同数量级、与"字内≈0 的静态误差"不符。
  - **本轮建议（已写进第 3 轮回复 §6）**：**R1** `PEN_UP_MM_PER_SEC 128→64`（治笔间拖墨，+6~10s/行，可一键回退）→ **R2 D1 每字预定位**（治来首笔）→ **R3 湿时点阵静差测试**（GUI 四角页填**任意坐标**+「预览」= 单条 `0x0008` 落点、不污染标定 ⇒ 3×3 点阵，量列距/行距/正交/位置相关性，**必须湿时量**）→ **R4 D2 直组 0.8**；**不做** z_settle/C/速度档/弯组/P-3/D3 当位置仪器/调试5 做几何测试。
  - **方法论（第三轮钉牢）**：**审计永远看不见"设备有没有走完"**（ACK 是"寄存器已写入"的即时应答）⇒ 任何"动态错位/拖墨"假设**都无法从审计证伪**，只能靠**物理 A/B（改参数看纸面）**；而干照/干布对墨量同样无解释力 ⇒ **凡涉及"淡/浓/连/偏移量"的判读，一律湿时看**。
12. **第 4 轮（对方复核我的两条事实 + 我修正两处）** —— 我方第 4 轮回复：`.../mucqjq4ir3q11afi/outputs/笔画质量-第4轮-复核方回复-本项目agent.md`。
  - **对方已独立复核并收回两条**：`calib.json` 无标定数据 ✓、调试5 走 `0x0064` 抓放宏 ✓（其"接入 calib.json""方框用调试5"两条作废）；接受我 §4.1（静态标定不可能造成字内错位）与 §4.2（伪 80ms），并把 §4.3 定为**首选机制但"有条件接受"**，提出两个质疑：**①循环论证**（v_dev 是我反推的）、**②平权竞争假设"抬笔净空不足"**。
  - **我接受循环论证的批评**：R1 不写死 64，改"**先测 X 再定值**"。**但 M0 必须换仪器**：**秒表法已经失败过一次**（context §7b：byte1–5 的 320mm 单指令全落 0.4~0.6s，被人反应时间 ±0.2s 吃掉）⇒ 改用 **①手机慢动作视频 240fps（首选、零代码）**：一次同时量 **X（XY 巡航）、Z 升降用时、抬笔时笔尖是否真离纸**；**②可选 ~20 行"运动中轮询 0x03"**（更严谨、顺带结掉 readPose 语义 ⇒ 判"内部插补流"还是"回显"）；③秒表仅作兜底且必须重复 5 次取最小。
  - **净空假设：接受为活候选 + 两处修正**：①**静态部分不可行**——`Z_UP=-325`、平面 `-353` ⇒ 净空 **28mm**（本轮审计实际出现过的 Z 只有 -325/-352/-353），布不可能凸 28mm、笔尖不可能垂 28mm；②**"抬高 Z_UP"基本无杠杆**——设备 Z 上限约 **-320**（`z_limits=[-320,-410]`；"-310 及以上 ACK 但不动"）⇒ Z_UP=-325 只剩 **~5mm** 余量 ⇒ 它实际是"**Z 上升速度**"问题，抓手＝**加 Z 预算 / 改浅书写平面**（代价＝墨更淡）。
  - **✅ 本轮主要新增（零上机）：用"移动长度"当判别变量**，并算出**每笔的"起点移动距离"**（上笔末点→本笔起点，抬笔态）：字内 **>25mm 31 处 / 12~25mm 15 处 / <12mm 7 处**；逐字（首笔后）：闲 20/69.3/45.8/24/32/28.5、来 35.1/24.3/34.6/48/32.8/39.8、垂 11.9/24.4/39.6/56.3/36.9/35/45、钓 29.5/27.1/30.7/17.9/55.5/10.9/27.7、碧 25.2/12.6/14.5/29.6/6/19.9/14.7/14.3/40.8/30.4/31/14.9/18.5、溪 19.9/38.2/51.2/**6.7/11.0**/16.7/11.1/13.1/6.1/39.9/32.9/29.8、上 27.9/65.9。**三套假设的预测互不相同**：抢占 ⇒ 只在 >25mm 处长出短尾；净空/Z 上升 ⇒ 每次抬笔都有贯穿细线；静态近邻+扩散 ⇒ 只在字库本就靠得很近的两笔间"。⇒ 湿时只看三类代表位置（长移动：闲3笔69.3/垂5笔56.3/上3笔65.9；短移动对照：**溪 5/6/7 笔 6.7/11.0/16.7 = 用户报的"写到撇上"那两点**；中等对照：碧3笔12.6/钓6笔10.9）。
  - **✅ 溪那一条我判"与两者都无关"（重要）**：字库把溪三点放在**距撇仅 4.5~7.2mm**，而我们的**驻留墨团实测宽 5~6mm**（context §126 宽斑 24~30px）⇒ **墨团本身就能把 4.5~7mm 缝填上**，用户"两点都写上了"也支持它 ⇒ **任何时序改动都治不了**，能治的只有：减小驻留、改浅平面/减墨、换字库。**且它同时是"抢占"假设的反例风险**：若 6.7~11mm 短移动处也连 ⇒ 抢占被反证。
  - **✅ 免费红利 R5（新）**：把伪 80ms 去掉（拐角判据跳过退化角：要求两邻点距离 > eps 才算转角）⇒ **下降点静压少 80ms（起笔墨团缩 ~20%）且结构零风险**——context §9d 已证 `z_settle=150`（当时下降预算 85+150+80=315）结构已完整，现 250+80=415，去掉 80 仍 335 > 315 ⇒ 有 20ms 余量。**变量隔离**：与 R1 都动"时间"（一个 Z 侧、一个 XY 侧），判读指标不同（R5 看起笔墨团、R1 看笔缝拖线）⇒ 建议 R5 排在 R1 判读之后。
  - **收敛计划（第四轮定稿）**：**M0'（慢动作视频，判 X/Z/净空）→ R3 湿测点阵 + §3 三类位置 → 据 M0' 定 R1 值（`PEN_UP=X·0.8`）→ R1 + R2(D1) 同批 → 湿判 → R5 → R4(D2 直组 0.8)**；若视频显示"抬笔时笔尖根本没离纸" ⇒ **先做改浅平面**而不是 R1。不做：z_settle/C/速度档/弯组/P-3/调试5/calib.json/D3 当位置仪器。
13. **第 5 轮（对方收口 + 我方三处补丁）= 本轮讨论收敛** —— 我方第 5 轮回复：`.../mucqjq4ir3q11afi/outputs/笔画质量-第5轮-复核方回复-本项目agent.md`；对方收口稿：`笔画质量-第5轮-本项目agent收口.md`。
  - **对方全盘接受我第 4 轮的修正**（M0 换仪器、净空只剩"Z 上升未完成"、移动长度判别、溪/门=短缝墨团涨团），并**重排优先级**：**主力＝D1（来首笔）+ R5（去伪 80ms 缩墨团）+ M0'/R3（测量）**；**R1 降为条件项**（只在 M0' 证实 X<128 且 R3 见长移动拖线时才上）——理由：R1 靶向的"长移动拖墨"**没有用户点名证据**，而来首笔与短缝粘连都有直接证据 ✓ 我同意。
  - **⚠ 我方补丁 ①：门点那一条没有闭合（挂账）**——**"角度很平"没有任何候选机制解释得了**（墨团解释"连一块"不解释"平"；抢占拖尾方向是向左的 20mm 移动；字库里门点本身是 11.3mm/−44° 的斜笔）⇒ 湿时判一句话：那一处是**两个独立墨痕（点 + 横折起笔）还是一个**；若其实是"点没上墨、看到的是横折起点墨团" ⇒ 与"来首笔"同族（首笔不上墨），只是闲有锚点保护、机理待查。
  - **⚠ 我方补丁 ②（更正我上一份的算错）：R5 的余量只有 20ms，不是 100ms；且 R5 后 `z_settle` 下限从 150 抬到 230**。推导：test5 那批"结构够用"是在 **z_settle=150 且伪 80 已存在**时得到的 ⇒ 当时真正够用的下降预算 = 85+150+80 = **315ms**（与实测直组 324 吻合）；现 250 含 80 = 415（实测 421 ✓）；R5 去掉 80 ⇒ 85+250 = **335**，余 **20ms** ⇒ **可做但必须盯结构**；等价条件改写为 **`z_settle ≥ 230`**（旧的"≥150"是含伪 80 的口径，以后不许再当安全线）；R5 后若见截断 ⇒ **先把 z_settle 提回 250~300，别动 C**。
  - **✅ 我方补丁 ③（新，正面）：字号 70→90 是"粘连"族唯一真正有效且纯软件的杠杆**。依据：本轮照片实测**线宽 ≈2~3mm**（4x 图 10~15px @4.8px/mm）、**驻留墨团宽 ≈5~6mm**（与 §126 的宽斑 24~30px 同量级）⇒ **粘连的元凶是墨团（不随字形缩放）而非线宽**；字号 70→90 时溪三点距撇 **4.5/6.7/7.2 → 5.8/8.6/9.3mm**，墨团仍 5~6mm（R5 后 4~5mm）⇒ **缝 > 墨团，粘连在数量上被解开**。约束：**每页 4→3 字**（4×90=360 > 可达宽 324；3×90=270 ✓）、页数 +33%（翻页仍是模拟 ⇒ 真机蓝牙到位前页数多更麻烦）、每字用时 ×1.29（与 D2 叠加 ≈ +38%，22→~30s/字；用户"字数少"可接受，正式赛字数一多须重算）。**字号键 = `free_char_size`（不是 `layout_char_size`）。对用户的口径：粘连族软件能做的两件＝R5（缩墨团）+ 把字写大（拉笔缝）；再往下是墨量/笔尖/纸，不是软件。**
  - **M0'/R3 执行细节（防白跑）**：①**视频与审计必须配对**（手机固定、拍同一次书写、保留该次审计 ⇒ 用坐标+时间戳做帧计数，X 与 Z 升降用时都能出数）；②**点阵必须"点—回中心—点"**（`preview_corner` 会把笔停在书写平面 ⇒ 连着点会画线；回中心 −320 比平面高 33mm 会抬起；**每点附近那一小段拖尾是协议产物——量点心不量尾**）；③**一切判读（点阵静差、§3 三类位置、门点两痕/一痕）都在湿时一次做完**，干照不作数。
  - **状态：本轮讨论收敛**；剩余不确定项全在"必须上机测一次"的集合里（X、Z 升降用时、抬笔是否离纸、连笔属涨团还是拖线、碧/钓/垂 长移动处有无拖线）。**门点一项挂账待湿照判读。**
14. **第 6 轮（D1+R5 真机落地 → 我方复核，查出变量污染）** —— 我方第 6 轮回复：`.../mucqjq4ir3q11afi/outputs/笔画质量-第6轮-复核方回复-本项目agent.md`；对方回报稿：`笔画质量-第6轮-本项目agent回复复核方.md`。
  - **代码状态（对方已落地）**：**D1** = `gui_service.cpp:1209` 去掉 `ci==0` 闸门（每字预定位）；**R5** = `motion.cpp:306` 拐角判据跳过退化角（去掉下降点伪 +80ms）。双 exe RC=0、dryrun 双判据坐实。
  - **✅ 我方独立验证（parse `logs/audit_20260923_172853.jsonl`）**：**D1** ⇒ 7 个字首的锚点等待 = 2236/1488/1718/1692/3087/1581/1675ms（改前仅每页首字有、其余 150ms）；**R5** ⇒ 60 个下降点间隔中位 **342(直)/281(弯)** = `Cfix+z_settle`(=335/275) + 抖动 ⇒ **伪 80 已消除** ✓。用户反馈：**闲点解决 ✓、来首笔未再提（推定修好）✓、其余"没有改善"**。
  - **⚠⚠ 本轮关键发现：test6 真机不是单变量——书写平面被改成 −350**（基线 −353；审计里 17:31:41 设 −351.5、17:32:53 设 −350，ok 的那次是 −350；中间那次 115 点 `estop triggered`→`failed/send`）。⇒ **"其余问题未改善"不能归给 D1/R5**；且**浅 3mm 对两组抱怨作用相反**（对粘连=好转、对"写短/不封口"=更差）。**上一晚 09-23 00:05 有一次 D1+R5 + 平面 −353 的运行（384 点/158.2s）但没有照片** ⇒ "同代码+基线平面"这一格数据缺失，须先补。
  - **✅ 归类改判（我方主张）：按"墨多 / 墨少"分，不是按"粘连 / 写短"分**：
    - **墨多（粘连）**：**溪三点连撇**（字库缝 4.5/6.7/7.2mm）、**来 撇/点 贴顶横**（缝 2.3 / 6.8mm）⇒ 字号 70→90 使缝 ×1.29 ⇒ 有帮助（来 2.3→3.0mm 帮助有限）；平面浅一点也有帮助。
    - **墨少**：**石没封口** ⇒ **从对方主张的 A 类改判过来**：字库「口」左下角缝 **5.4mm**（端点 6.0）、右下 2.6mm、左上 2.2mm ⇒ 左下角只能靠**驻留墨团（~5~6mm）**填；**R5 把墨团缩 ~20% 正好可能把这一角从"勉强封住"变成"露 1~1.5mm 缝"**（很可能是"没封口"的真因）；**字号放大到 90 会让缝变 6.9mm 而墨团不跟着长 ⇒ 更不封口** ⇒ **"字号 90 复跑"不能拿"石有没有封口"当成功判据**。
    - **墨少（画不满）**：**垂竖短**（命令末端 y=−17.1，第 7 横 y=−7.9 ⇒ 缺最后 **9.2mm**，该段周围 18.4mm 内无其它笔画）、**钓竖提短**（端点 (112.3,−8.2)，最近勺笔画 19.4mm 外）⇒ **与粘连无关**。
  - **✅ B2 否掉（我方算的）**：笔画**末端**的预算 = `max(12,0+Cfix)+z_settle` = **335(直)/275(弯)ms**，与笔内其它点同量级（8mm 段 370ms、10mm 段 442ms），按每毫米算末端更"富" ⇒ **末端是全笔最不缺驻留的地方**（还叠加 ±28mm 的 Z 停顿）⇒ "末端缺墨"不可能由"抬笔太早"造成 ⇒ **`stroke_end_ms` 不是 B 组候选**；B 只剩 **B1（臂没走到）/ B3（沿途接触失败）**，而**平面被改浅 3mm 使 B3 更可疑**（布面局部的低点会让笔尖失去接触 ⇒ 正好表现"长竖下半段/末端没墨"，与在册的「局部接触不足」假设一致）。
  - **✅ 本轮最省的新实验：单指令长线「平面阶梯 × 字节阶梯」**——控制台 **调试8**（逐行输入 X Y Z 落笔(1/0)）输入两点即可画**一条 65mm 竖线**（照抄垂竖的位置/长度）：同一条线在 **Z=−348/−350/−353/−356** 各画一次，再在**速度档 1/3/5** 各画一次（每次只改一个量）⇒ 一条指令、无后续指令、无预定位 ⇒ **B1/B3 与"时序"彻底分开**，同时把"字节对墨量的影响"从整字噪声里隔离出来（9-22 起一直没做成的判决实验的干净版）。**禁用调试5（`0x0064` 抓放宏）**。
  - **下一批四件套（我方建议顺序）**：**①同代码+平面 −353 对照跑（补缺格）→ ②单指令长线平面/字节阶梯（最省，定 B1/B3）→ ③R3 点阵静差 + 三类位置 → ④字号 90（只判溪/来，不用石判）**；慢动作视频降为可选。判读表：同一线在 −348/−350 断、−353/−356 连续 ⇒ **B3（改深平面）**；四种 Z 都画不满 ⇒ **B1 无软件解**；四种 Z 都完整但画字仍断 ⇒ 才回到时序；同 Z 下字节间墨量差明显 ⇒ 字节是有效墨量旋钮。
15. **第 7 轮（用户裁决 → 计划定稿）** —— 我方第 7 轮回复：`.../mucqjq4ir3q11afi/outputs/笔画质量-第7轮-复核方回复-本项目agent.md`；对方收口稿：`笔画质量-第7轮-本项目agent收口.md`。
  - **🔴 用户裁决（决定性）**：**"350 平面是当前最佳写字面，不影响书写，不考虑影响。"** ⇒ **平面定在 −350、不再作为变量** ⇒ **删掉"新跑 −353 对照"与"平面阶梯 −348/−350/−353/−356"两格**（按此裁决跑就是白跑）。以后平面只作**背景条件**记录（每次跑顺手看一眼 `set_writing_plane`），不再设计实验。
  - **✅ 核销一条**：`audit_20260923_000529.jsonl` 的 `mode = DRYRUN`（对方纠正成立）⇒ 那是对方跑 D1+R5 的 **dryrun**，**不是"丢了照片的真机 −353 跑"**；我上一轮"缺这一格"的表述作废，且用户裁决后也不必补。
  - **✅ 新增/定稿的零代码实验（正对"封口 vs 粘连"的同一个旋钮）**：**`stroke_begin_ms` 阶梯 0 → 30 → 60**。位置与理由：`robot_common.h:117` + `motion.cpp:126`（`if (!zDownPrev && zDownNow) extra += get_STROKE_BEGIN_DWELL_MS()`）⇒ **它加的正是"笔画起点（下降那一刀）"的驻留，与 R5 去掉的伪 80ms 同位置** ⇒ 零代码、单变量、可回退、config 键现为 0。
    - **双赢区间的算账（本轮新增，定量解释"闲点解决但石不封口"）**：**石「口」左下角缝 = 5.4mm** ⇒ 两侧墨团半宽各需 **≥2.7mm**；**闲点末端↔横折起点 = 9.0mm** ⇒ 要不连则各需 **<4.5mm** ⇒ **双赢区间 = 墨团半宽 [2.7, 4.5) mm**；实测墨团宽 5~6mm（半宽 ~2.5~3.0mm）⇒ **R5 把半宽从 ~3.0 推到 ~2.5 ⇒ 掉出下界** ⇒ 与用户"闲点解决 ✓ / 石没封口 ✗"完全吻合 ⇒ 预测补回 **30~60ms** 起点驻留即可落回区间。
    - **判据三向（同一张湿照一次看完）**：①石左下角**封住** ②闲点↔横折**不连**（保住 R5 战果）③溪三点↔撇**不连**（"墨多"组的上界）。三向全满足 ⇒ 即工作点、不必动字号；不能同时满足 ⇒ **物理下限确认**，才轮到字号 90，且必须同时说明**字号 90 帮溪/来、害石**（石缝 5.4→6.9mm）。
    - **不碰 `stroke_end_ms`**：它在**末端**，而末端墨团本轮没缩过（R5 只缩**起点**墨团），且它治不了"末端缺墨"（B2 已否）。
  - **✅ ② 改为"纯字节阶梯"**：一条 **65mm 单指令竖线**（照垂竖位置/长度、Z=−350），**档 1/3/5 各一条**（只换字节）⇒ 判 **(a) 线长是否 = 65mm（臂到没到 = B1）**、**(b) 三条线的墨量/线宽差（字节的墨量效应）** = 9-22 起没做成的判决实验的干净版（单条指令、无抢占、无整字噪声）。
  - **R5 的精确边界（对对方的补正）**：R5 只改**下降点**（笔画**起点**）的驻留 ⇒ **只缩起点墨团**；笔画**末端**（抬笔侧）驻留未变 ⇒ 石左下角两个墨团里只有"底横起点"那个缩了 ⇒ 若"石没封口"真是 R5 引起，力度只有一半。
  - **第 7 轮锁定版执行计划（顺序）**：**②（调试8 单指令线 · 字节阶梯）→ ③（`stroke_begin_ms` 阶梯 0/30/60，三向判据）→ ④（R3 点阵静差）→ ⑤（字号 90，只判溪/来）**；② 若三档线长都明显 <65mm ⇒ **B1 坐实、B 组无软件解**（转厂商/机械、留档），且 ③ 对 B 组无效。
16. **第 8 轮（最终收口）= 本轮讨论结束，移交用户执行** —— 我方第 8 轮回复：`.../mucqjq4ir3q11afi/outputs/笔画质量-第8轮-复核方回复-本项目agent.md`；对方最终收口稿：`笔画质量-第8轮-本项目agent最终收口.md`。
  - **双方对"机制分类 + 旋钮选型 + 上机判据"完全一致，无异议**。已结案（三重确认）：**D1（来首笔）、R5（闲点）**；`z_settle ≥ 230` 安全线已进注释、本轮无截断回归；y 轴/镜像、`calib.json` 无数据、调试5 禁用、D3 不作位置仪器。
  - **我方最后补的三条执行前置（漏一条这次上机就白跑，全部来自本项目历史教训）**：
    1. **③ 要手改 `robot_config.json` ⇒ 改之前必须先关 GUI**：`stroke_begin_ms` **在 GUI 里没有任何控件**（`gui_win32.cpp` 里 `stroke_begin_ms`/`stroke_end_ms`/`cold_start_min_ms`/`min_point_interval_ms` 四键都无 UI），只能手改 JSON；而 `gui_service.cpp` 有 **28 处 `cfg_save()`**，GUI 任一设置项变更就回写整个 config、覆盖手改值。⇒ 正确序列：**关 GUI → 改 JSON → 开 GUI → 写字 → 湿照**。另：config 按进程 CWD 解析 ⇒ **必须从项目根目录启动**。
    2. **② 用控制台跑 ⇒ 串口独占 ⇒ 同样先关 GUI**；控制台要输串口号（上次 **COM16**）；**串口打开失败会静默退到 DRYRUN ⇒ 布上不会留线**，执行者要确认没出现"[错误] 串口打开失败，自动切换 DRYRUN"。
    3. **② 三条线必须错开位置**（同一 Y、X 各错开 ~20mm）并**画在干布面**上、**一张照片拍全三条**（context §11b 的老规矩）。
  - **② 的确切坐标（我方从字库算好，执行者直接抄）**：垂·长竖 = **(26.0, 36.0) → (27.5, −17.1)**，**直线长 53.1mm**（字库路径 55.8mm，因中间有 4 点；单指令版即这条 53.1mm 直线）。调试8 逐行输入（落笔=1，Z=−350）：`26.0 36.0 -350 1` / `27.5 -17.1 -350 1`（档1）；X 各 +20mm 得第 2、3 条（档3、档5）。**量纸上实长 vs 53.1mm**（短 >2~3mm ⇒ B1 臂没到），三档墨量/线宽在同一张照片横比。可选第二条：钓·钅竖提 **(95.7, 12.5) → (112.3, −8.2)**（直线 26.5mm；原笔是"竖+提"带折、路径 47.3mm）。
  - **③ 执行时的补充**：**三档都要拍湿照**（否则"三向冲突"无法证明）；三向冲突 ⇒ 物理下限确认，才走 ⑤，并交代 **字号 90 帮溪/来、害石**（石缝 5.4→6.9mm）。
  - **待用户执行后回报**：②/③ 结果 + 审计 + 湿照；若与分流表不符（例如线长正常但整字仍断），我方再算。
17. **第 9 轮（用户要求"全链重开"：从确定坐标到落笔逐环理清）** —— 我方第 9 轮回复：`.../mucqjq4ir3q11afi/outputs/笔画质量-第9轮-复核方回复-本项目agent.md`；对方重开稿：`笔画质量-第9轮-本项目agent重开.md`。
  - **🔴 本轮最重要更正：② 的真机证据不存在**。坐标级扫全部 85 个审计（今天 30 个）：`(26.0,36.0)→(27.5,−17.1)` 只见于 `audit_20260923_185822.jsonl`（18:58，档3）与 `(46.0,36.0)→(47.5,−17.1)` 于 `audit_20260923_185954.jsonl`（18:59，档1），**两条 `mode=OFFLINE`（dryrun）、`source=CONSOLE`**；档5 无记录。真机（`mode=REAL`）18:41:33~18:45:05 跑的是**另一组**：中心↔`(±120,±60)/(±120,0)/(0,±60)/(0,0)` 共 9 点、60~134mm、**全部档3**。⇒ 对方"② 单线画满 ⇒ 排除臂程"的前提不成立；**② 与 ④ 的"满/准"都发生在人手间隔 4.1~21.4s 的预算下**（我逐点量了 ④ 的 9 个间隔：最小 4.112s），而任务里长笔每点预算只有 1.3~1.8s ⇒ **"唯一差别是有没有别的指令"漏掉了混淆变量"时间"**。
  - **④ 的仪器已确认**：3×3 点阵用 GUI `preview_corner`（`gui_service.cpp:627`，单条 0x0008、Z=书写平面、pen=false）+ `preview_writing_plane(-320)`（`:587`）回中心抬笔 ⇒ 每个点附近那一小段斜向拖尾是协议产物（量点心不量尾）。③ 三档成立：首个下降点预算 339/371/401ms = 85+250+0/30/60。
  - **✅ 全链 12 环表（我方新增，本轮主体）**：文本→字库(hanzi.cpp:58)→逐笔点(`:73`，平面有效全字同 z `:92`)→缩放居中(`:78`+gui_service.cpp:1196-1212)→朝向(rotate_local)→每字预定位(motion.cpp:413)→去重(`:27`)→两段式 RDP+分类(`:186-197`，决定 Cfix 85/25)→**每点预算(motion.cpp:117-138)**→补偿等待(`:237`)→组帧/字节(serial_port.cpp:76)→**控制器执行语义**→物理落墨。**仅有的两个从未被正面测量的环 = 第 8 环（v=28 常数）与第 11 环（0x0008 是队列还是覆盖）**。
  - **✅ 垂竖短的毫米级算术（本轮核心）**：③0ms 那一笔（命令 53.1mm 直线、路径 55.76mm）逐点预算 = P1 4.26mm/231ms、P2 5.22/214、**P3 42.35/1553（需 27.3mm/s，模型 28 ⇒ 单段余量 2.6%）**、P4 1.45/90、P5 2.48/121 ⇒ 整笔等效 **55.76mm÷2.209s = 25.2mm/s**（含下降 278ms 则 22.4）。照片实测 ③0ms 垂竖末端缺口 **≈6.5mm**（两根横间距 11.9mm/65px 定标 ⇒ 5.5px/mm，±1.5mm）⇒ 反推设备等效速度 **≈23mm/s**（v=25 ⇒ 缺口 3.6mm；v=22 ⇒ 8.2mm）⇒ **第 8 环常数偏乐观**，可改 `PEN_DOWN 28→22`（整页 +15~25%），**但改前必须实测真速度**。
  - **⚠ 旋钮更正（对方 §5-3 想要的"R1 升回 T 组主力"是错的）**：长竖长度只由**落笔段**预算决定（竖的末点由我们发，下一条命令才是抬笔，间隔由 `cur.isPenDown=true` ⇒ v=28 给）⇒ **改 `PEN_UP`(128) 对竖长度一分钱不加**；抬笔欠程是**另一种缺陷**（下一笔下降时臂还在路上 ⇒ 斜着入纸、起点漂移/拖线）⇒ R1 应另立"起点精度"项。
  - **⚠ G 组改判据**：我算出字库骨架最小间距（medians 折线点↔线段，S=max(bbox) 归一）——**来 短撇↔顶横 6.61/8.50、来 短捺↔顶横 5.50/7.08、溪 上点↔奚首撇 6.43/8.27、碧 石·口左下 2.67/3.44（@70/@90）**；全字最小值 垂 1.41 / 来 1.60 / 钓 1.65 / 溪 1.75（@70）。字号 ×1.29 ⇒ 缝 ×1.29、墨迹宽不变 ⇒ 缝隙净增只 1.6~1.9mm ⇒ **"骨架重叠"与"笔宽过大"都预测 ⑤ 的阴性**，⑤ 分不开二者。**正确判据 = 湿时尺量墨迹宽 w vs 缝**：w ≥ 缝 ⇒ 物理必粘（软件无解）；w < 缝 ⇒ 才有软件空间。另：我复核 `⑤ 字号 90/0ms.jpg` —— 溪/来 在 90mm 下**是分得开的**，与对方"纹丝不动"不符（要求同字号并排复核）。
  - **③ 的覆盖范围（对方用它否定"时间论"，不成立）**：`stroke_begin_ms` 只加在"由抬到落"的那一个下降点（`motion.cpp:126`），**永不进长笔中段预算** ⇒ ③ 只能杀"起笔墨团"，碰不到"行进时间"。
  - **下一步三件（全部零代码/config，待用户上机）**：**A 湿时尺量墨迹宽 w**（判 G 有无软件空间）→ **B 调试8 真机单指令线 96mm（档 1/3/5、X 各错开 20mm、第二条点 1~3s 后打、协议含"原位下刀/原位抬笔"防拖线；v = 实画长度 ÷ 审计实测间隔）** → **C 单字「垂」×`point_fixed_curve_ms` 25/150/400**（竖长随值变长 ⇒ 坐实时间不足）。⑤D = 同字号并排复核 ③0ms vs ⑤0ms。
18. **第 10 轮（对方认账 + 我方点名一处异议、判据钉死）** —— 我方第 10 轮回复：`.../mucqjq4ir3q11afi/outputs/笔画质量-第10轮-复核方回复-本项目agent.md`；对方认账稿：`笔画质量-第10轮-本项目agent认账收敛.md`。
  - **对方认账三处**：② 是 OFFLINE（"排除臂程"撤回）；悖论真因是"时间预算"（非"有没有别的指令"）；T 组旋钮是 `PEN_DOWN` 非 `PEN_UP`；G 组弃"尺度不变"改测 w。并锁定 A/B/C；其主张 **"C 是唯一能同时判第 8 环与第 11 环的决定性实验，应排在 B 之前"** ⇒ 要我点名异议。
  - **🔴 我方点名异议（本轮唯一实质分歧）：C 不能单独判第 11 环**。C 的阴性只证明"**预算不是这个 6.5mm 缺口的原因**"，缺口仍可能是**接触/墨（第 12 环）或丢步（机械）**；"控制器是队列"只是其中一支。⇒ **第 11 环（队列 vs 抢占）的真正判据 = B 的"短间隔"那一格**：160mm 单线、第二点**尽快**打 —— 实画 ≈ v·Δ（明显短）⇒ **抢占/覆盖语义成立**（截尾可能，第 8 环有解）；实画**满长**（Δ 再短也满）⇒ **队列语义成立**（截尾不可能，"写短"只能查接触/丢步）。
  - **分工改定**：**B = 第 8 环的真值 v ＋ 第 11 环的队列/抢占**（正反都干净）；**C = 第 8 环"时间在任务路径里就是瓶颈"的机制坐实**；**B 先于 C**（v 是 C 的解释前提：若 v 远高于 28，C 的阴性就不再是"时间论出局"而是"时间本来就不缺"）。
  - **B 协议按"只变间隔"重排（比第 9 轮版干净）**：**线长改 160mm**（Y +80 → −80，仍在四角标定框内；Δ 受人手限制、线越长可测 v 上限越高）⇒ 档 3 固定、**Δ 阶梯 4 档（尽快 / ~1s / ~2s / ~5s，X=26）** + **档 1 与档 5 各一条（Δ≈1s，X=46/66）**；Δ 一律取**审计里"第 3 行 → 第 4 行"的实测时间差**（不用数秒）；判读只量**长度**、不判墨色深浅（字节/湿润度都在变）。
  - **C 的三条防误读细节**：①**审计先核预算真的涨了**（42.35mm 段 1553 → 1680(150) → 1930(400)ms；没涨=旋钮没生效，不是"时间无用"）；②判"竖长"用**结构判据**（末端是否越过倒数第二横 / 接到最后一横上方 ~3.7mm），**别量墨外沿**（驻留变长会让端点变胖、外沿假变长）；③单字「垂」+每页 1 字 + 先关 GUI 再改 JSON + 三档各拍**湿照**。
  - **A 与 ⑤D 并入同一次上机**：A 趁 C 某轮**还湿**用直尺量墨迹宽 w（同一次量 3 处取中位；干湿会变）⇒ 对第 9 轮 §3.2 缝表逐对判 `w ≥ 缝 ⇒ 物理必粘（软件无解）`；⑤D 把 ③0ms 与 ⑤0ms **缩放到同一字号**并排只看溪/来那几对缝。
  - **一次上机顺序（定稿）**：**B（6 条线 ~5min）→ C（3 轮 ~3min）→ A（趁湿尺量 w）→ ⑤D（回电脑并排）**；交付物 = **B/C 审计（确认 `mode=REAL`）+ B 六线照片 + C 三张湿照 + w 三个读数**。
19. **第 11 轮（对方最终锁定 + 出上机执行单 v2 → 我方勘误三处）= 讨论冻结、交用户上机** —— 我方第 11 轮回复：`.../mucqjq4ir3q11afi/outputs/笔画质量-第11轮-复核方回复-本项目agent.md`；对方锁定稿：`笔画质量-第11轮-本项目agent最终锁定.md`；对方执行单：`上机执行单-v2-ABC.md`（同目录）。
  - **对方全部接受第 10 轮的分工更正**（C 不能单独判第 11 环；B 判第 8+11 环、C 坐实机制；B 先于 C），并出**上机执行单 v2**（B 六条线：X=26 档3 × Δ{尽快/1/2/5s} + X=46/66 档 1/5 × Δ≈1s；C 单字「垂」×`point_fixed_curve_ms` 25/150/400；A 趁湿尺量 w；⑤D 同字号并排）。声明"讨论到此冻结，移交用户一次上机"。
  - **🔴 我方本轮唯一实质产出 = 执行单的三处勘误（不改就会白跑）**：
    1. **【必改】C 的"改 `page_text` 即写「垂」"不成立**：GUI 文本框预填来自 **`last_task.txt`**（`gui_win32.cpp:1242` → `gs::load_last_task_text()` `gui_service.cpp:1711`），书写用**文本框内容**（`gui_win32.cpp:1675` `gs::start_write(to_u8(t))`）；`page_text` 只在 `cfg_save` 记录（`:1472`）与**空页兜底**（`:1620-1623`）生效。当时 `last_task.txt` = **`溪来`** ⇒ 只改 JSON 会把「溪来」重写三遍。**正确写法：关 GUI → 改 JSON（`point_fixed_curve_ms` + `page_chars/page_count`）→ 开 GUI（项目根）→ 在文本框里输入「垂」→ 确认每页字数=1/总页数=1 → 书写**（`page_chars` 两框**吃** config：`snapI("cfg",…)` `gui_win32.cpp:1266/1271`）。
       **顺带定字号**：当前 `free_char_size=90`（⑤ 留下）⇒ 建议**设回 70**（可与 ③0ms 的缺口 ≈6.5mm 直接对比，预算判据 = 42.35mm 段 **1553→1680→1930ms**）；若坚持 90（信号更大：竖 68.3mm），该段变 54.4mm ⇒ 判据改 **≈1968→2093→2343ms**（按 ⑤ 审计 49.84mm→1818ms 实测比例复算）。**三档同字号，别动 `point_fixed_ms`。**
    2. **【必改】B 的"尽快"现实是 1.5~3s**：人手打一行要 1.5~3s，做不到 0.5~1s ⇒ 执行单改写"越快越好、**≤2s 都合格**"，别因手速重跑（Δ 由审计精确记录，判定不受影响：v≈23 时 Δ=2s ⇒ ~46mm、Δ=5s ⇒ ~115mm、队列 ⇒ 恒满）。
    3. **【补判读方向】字节档谁快谁慢**：手册「00 最快」+ `speedHigh=档−1`（`serial_port.cpp:85`）⇒ **档1=字节0=最快、档5=字节4=更慢**；若字节是速度旋钮，**线5(档1) 应比线2(档3) 长、线6(档5) 更短**（同 Δ）；三条一样长 ⇒ 字节与速度无关（也是结论）。**结论要落回档 3**（`"speed":3` ⇒ 字节 2）。
  - **两处确认无需改**：①红线文案与 `main.cpp:611` 原文一致（"[错误] 串口打开失败，自动切换 DRYRUN。/ Open failed -> switched to DRYRUN."），但**GUI 侧没有这句提示**（只表现为"不动"）⇒ REAL 判据一律以**审计 `mode`** 为准；②A 的缝表与 ⑤D 做法我复核一致。
  - **状态：讨论冻结**。我方等四份交付物（B 审计+六线照 / C 三档审计+三张湿照 / A 的 w 三个读数 / ⑤D 并排图）⇒ 算 v、判队列/抢占、给 `PEN_DOWN` 建议值与 G 组结论。
20. **第 12 轮（用户改裁决"不测 B、是软件问题" → 对方提统一假设；我方判分支 + 升级 C）** —— 我方第 12 轮回复：`.../mucqjq4ir3q11afi/outputs/笔画质量-第12轮-复核方回复-本项目agent.md`；对方提案：`笔画质量-第12轮-本项目agent提案-未改码.md`。
  - **用户新裁决（经对方转述）**：**剩余缺陷是软件问题、不测 B、没修好就是没找对地方**；双方收敛前不改码。对方**曾擅自动码、已全部回退**（`motion.cpp` 常数回 28/128、双 exe 重编）。〔我已复核〕`motion.cpp:98-99` = 28/128 ✓、全仓无残留旋钮（`grep pen_down|mm_per_sec` 只命中 UI 状态字段）✓、`Robot.exe`/`RobotGUI.exe` 时间戳 09-24 20:04/20:09 ✓、且用 ③ 审计反查行为基线（落笔预算 = `max(12, d/28·1000+Cfix)+extra`，42.35mm⇒1553ms）✓ ⇒ **运行态确为 28/128 基线**。
  - **对方提案**：一个"统一"假设——`PEN_DOWN=28/PEN_UP=128` 偏乐观（比设备真实速度给的时间少）+ 控制器抢占 ⇒ 落笔长段被截=**写短**、抬笔/字间移动没走完就下扎=**拖连（粘连）**；并自陈"不知道设备真速度是快于还是慢于 28"。
  - **我方三处判定**：
    1. **"统一"要去掉，粘连分两族**：**R5 只改"下降点驻留时长"、未动任何移动段预算，却把「闲点↔横折」治成分开**（真机+dryrun+双审计）⇒ **"点/短笔自己涨大"那族是驻留墨团**（抓手=驻留/平面/字号/笔尖），拖连族只覆盖**跨笔长移动**（抓手=PEN_UP/预定位）⇒ 合成一个常数故事会导致"改完常数误以为粘连也该好"。
    2. **⑤ 的阴性不判别机制**：墨团解释（缝×1.29 只净增 1.6~1.9mm、R5 后墨团半宽仍 ~2.5mm）与拖连解释（拖线跨整个缝）**都能解释它** ⇒ 不能当"拖连"的证据；判别只需 A（尺量 w）+⑤D（同尺度并排）。
    3. **C 升级为"机制 + v"双重判据（本轮最有价值的量化）**：按截尾模型 `实到=min(d, v·B)`，垂·竖 42.35mm 段的预算 1553→(150)1678→(400)1928ms ⇒ **预测缺口 ≈6.9 / 3.8 / 0 mm**（基线 6.9mm 与我照片量到的 ≈6.5mm 自洽）。**三分支**：≈6.9/3.8/0 ⇒ 截尾成立、**v≈23mm/s** ⇒ 下调 `PEN_DOWN`；三档基本一样 ⇒ 不是预算（转接触/机械/字库，**写死不再动计时常数**）；**6.9/0/0 ⇒ "每点固定损耗"型** ⇒ 抓手换成**减点数**（`rdp_tol_curve_mm` 0.1→0.3~0.5，5 点压到 2~3 点）而非降常数。判据用"末端还差几毫米"（±1.5mm），**别用"看着连上没"**（C 加驻留会让端点墨团变胖、伪造 1~2mm 连接）；三档同字号（建议 `free_char_size` 回 70 复用 ③0ms 对比与上述数字；留 90 则该段变长、判据换 ≈1968/2093/2343ms）。
  - **我方新增一格零代码对照：速度档 3 → 1**。依据：历史标定里**档1（字节 0）是唯一在"落笔 v=28"下被判干净的档**（`v_req∈[28,42)` 的夹逼来自"档1 干净/档2 残"），而**当前档 3（字节 2）从未验证过**，且 `estimateMoveMs` 不看字节 ⇒ 失配只可能落在档 3。判据：竖变长/画满 ⇒ 档 3 设备速度 <28 ⇒ 用档 1 **或**下调 `PEN_DOWN`（若档 1 带回串珠就只能后者）；竖不变 ⇒ 与字节无关 ⇒ 回到 C 的分支表。**两格同一次上机、互不污染**（一个 config 键、一个 GUI 档）。
  - **对 §4 四问的回答**：①候选接受但去"统一"；更正"我从不主张设备快于模型"（第 5 轮那句是对方的，我第 9 轮的 23 也是倒算）⇒ 姿态=不下结论、用效果型实验定方向；②C 认（并升级用法）；③**PEN_DOWN 做成可调同意，但**：放在 C 之后、作为独立一次改动、单变量验证（审计逐点对表 `d/v_new+Cfix` + 湿照）；**per-mm 才是"长度"的正确旋钮**，C 的 per-point 加量只当诊断（给每点都加驻留、短段被拖慢、涨团风险↑）；量化副作用：28→22 时 42mm 段仅多 ≈+80ms 余量、短段几乎不动；④**抬笔/字间归时间、不归静态净空**（28mm 静空第 4 轮已排除；剩下的只是"Z 上升没在下条 XY 前走完"）⇒ 抓手是 `PEN_UP`/Z 上升预算，**不是抬 Z_UP**（−325 距上限 −320 仅 ~5mm）；判据（不动设备）=湿照看拖线形态（贯穿整段=抬笔/Z 未完成；只在起点前一小段=抢占拖尾）+ 可证伪预测：**新笔起点会沿"上一笔末点→本笔起点"方向漂移**（可能同时解释石不封口/来撇贴横）。
  - **我方建议的下一步**：一次上机零代码三件（C 三档 + 速度档 A/B + A/⑤D）⇒ 按判决落**单个**改动（`PEN_DOWN` 提为 config 键初值 22 / 或换档 1 / 或减点数，**三选一不叠加**）⇒ 粘连族另立一步分开验证；**"不测 B"我接受**，只记风险：v 只能由 C 的缺口反算（±2~3mm/s，够用），最终取值是"试出来的"、每轮迭代要一次真机跑。

## 已知约束
比赛要求动物毛毛笔；正文每字至少 6x6 cm；按规范笔顺；书法和国画均需自主蘸墨至少一次，且蘸笔、提笔、沾墨动作要明显；一台机器人连接一台电脑；正式自动执行 20 分钟，期间不得远程或人工干预；场地桌面 1.5x1.2 m；文字现场输入且为常用汉字；纸张可自定但不能超出桌面。