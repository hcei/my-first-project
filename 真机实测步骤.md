# 真机实测步骤（机械臂 + 蓝牙翻页）

> 适用：`RobotGUI.exe` 从 DRYRUN 切到真机第一次实测。
> Dry Run 已经跑通（2026-09-21 21:01，审计 `logs/audit_20260921_210129.jsonl`，`task_end=ok`）。
> 本文按**风险从小到大**分 6 步，**每一步都要确认过再进下一步**。

---

## 0. 三条红线（先读）

1. **手放在电源/硬件急停旁边。** 软件急停**在串口等待期间不响应**，只在下一个发送点才检查——靠它来不及。
2. **Z 深度别越界。** 文档实测可动范围是 **-320 ~ -385**，但 `robot_config.json` 里 `z_limits` 写的是 `[-320, -410]`，比实测宽。**别信配置，信实测**：不要把笔压到 -385 以下。
3. **翻页机构动的时候，手离开走纸区。** 电机会真转（当前参数 30 档 / 3000ms）。

> ⚠ **Dry Run 管不到蓝牙翻页**（已实测确认）：`Dry Run` 只让**串口/机械臂**变成模拟，翻页走的是蓝牙那条独立通道。
> 所以「Dry Run + 勾着蓝牙翻页」= 臂不动、**电机真转**。

---

## 1. 开工前核对（不动任何东西）

| # | 核对项 | 当前状态 | 要求 |
|---|---|---|---|
| 1 | GUI 里「Dry Run」 | 你刚才开着 | **取消勾选**（不落盘，重启 GUI 也自动是真实模式） |
| 2 | 串口线 | `USB-SERIAL CH340 (COM4)` 已插 | 设备连接页**端口选 COM4**（协议 9600/8E1 固定不可改） |
| 3 | GUI 实例数 | 1 个（PID 12332） | **只开一个**，多开会抢串口和审计文件 |
| 4 | 启动目录 | — | 必须在项目根目录启动（状态文件都是相对路径） |
| 5 | 蓝牙模块 | 已配对、Windows 常驻持链路 | 通电即可；**首次翻页约 2~15 秒建链**，别看到慢就重复点 |

**当前参数快照**（`robot_config.json`，实测无需改动的部分）：

| 项 | 值 | 说明 |
|---|---|---|
| 中心点 | (0, 0, **-320**) | 回中心点用这个 |
| 抬笔 Z | **-325** | `z_layers[0]` |
| 落笔 Z | **-365** | `writing_plane_z`（`writing_plane_valid=true`，**写字全用这个值**） |
| 字号 | 60 mm | 每页 5 字 1 行 |
| 行距 | 8 mm | → **版面高 ≈ 68mm**（走纸目标，见第 5 步） |
| 速度 | 3 / 6 | 首次实测别调 |
| 蘸墨 / 顿笔 | 关 / 关 | **保持关**。顿笔会用到 -388，超出实测底限 |
| 翻页 | 蓝牙 30 档 / 3000ms | 时长要按第 5 步标定后改 |

---

## 2. 通信链路验证（机械臂不动）

| 步 | 操作 | 预期 | 失败怎么看 |
|---|---|---|---|
| 2.1 | 设备连接页 → 端口选 **COM4** → **连接设备** | 状态「已连接 COM4（9600/8E1）」 | 显示"串口打开失败"→ 别的程序占了 COM4；拔插 USB 后「刷新串口」重来 |
| 2.2 | 快捷操作 → **发送心跳** | 设备信息「最近响应」出现 **ACK**，「连续失败」= 0 | 一直 NO-RESP → 波特率/接线；确认转接器是这台的 COM4 |
| 2.3 | 快捷操作 → **查询位姿** | 返回当前坐标（不是空/超时） | 同 2.2 |

三项都过才往下。**这一步臂完全不动。**

---

## 3. 小幅度动作验证（臂动，全程抬笔）

| 步 | 操作 | 预期 | 看什么 |
|---|---|---|---|
| 3.1 | 快捷操作 → **回中心点** | 臂移到 (0,0,-320)（抬笔高度） | 方向对不对、有无异响、有没有接近限位 |
| 3.2 | 快捷操作 → **发送测试点** | 移到 (30,-30,-325) | 位移方向符合预期（X+ 向右 / Y- 向前 按你的坐标系） |

有异响、动作方向反了、或走到限位 → **停下，别继续**，先查坐标系/接线。

---

## 4. 落笔深度确认（臂动，笔尖触纸）

1. 进 **书写平面** 页。
2. Z 输入框填 **-365**（当前值）→ 点 **模拟悬停**。
3. 臂移到 (0,0,-365) 停住，**肉眼确认笔尖高度**：
   - 笔尖刚好触纸 → 直接进第 5 步；
   - 悬空 → 改更小（更负）的值，重悬停，直到刚好触纸；
   - 戳进纸里 → 改更大（接近 0）的值。
4. 满意后点 **保存并应用**（写进配置，重启仍生效）。

> ❌ **不要**为了让笔"更实"把值往 -385 以下调。也别开「顿笔」功能（会启用 -388 那一层）。

---

## 5. 走纸量标定（关键，机械臂完全不动）

**目标**：一次翻页走纸 = **一个版面高 ≈ 68mm**（字高 60 + 行距 8）。

> 为什么必须标：程序里**下一页和当前页的世界坐标完全相同**，靠"纸往前走一格"来错开。
> 走纸多了/少了 → 第 2 页整体错位，程序**不做任何补偿**。

**做法**（只动电机，臂不动）：

1. 在纸上沿笔尖正下方画一条**基准线**（或用尺子量）。
2. 发一次 `RUN30,3000`，量出纸实际前进了多少 mm，记作 `d`。
3. 反算：**新时长 = 3000 × 68 ÷ d**（档位固定 30 不要动，只调时长）。
4. 把新时长填进翻页条「时长(ms)」框；再发一次验证，直到 `d ≈ 68mm`。

*（这块我可以直接用命令行工具发指令，机械臂不会动——想让我跑就说一声。）*

> ⚠ **如果要我用 CLI 发指令，先把 GUI 的「蓝牙翻页」开关取消勾选**：BLE 模块一次只服务一个中心设备，
> GUI 翻过一次页之后会一直占着链路，两边会互相抢。

---

## 6. 单页试写（第一次真正落笔）

先跑最短路径，别一上来就多页。

| 设置项 | 值 |
|---|---|
| 每页字数 | **2** |
| 总页数 | **1** |
| 文本 | 改成 2 个字（例如「测试」） |
| 蓝牙翻页 | **先不勾**（单页用不到翻页） |

操作：**预检任务** → **开始书写**。

预期：臂落笔写 2 个字 → 抬笔 → 任务结束（`task_end result=ok`）。

看什么：字的大小（应 ≈60mm）、位置、笔画是否连贯、墨量是否合适、有没有丢笔画/断笔。

---

## 7. 多页 + 翻页整任务（全链路）

| 设置项 | 值 |
|---|---|
| 每页字数 | **5** |
| 总页数 | **2** |
| 文本 | 10 个字（`last_task.txt` 里已有「欲穷千里目更上一层楼」） |
| 蓝牙翻页 | **勾上** |
| 档位 / 时长 | `30` / **第 5 步标定出来的时长** |

操作：**预检任务** → **开始书写**。

**盯住第 1 页写完那一刻**，预期顺序：

```
第1页写完 → 抬笔（臂抬起避让）→ 电机转（走纸）→ 板子回 DONE → 轨迹清空 → 写第 2 页
```

**关键验收点**：第 2 页的字应该落在**第 1 页下方约 68mm** 的位置，**不是叠在第 1 页原处**。
如果叠上了 → 走纸量太大或太小，回第 5 步重标。

---

## 8. 出问题怎么查（我来做）

跑完（或中途卡住）**告诉我一声**，我去读 `logs/audit_<时间戳>.jsonl`，按这串事件定位：

| 事件 | 含义 | 出问题时的判据 |
|---|---|---|
| `connect` | 串口连接 | `result=fail` + `error_code=open_failed` |
| `task_preflight` | 排版预检 | `result=fail` → `error_code=layout` |
| `task_start` / `task_end` | 任务首尾 | `result=canceled` + `error_code=estop` = 被停/急停 |
| `send_point` | 逐点下发 | `device_response=NO-RESP` 或 `ack_valid=false` = 通信丢包 |
| `page_turn` | 翻页开始 | — |
| `page_turn_ble` | **蓝牙翻页结果** | `result=ok` + `detail="DONE 30 3000"` 才算成功；失败看 `detail` |
| `page_turn_result` | 翻页收尾 | `result=fail` → 任务以 `failed/page_turn` 结束 |

**蓝牙翻页失败时 `detail` 里的状态码含义**（`[link= open= comm= n=]`）：

| 现象 | 原因 | 处置 |
|---|---|---|
| `comm=1` (Unreachable) | 空口够不到模块 | 查模块供电 / 是否被手机连着 |
| `open=4` (SharingViolation) | 模块被别的程序占着 | 关掉 USB 蓝牙工具、或等我这边断开 |
| `link=0` (Disconnected) | 没有活动链路 | 模块不在线 |
| 建链 **25 秒**才失败 | 模块不在广播/不可达 | 检查通电 |
| 建链 **0.1~0.6 秒** | 链路已存在（正常） | — |

---

## 9. 记录与回滚

- 每一步的截图/审计文件我会归档到 `agent_memory/`。
- 想退回纯模拟：**取消勾选「蓝牙翻页」** → 翻页变回固定等待「翻页等待(ms)」。
- 想退回 Dry Run：勾上「Dry Run」+ 取消「蓝牙翻页」（否则电机还是会转）。
